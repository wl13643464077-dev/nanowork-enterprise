# 决策日志（自动模式，总指挥拍板并记录理由）

## 2026-07-26 v3 落地轮

1. **通道选择：标准通道。** 已有项目、路线图明确、多文件改动；未走完整通道因产品定义与设计已在 v1/v2 锁定。
2. **私有文件不入公开仓库。** 仓库 `wl13643464077-dev/nanowork-enterprise` 是 public。原书 PDF 属版权物，人格规格/商业认知按项目自身协议"永不进入公开分发包"。处置：gitignore + PRIVATE-FILES.md（含 SHA256 恢复清单）+ `verify_project.py` 公开检出模式；私有新产物通过会话文件直接交付本人。
3. **黄金样例用虚构人物张敏，不用斯文/王磊。** 回归测试需要稳定、可公开的样例；沿用 example.md 已确立的虚构演示设定，避免把真实人物的 B 级主张固化进公开回归资产。
4. **长图模板化以"逐字节一致"为验收线。** `build_long_image.py --check` 证明模板+数据可以无损重建原 HTML，排除了改造引入视觉回归的可能。
5. **`03-deliverables` 正式成品不覆盖。** Linux/Noto 渲染高度 7733 与原版 7736 有字体度量差；按项目规则"每次升级输出新文件名"，新渲染以 `-20260726-v2.png` 存于 02-design，替换正式成品留给本人视觉验收。
6. **修正 example.md 的 VX-15-V1 文案。** 原稿实测估算 20.6s，超出其自称的 15s 目标带（±20%），属于既有产物与自身质量门槛的矛盾；改为 17.9s 在带内稿并与黄金样例同步，避免示例教坏后续生成。
7. **清单语义调整：`MANIFEST.sha256` 只覆盖公开树。** `04-private-sources` 移出清单范围，使"本地补回私有文件"与"公开检出"两种形态都能通过校验；私有文件完整性由 PRIVATE-FILES.md 的 SHA256 单独保障。
8. **便携包 v4 重建而非补丁。** 技能源码新增黄金样例与回归脚本后，按项目惯例整包重建、重新生成包内清单与 zip SHA256，并做了从空目录解压→verify→install 的全流程实测；发现并修复了 `__pycache__` 混入包内的问题。
