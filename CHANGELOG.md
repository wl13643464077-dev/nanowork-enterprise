# 更新记录

## 2026-07-26 · 第三轮升级迭代（每日经营日报与检索基建）

- 新增每日经营日报引擎（涨跌→归因→建议三段式，orders/daily_ops 双口径，无数据不硬生成），每日 08:00 由调度器原子幂等推送给老板与运营负责人（站内通知 + 已绑定的飞书群）。
- 驾驶舱「今日经营诊断」卡新增昨日日报头条（涨跌标签 + 归因 + 建议 + 数据口径），并支持「今日不再显示」附反馈理由，次日自动恢复。
- RAG 容量护栏：知识库候选按热度预筛 200 篇、分块查询限量，消除全表拉取 embedding 阻塞事件循环与超 SQLite 变量上限报错；超限如实打点。
- 联网搜索多源化：博查 → Tavily → Serper → DuckDuckGo 兜底链，保留防注入包裹与失败降级说明；.env.example 新增三个检索源 Key 位。
- 验收：后端测试 341/341、TypeScript 检查通过、隔离扫描无高危、生产构建成功；日报引擎空库/有数据双冒烟通过。

## 2026-07-26 · 第二轮升级迭代（主动诊断、真 AI 工具箱与体验治理）

- 驾驶舱新增「今日经营诊断」主动卡片：最多 3 条（问题 + 维度 + 建议 + 一键问参谋），无数据或无权限时整卡隐藏。
- 切页体验根治：MainLayout 内容区加 Suspense 内容感知骨架（PageSkeleton），切换页面保留布局外壳，不再全屏白屏转圈。
- 新增 ErrorState 组件并接入 Platform / Recharge / Dashboard：整页静默吞错改为明确错误态 + 局部重试。
- 经营工具箱接入真 AI 生成（云雾 → Claude → 本地模板三级降级），结果页如实标注执行模式与模型；仍免费不消耗积分。
- 餐饮员工派活后新增四阶段进度（已派发→AI生成中→待审阅→已完成），生成中自动轮询任务状态，失败明示积分退回。
- 轮询治理：通知/状态轮询加 visibilityState 门控，后台标签页零请求；飞书 OAuth 轮询改指数退避（5 分钟封顶）。
- 深色主题对比度修复：帮助抽屉与通知时间的硬编码低对比色改为主题 token。
- 员工列表搜索 200ms 防抖 + 卡片 content-visibility，70 卡列表输入不再卡顿。
- 升级迭代报告新增第二轮附录（体验细节对标调研 + 变更明细 + 下一轮优先项）。
- 验收：后端测试 341/341、TypeScript 检查通过、隔离扫描无高危、生产构建成功。

## 2026-07-26 · 第一轮升级迭代（UI 与诚实化止损）

- 建立设计 token 层（`web/src/theme.css`）：间距 8px 网格、圆角 3 档、字号 7 档、阴影、动效时长/缓动、中性灰阶、语义色唯一化、层级与断点；新增全局 `:focus-visible` 焦点环。
- 登录页整页重做：删除 canvas 粒子连线与匀速轨道，改为 CSS mesh 动态渐变 + 0-800ms 分层进场编排 + 玻璃拟态卡片；原生支持明暗双主题与 `prefers-reduced-motion`。
- 新增 `AuthShell` 组件统一 Login/Register/Pending 三页的背景、品牌、卡片规格与按钮/输入框尺寸。
- 删除登录页编造经营数字（营业额/翻台率等）与「服务 6 个行业」夸大宣称，改为真实产品闭环展示。
- 移除纯装饰「AI音频」创作入口（前端 4 处 + 后端统计字段）。
- 经营工具箱诚实化：8 个工具卡的积分标注改为「免费 · 模板整理」，明示当前不调用外部 AI 模型。
- 客单价默认值 8000 → 120（餐饮业态），修正经营健康度分母；`analysis.js` 清除 `SELECT 1` 占位；资产未填估值不再编造默认值。
- 知识库 RAG 宣传改条件渲染：未配置 AI 密钥时明示降级模式，删除「知识库越大越准」的失实表述。
- `xlsx` 依赖从 CDN tgz 改为 npm 镜像包 `npm:@e965/xlsx@0.20.3`，解除构建单点故障与供应链盲区。
- 新增《纳米Work升级迭代报告-2026-07-26.md》：竞品调研更新、外部建议评估、本轮变更与后续路线图。
- 验收：后端测试 341/341、TypeScript 检查通过、隔离扫描无高危、生产构建成功。

## 2026-07-23 · 当前交付修订

- 建立独立的“纳米Work行业版”新项目，固定使用 3107 端口；所有旧项目保持只读。
- 接入 8 个餐饮分部、101—160 共 60 名餐饮数字员工，以及 Paihuo 内容生产部 0—9 共 10 位正式员工。
- 为 70 位员工补齐“单独派活、能力、工作方式、技能库、提示词、工作配置、岗位档案”七面板完整工作台。
- 为每位员工配置岗位专属的问题示例、派活输入、资料清单和交付检查项，移除与职责不匹配的通用占位问题。
- 接入统一文件中心附件：一次派活最多选择 6 个有权限的企业文件，并可另附 1 张内联图片；运行快照只保存安全引用与证据摘要。
- 为 10 位内容员工接入严格岗位 JSON 输出契约；契约不合格的结果不能采纳。演绎师以 HTML 为主交付，PPT 为附加连接器。
- 补齐内容生产的手动创作、立即自动执行、每日定时和每周定时；自动规则只接受与所选员工匹配的任务类型。
- 建立“生成 → 人工审核 → 采纳入素材库 → 人工发布 → 效果登记”闭环；任何入口都不会自动发布。
- 为内容自动任务增加上海时区排程、并发原子认领、唯一认领键、重复触发保护和超时运行恢复。
- 新增 8 个高频经营工具及可追溯运行记录，并将餐饮任务、内容任务、内容成品和工具运行统一纳入经营洞察。
- 将经营首页统一为“老板驾驶舱”，按角色约束数据范围，并移除界面中的演示提示与发布编号标签。
- 将账号删除改为可恢复的软停用：保留历史归属，停用关联自动任务，并保护关键管理员账号。
- 审查官继续作为独立质量闸门，不计入 70 位正式员工；默认不自动发布、不迁移旧数据库、不继承旧项目凭据。
