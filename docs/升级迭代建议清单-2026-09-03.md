# 纳米Work行业版 · 升级迭代建议清单（2026-09-03）

> 状态：**老板已确认范围 = 批次 A + 批次 B**（G1 选址→真接高德；B12 美团接口暂不做，只做 B2 过渡；定价只做 9800/年一档、字段预留；B1 先做租户级配额）。
>
> **批次 A 执行记录（2026-09-03，分支 `iteration/2026-09-batch-a`，未提交）**：A0–A8 全部完成 + A9 收口。独立复核：后端 1996 测试 / 1950 通过 / 28 失败（全部为基线内 Windows 环境专属：Claude CLI mac 路径、POSIX 权限位、运行时切 TZ；**新失败 0**；A0 修复使基线 17 条转绿）；web typecheck / lint:strict / check:isolation / check:tokens / check:ui-contract / check:paihuo-content-source 全部退出码 0；web build 成功无 chunk 超限。
> 待老板决策：① 积分权益口径（反算 6 万积分 = 800 图 / 35 视频 / 2 万次文本，销售口径 44 / 66000）；② 套餐到期是否锁功能（当前只提醒）；③ 续费是否再送 6 万积分（当前每次开通都送）；④ `.prettierrc` 是否加 `endOfLine: auto` 或加 `.gitattributes`（Windows CRLF 导致 format:check 无法整体绿）；⑤ 高德 Web 服务 Key 需申请并配置到服务器 env。
>
> **批次 B 执行记录（2026-09-03，分支 `iteration/2026-09-batch-b`，未提交）**：B1–B11 全部落地（B12 美团接口按决策不做，仅 B2 过渡）。
> - B1 按人配额/模型路由/月度预算：企业级模型路由覆盖层 + 月度 AI 积分预算（达 `budget_alert_ratio` 当日首次通知老板）+ 用量报表；按人月度配额本轮只预留字段与统计（precheck 记 quotaState，不拦截）。端点 `GET/PUT/DELETE /api/admin/model-routing`、`GET/PUT /api/admin/credit-budget`、`GET /api/admin/credits/usage`、`GET/PUT /api/admin/users/:id/quota`（`routes/admin-model-budget.js`）；表 `tenant_model_routing`，列 `tenants.monthly_credit_budget/budget_alert_ratio`、`users.monthly_credit_quota`；前端 `AdminModelRoutingPanel`/`AdminCreditBudgetPanel`；测试 `tenant-model-routing.test.mjs`、`credit-budget.test.mjs`。
> - B2 数据免手工录入：多门店/菜品/成本/会员 Excel 模板下载（表头与 `dataintake` TARGETS 一一对应）+ 拍照/截图识别报表（vision 计费，先估价再预览，低置信字段要求人工确认）。端点 `GET /api/data-intake/templates`、`GET /api/data-intake/templates/:key.xlsx`、`POST /api/data-intake/vision-estimate`、`POST /api/data-intake/vision-preview`；引擎 `data-intake-templates.js`/`data-intake-vision.js`/`data-intake-workbook.js`；env `DATA_INTAKE_VISION_TIMEOUT_MS`；前端 `DataIntakeTemplates`、`DataIntakeVisionImport`；测试 `data-intake-templates.test.mjs`、`data-intake-vision.test.mjs`。
> - B3 多门店组织模型：`users.store_id`（NULL=总部/全店）、业务表统一加 `store_id` 并回填默认店、三张按租户唯一的表重建为「租户+门店」复合唯一；请求头 `X-Store-Id` 经 `engines/store-scope.js`（ALS）落到查询；店长视角与连锁对比视图。端点 `GET /api/store-data/staff`、`GET /api/store-data/compare`；表 `store_daily_ops`；前端 `StoreSwitcher`、`StoreCompare`、`UserStoreField`、`api/store-context.ts`；测试 `multi-store.test.mjs`。
> - B4 契约分级 + 未达标草稿：`engines/contract-tiers.js` 按「租户数据模式 × 模型档位」决定 strict/standard/lenient；安全底线在任何档位都是硬门；全部尝试用尽时以「未达标草稿」交付而不交白卷（预授权仍按原路径处理）。前端 `EmployeeDraftCard`；测试 `contract-tiers.test.mjs`、`draft-on-failure.test.mjs`。
> - B5 内容连接器真正联网 + 时效性标注：趋势官/情报员/复盘师复用餐饮岗位检索链路（TinyFish → Claude WebSearch → 商业搜索 API → 受控 WebFetch），每条来源带抓取时间，按 `freshnessWindowDays` 标 stale，全部不可用时明确 unavailable。引擎 `content-live-research.js`、`content-source-freshness.js`；env `NANOWORK_RESEARCH_MODEL`；测试 `content-connectors-live.test.mjs`、`content-live-research.test.mjs`。
> - B6 合规半自动分发：排期提醒 + 一键复制发布包 + 发布登记回填 + T+1/3/7 自动催复盘（不调用任何平台 API，不自动发布）。端点 `PUT /api/content/:id/schedule`、`GET /api/content/:id/publish-pack`、`POST /api/content/:id/metrics`、`GET /api/content/:id/publish-assistant`；表 `content_publish_followups`、`content_publish_metrics`；调度器 `runContentPublishFollowups`/`runContentPublishScheduleReminders`；前端 `ContentPublishAssistant`；测试 `content-publish-followup.test.mjs`。
> - B7 移动端补齐：`Mobile.tsx` 重写为 5 Tab（首页/派活/任务/待办/我的，客户与内容并入首页二级），路由 `/m/*` 子路径驱动、旧 `/m?tab=` 深链兼容，`components/mobile/mobileRoutes.ts` 做桌面→移动深链映射；一句话派活复用 `POST /api/employees/match-team`；进度/结果/采纳/驳回全部走 PC 端同一后端。契约测试同步到新文件：`role-entry-contract.test.mjs`、`onboarding-ui-contract.test.mjs`（读取 `components/mobile/MobileHome.tsx`、`MobileCustomers.tsx`、`MobileContent.tsx`）。
> - B8 统一「待我处理」收件箱 + SSE：`GET /api/inbox`、`GET /api/inbox/count` 聚合内容审阅/系统审批/执行验收/返工，可见性完全复用各源头判定；`GET /api/events/stream` SSE（`retry`/`ping`/`Last-Event-ID` 回放、每用户最多 3 连接），`engines/event-bus.js` 进程内事件总线（多实例前需接 Redis/SQLite 投递，见 D6）。前端 `InboxDrawer`、`hooks/useRealtimeEvents.ts`；测试 `inbox.test.mjs`、`event-bus.test.mjs`、`events-stream.test.mjs`。
> - B9 审批策略下放企业老板：`PUT /api/sys/approval-policy` 编辑权 boss/admin/platform_super（按租户存储、在途任务沿用发起时锁定规则），`GET/POST /api/sys/approval-policy/preview` 服务端大白话预览；三条底线不可关闭。前端 `ApprovalPolicyPanel`（替换 `SystemApprovalRuleEditor`）；测试 `approval-policy-owner.test.mjs`、`approval-routing-policy-route.test.mjs`。
> - B10 RAG 向量自动回填 + 红点：`scripts/embed-backfill.mjs` 加游标断点/`--dry-run`/`--limit`（纯逻辑抽到 `engines/kb-backfill-plan.js`），调度器每日 04:00 `runKbVectorBackfillSweep`；端点 `GET /api/sys/kb/health`、`POST /api/sys/kb/backfill`（单次最多 20 条）；表 `kb_health_events`；前端 `SystemKbHealthCard` + 侧栏红点；测试 `kb-health.test.mjs`、`rag-chunks.test.mjs`。
> - B11 工作流/自定义智能体导入与前端入口：`GET /api/agents/:id/export`（不含租户内部字段，可再导入）、`POST /api/agents/import/preview`、`POST /api/agents/import`（支持本平台导出 JSON 与通用步骤式工作流 JSON，原文存 `source_workflow`）；引擎 `agent-workflow-import.js`；前端新页 `/agents`（`pages/Agents.tsx`，当前挂在 `marshals` 模块键下）；测试 `agents-import-export.test.mjs`。
>
> 全量验证（收口复核）：后端 2110 测试 / 2064 通过 / 28 失败 / 18 跳过，28 条失败全部命中 `baseline-known-failures.txt`（Windows 环境专属：Claude CLI mac 路径、POSIX 权限位/umask、运行时切 TZ、ffmpeg 真实渲染），**非基线失败 0**；web typecheck / lint:strict / build 退出码 0；本批次改动的 72 个 web/src 文件 Prettier 全部合规（9 个文件补跑 `--write`，仅换行差异）；check:isolation / check:tokens / check:ui-contract / check:paihuo-content-source 均退出码 0（`Admin.tsx` 因 Prettier 展开一度 2159 行 > 2155 上限，已在不改语义前提下收回 2154 行）；`preflight-production.mjs` / `provision-demo-tenants.mjs` / `import-video-samples.mjs` / `embed-backfill.mjs` `--help` 均退出码 0。
> 仍待老板决策：① 套餐到期是否锁功能（当前只提醒）；② 续费是否再送 6 万积分；③ **已决策：保留中转站口径价目表，毛利系数 1.5→2.0（2026-09-03，售价 = 中转站成本 × 2；常量 `credits.js` `CREDIT_MARGIN_FACTOR`，`minBalance` 预检下限随系数推导为 text 6 / image 160 / video 1600 分）**；④ 月度预算预警频率（当前达阈值当天只通知一次）；⑤ `/agents` 是否独立模块键（当前复用 `marshals`）；⑥ 内容效果 TOP 榜口径（按曝光/互动/转化哪一项）；⑦ 开店向导菜单图片识别是否计费；⑧ 多老板租户的催复盘收件人（当前发登记人 + 创建者 + 全部启用老板）。
> 毛利系数 2.0 下的权益反算（`estimateCreditEquivalents(60000)`，价目表口径 2k+1k token、默认路由模型，2026-09-03）：6 万积分 ≈ **333 张图**（gpt-image-2，180 分/张，成本 ¥0.90/张）/ **26 条 30 秒视频**（MiniMax-Hailuo-2.3 3 段×¥3.80，2280 分/条，成本 ¥11.40/条）/ **2727 次员工级文本任务**（deepseek-v4-flash，22 分/次，成本 ¥0.108/次）/ **1034 次老板级文本任务**（gpt-5.5，58 分/次，成本 ¥0.288/次）；6 万积分对应供应商成本 ¥300（系数 1.5 时为 ¥400），接口 `GET /api/recharge/equivalents` 的 `marginFactor` 同步为 2。
> 依据：① 后端 server 全量代码分析（141 文件/15 万行，483 端点，237 测试文件）；② 前端 web + desktop 代码分析（22 页面、~100 组件）；③ 项目全部文档（D-001~D-080 决策、07-26 审计、07-30 方案、08-07 三视角评审、state.md 08-28）；④ 两份会议纪要（2026-09-02 对外宣讲 62 分钟 + 内部复盘 23 分钟）。
> 硬节点：**2026-09-09 成都招商会**（招商定价 9800 元/年 · 5 账号 · 赠 6 万积分）。

---

## 0. 先看结论：四个必须正视的"口径差"

| # | 差距 | 宣讲/文档口径 | 代码现实 | 风险 |
|---|---|---|---|---|
| G1 | **选址数据源** | 宣讲："内置高德、窄门餐饮、红餐数据源，数分钟输出完整商圈分析报告" | `location-intelligence.js` 仅 OpenStreetMap Valhalla 等时圈 + 34 城硬编码正则；D-070 明确高德/点评/美团"仅为公开检索 lane，apiClaims=[]" | 07-26 审计已批评过"假功能损伤客户信任"，招商会现场若被追问即翻车 |
| G2 | **内容连接器联网** | 目录以 Horizon/MediaCrawler/social-auto-upload 命名技能 | `content-connectors.js` 趋势/情报/拆解/分发/复盘全部 `networkAccess:false`、`requires_live_data` | 内部承认"内容能力无差异化"，而本应成为差异点的"实时热点"实际不联网 |
| G3 | **产品定位** | 对外：数字员工、按岗位薪酬定价；对内复盘：AI 营销工具、卖积分与内容 | 代码重心确实是数字员工治理（两阶段计费、契约、溯源） | 9 月 9 日物料需统一口径 |
| G4 | **员工数量** | 宣讲"60 余个"；README 72；纪要/文档混用 70/72 | catalog：餐饮 61（101–161）+ 内容 10 + AI 带货员 1 = 72 | 小问题，但物料需统一 |

---

## 1. 批次 A：招商会前必须（截止 09-08，约 5 个工作日）

目标：现场演示不翻车、销售能讲清套餐、客户提的两条建议看得见。

| # | 功能 | 需求来源 | 代码现状（依据） | 工作量 |
|---|---|---|---|---|
| A1 | **5 问式新手引导 + 企业知识库初始化**：新租户首登引导"门店信息→业态/客单价→菜单→目标→第一单"，完成后自动写入 kb 与门店表，并派出第一份交付 | 纪要 F1（参会方建议、王磊待办首位）；三视角 P0-3 开店向导；02 方案 §1.4 客单价按业态引导 | 后端 `GET/PUT /api/meta/onboarding` 已存在；前端 `RoleOnboarding`（828 行）为角色指引而非数据初始化；`stores/dishes/costs` 表与 `dataintake` 已具备 | 中 |
| A2 | **数字员工"自我介绍"独立页 + 随时矫正 + 每周自动校验** | 纪要 F2（已采纳，待办第二位） | `tenant_specialist_overrides` 支持改名/停用；`employee-evolution.js` 有进化提案与老板决策流；调度器 `scheduler.js` 可挂周任务；缺"自我介绍"字段与校验任务 | 中 |
| A3 | **积分权益可视化**：充值页与员工卡显示"6 万积分 ≈ 800 张图 / 44 条视频 / 66000 次文本"，套餐页按 9800/年·5 账号呈现 | 纪要 F13、纪要 2 定价决策 | `credits.js:7-61` 价目表可反算；`Recharge.tsx` 仅展示套餐与记录；`recharge_packages` 无账号数/有效期字段 | 小 |
| A4 | **套餐模型补齐**：`tenants.plan` 增加有效期、账号上限（5）、赠送积分；到期/低余额通知 | 纪要 2 定价；后端建议 #11 | `tenants.plan` 仅字符串标签（`db.js:1441`）；`platform.js:59` 已判 `lowBalance<5000` 但无推送 | 中 |
| A5 | **演示环境上线**：临时服务器 + 域名 + HTTPS + 销售/测试账号 + 密钥轮换 + `TZ=Asia/Shanghai` + `JWT_SECRET` 固定 | 纪要 A5/A8/A10；D-078 公网 HOLD；11-release 前置清单 | `ops/README.md` 明确无一键部署脚本；三视角 P2-9 JWT 随机密钥重启掉线 | 中（运维） |
| A6 | **视频样板库 + 30 秒带货视频稳定可演示**：预生成 5–10 条样片入素材库；确认 MiniMax/海螺路由与计价 | 纪要 F7/F8（客户问"有无样板"答"暂无"）；D-058 | `ai-sales-video.js` 3×10s + ffmpeg 拼接已实现；H3 直连四门默认关闭；`mediaTemplates.ts` 有静态模板 | 小–中 |
| A7 | **海报指定汉字 100% 准确**：图片生成后用 resvg 叠加矢量文字（菜名/价格/门店名），而非依赖模型出字 | 纪要 F5（双方确认可通过技能预置） | `wechat-draft.js` 封面渲染已用 `@resvg/resvg-js` + Noto Sans SC 子集；可复用到 `generate-image` 与工具箱"产品图文" | 小–中 |
| A8 | **G1 止损**：选址岗位（102/104）输出中诚实标注数据来源（OSM + 公开检索），宣讲物料删除"内置高德/窄门/红餐"表述，或在本批接高德 POI 免费额度 | G1；D-019"固定模板不得冒充实时 AI" | `location-intelligence.js:5-58` | 小（文案）/ 中（接高德） |

---

## 2. 批次 B：成交转化关键（招商会后 2–4 周）

目标：兑现宣讲承诺、解决连锁客户"数据打通"与单店"太复杂"两大付费阻力。

| # | 功能 | 需求来源 | 代码现状 | 工作量 |
|---|---|---|---|---|
| B1 | **按人分配 token/模型配额 + 月度上限 + 超额申请流** | 纪要 F9（已对外说明规则）；大体量客户追问 | 积分只在 `tenants.credits`；`users.credits` 为遗留字段（`db.js:1237`）；模型路由按角色 `yunwu.js:79-113` 可按租户覆盖但不到人 | 中–大 |
| B2 | **数据免手工录入过渡方案**：多门店/菜品/成本/会员 Excel 模板 + 拍照识别报表/账单（vision 已计费） | 纪要隐含 5；三视角 P1 数据孤岛；02 §4.1 | `dataintake.js` 预览→映射→提交已具备；`extract.js` 有识图 | 中 |
| B3 | **多门店组织模型**：`users.store_id`、订单/任务/排班归属门店，店长视角权限，连锁总部对比视图 | 三视角 P1-5；后端 #9；04-路线图第三期 | `stores` 表存在但 `users` 无 `store_id`；`access.js` 仅 `manager_id` 树 | 大 |
| B4 | **契约分级 + 增量修复 + "未达标草稿"展示**：失败不再交白卷，10 分钟等待后至少可见草稿 | 三视角 P0-1；D-050 101–107 0/7 | `restaurant-output-contract.js` 两次定向修复；`marshalWork` 失败抛错释放 hold | 中 |
| B5 | **内容连接器真正联网 + 时效性标注**：趋势官/情报员复用餐饮岗位的 TinyFish/Claude 检索链路；产出带抓取时间戳与刷新策略 | 纪要 F11、G2；后端 #4 | `agentic-web-research.js` 链路已在餐饮岗位可用；`content-connectors.js:119-126` 关闭联网 | 中 |
| B6 | **合规半自动分发**：排期提醒、一键复制到剪贴板、发布登记回填、T+1/3/7 自动催复盘 | 纪要隐含 7（自动分发被小红书风控暂停）；07-30 方案 W6-7 | `content_publish_logs`、`POST pipelines/:id/metrics` 需人工填数；调度器可挂催复盘 | 中 |
| B7 | **移动端补齐**：一句话派活、进度/结果、采纳/驳回 | 前端 #3；三视角 P1-6 | `Mobile.tsx` 仅 4 Tab 子集 | 中 |
| B8 | **统一"待我处理"收件箱 + SSE 实时推送**：合并内容审阅/系统审批/执行验收/返工；任务终态主动推送 | 前端 #1/#2；后端 #8；三视角 P2-7 | 审批入口分散三处；通知 60s 轮询；SSE 基建已有 | 中 |
| B9 | **审批策略下放企业老板**（四选项 + 三条不可关闭底线 + 自然语言预览） | 后端 #7；切片 46 UI 待核对 | 策略仅 `platform_super` 可写（`system.js:978`） | 小–中 |
| B10 | **RAG 向量自动回填 + 管理员红点** | 三视角 P0-2 | `embed-backfill.mjs` 无进度/断点；`ai.js:406` 向量失败静默停止注入 | 小–中 |
| B11 | **工作流/自定义智能体导入与前端入口**：承接合作方从龙虾/火山/扣子迁移的工作流 | 纪要 F3（说话人 3 共创需求） | `/api/agents/*` 7 端点后端已有测试；前端入口状态未确认（W6-2） | 中 |
| B12 | **美团 SaaS 收银接口**：先建适配层与数据映射（堂食/外卖/点单/后厨），接口拿到即接 | 纪要 F4（宣讲已承诺"正在申请"；内部"前期暂不做"）；连锁客户核心痛点 | 无对接；`orders/order_items` 已有表 | 大（需资质） |

---

## 3. 批次 C：差异化与中期（1–2 个月）

| # | 功能 | 需求来源 | 代码现状 | 工作量 |
|---|---|---|---|---|
| C1 | **LLM 版经营日报/周复盘**（带证据引用的可解释归因） | 后端 #5；美团"主动管店"竞品压力 | `daily-digest.js` 为固定规则 4 分支 | 中 |
| C2 | **内容复盘闭环产品化**：上传真实数据截图→自动归因→优化下一轮选题（宣讲已讲，是内容模块唯一差异点） | 纪要"应该做"；G2 | 复盘官 #9 事实门禁已有（D-047），缺截图识别入口与闭环 | 中 |
| C3 | **玩法/指令/关键词教程与"一键出活"模板** | 纪要隐含 1/2（"用的人比知道的人少"、"用不会工具"） | `featureGuideCatalog.ts` 767 行已有指引框架；工具箱 11 工具 | 小–中 |
| C4 | **高德/百度 POI + 人口热力 + 租金数据**增强选址岗位 | G1；后端 #14 | 仅 OSM | 中 |
| C5 | **驾驭舱异常 → 直接推荐员工并预填派活** | 前端 #7 | 诊断卡只跳 `/advisor` `/analysis` | 小 |
| C6 | **员工产出结构化回流业务台账**（菜单→菜品草稿、排班→排班页、活动策划→建活动） | 前端 #5 | `ArtifactActions` 仅导出/入档 | 中 |
| C7 | **代理商/服务商多租户与白标**（数字底座打包全案） | 纪要"可以做"（运营侧诉求） | 平台超管已能管租户；缺代理商层级与品牌化 | 大 |
| C8 | **单店试用/月付档 + 阶梯价 + 代采折扣报表** | 纪要 2 说话人 7；大客户代采折扣 | 依赖 A4 套餐模型 | 小–中（依赖定价决策） |
| C9 | **报表导出中心**（周/月经营报告一键 docx/pdf） | 后端 #13 | `skillrun.js` 已能出四格式 | 小–中 |
| C10 | **生产债务三件**：持久化人工对账处理器、异步视频后台主动轮询、高成本写请求幂等键 | D-030；12-retro | `pending_reconciliation` 仅安全状态 | 中 |
| C11 | **登录安全**：手机验证码/飞书 SSO/2FA | 后端 #12 | 注册无验证码；飞书 OAuth 绑定已有 | 中 |

---

## 4. 批次 D：工程质量（与 A/B 并行，低调推进）

| # | 项 | 依据 |
|---|---|---|
| D1 | **Windows 兼容**：`agentic-web-research.js:45` 写死 `/Users/wanglei/.local/bin/claude`，win32 进程树杀不干净；桌面端 LAN http 被拒（`config.cjs:53-55`） | 后端 #7；前端 §9.4 |
| D2 | **员工编号区间去硬编码**：`BETWEEN 101 AND 161`、CHECK 约束、`idx===8` 魔法数分散多处 | 后端 #9；前端 §9.2 |
| D3 | **前端测试补位**：vitest 覆盖 `api/client`、`useQuery`、`useStreamingTask`、presentation 纯函数；Playwright 登录→派活→看结果冒烟 | 前端 #9；CHANGELOG 记录测试文件损毁事故 |
| D4 | **巨型文件拆解并纳入棘轮**：`content.js` 7416 / `ContentFactory.tsx` 4715 / `EmployeeWorkbench.tsx` 3829（后两者不在契约上限表） | 前端 #10；后端 #4 |
| D5 | **README/CHANGELOG 口径对齐**：工具箱 8→11 且计费、员工 72、去作者本机路径、补桌面 0.1.0 条目 | 前端 §9.3 |
| D6 | **SQLite 单写者与进程内状态**：为后续多实例做准备（限流/并发池/查单节流外置），RAG 迁 sqlite-vec | 后端 #1/#2/#3 |
| D7 | **术语清理**：`Execution.tsx` 馆主、`Activities.tsx` 品鉴会等旧行业词；CRM 漏斗改"新客/回头客/储值会员" | W6-4/5；三视角 |

---

## 5. 需要老板拍板的决策

1. **批次范围**：A 全做？B/C 各选哪些？D 是否并行？
2. **G1 选址数据源**：招商会前是"改口径"还是"真接高德"？
3. **美团接口**：宣讲已说"正在申请"——是否启动申请与适配层（B12），还是继续"前期暂不做"？
4. **定价档位**：是否增加单店试用/月付档（C8）？影响 A4 套餐模型的字段设计。
5. **对外定位口径**：以"数字员工"为主线（与代码重心一致）还是"AI 营销工具"？
6. **B1 按人配额**：是否作为 9800 套餐的标配能力（宣讲已讲），还是企业版增值？

---

## 附：需求 → 功能项映射速查

| 纪要需求 | 对应项 |
|---|---|
| F1 新手引导 5 问 | A1 |
| F2 自我介绍页 + 每周校验 | A2 |
| F3 工作流迁移共创 | B11 |
| F4 美团 SaaS 接口 | B12（过渡 B2） |
| F5 海报指定汉字 | A7 |
| F6 图片局部编辑 | 已有，纳入 A6 演示核对 |
| F7/F8 30 秒视频 + 样板 | A6 |
| F9 按人 token/模型 | B1 |
| F10 权限分级 | 已有（模块矩阵 + 数据范围），演示核对 |
| F11 抓取时效性 | B5 |
| F12 多模型适配 | 已有路由，B1 补 UI |
| F13 权益量化 | A3 |
| F14 演示环境 | A5 |
| 隐含：傻瓜式/玩法教程 | C3、A1 |
| 隐含：低门槛试用 | C8 |
| 隐含：免手工录入 | B2、B3、B12 |
| 隐含：合规分发 | B6 |
| 隐含：内容差异化 | B5、C2 |
| 隐含：代理商白标 | C7 |
