# 发布包

## 2026-08-08 当前发布判断

| 层级 | 结论 | 说明 |
| --- | --- | --- |
| 本地无付费凭证预览 | **GO** | 完整业务界面、员工档案、派活、联网证据策略、自动采纳与 AI 带货员任务页可预览；不产生视频供应商费用 |
| 自动化回归 | **GO** | 服务端 `1223/1223` 通过；本轮关键链 `230/230` 通过 |
| 真实文本/联网通道 | **CONDITIONAL GO** | 需使用新密钥完成当前部署环境显式连接验证；必需联网岗位无证据时失败关闭 |
| MiniMax 真实 30 秒成片 | **CONDITIONAL GO** | 代码链已完成；需完成云雾端点/计价验证、密钥轮换和一次受控小额 canary |

当前本地交付实例：`http://127.0.0.1:3107`。服务进程工作目录为本项目 `server/`，显式使用 `server/data/nanowork-preview.db`，Scheduler 关闭；`/api/health` 返回 200，老板登录、11 人内容 roster、15 项连接器和 AI 带货员完整岗位档案接口均已通过冒烟验收。

本轮可发布功能：

- 平台超级管理员配置企业员工审批策略；低风险内部产物自动采纳，高风险/外发/付费/不可逆动作保持老板门禁。
- 60 名餐饮员工、10 名 Paihuo 内容员工与 1 名 NanoWork 原生 AI 带货员，都有完整能力、工作方式、技能、提示词、配置、岗位档案和运行绑定。
- 内容生产仓保留 11 名数字员工、素材库、模板库和任务中心；独立 AI 图片/视频/PPT 入口已隐藏，底层兼容 API 与历史数据仍可读。
- AI 带货员从上传图片、脚本/分镜、分段生成、下载、30 秒合成、积分账本到媒体验收完整接线，前端支持关窗后继续轮询与任务恢复。
- 内容岗位统一支持租户知识库，需实时信息的任务调用联网搜索并保留证据快照。

发布包不包含：生产部署、Git 提交/推送、真实付费视频调用、自动对外发布或对历史待审任务的批量改状态。

日期：2026-07-31  
通道：第二轮完整通道 + 第三轮标准通道 + 第四轮标准通道  
发布动作：本轮只形成发布判断与交接包，未部署生产、未推送 Git、未调用真实外部服务。

## 修复前基线

- 本地页面与一次受控云雾文本联调可运行。
- 自动化基线为 366/366，但内容员工配置、强制联网、严格契约、后台计费/并发、角色权限和底稿统计仍有阻断。
- 修复前生产判定：**NOT GO**。

## 第四轮当前发布结果

| 发布层级 | 结论 | 说明 |
| --- | --- | --- |
| 本地无密钥演示 | **GO** | 可启动、登录、浏览业务页面和已保存数据，不产生真实外部费用 |
| 自动化本地回归 | **GO** | `491/491` 通过，0 失败；全部静态质量门通过 |
| 生产 | **CONDITIONAL GO** | 外部链路仍须凭证验收；持久化对账、视频主动轮询和高成本请求幂等仍待完成 |

第四轮真实浏览器与服务健康最终验收已通过：3107 最新服务健康，Scheduler 关闭且无真实 AI 通道；管理员概览、接口管理和系统页展示 8 通道真实就绪状态，无连接状态误报，新鲜浏览器 error 日志 0。

## 本轮交付范围

- 启动、密钥和上游错误安全边界。
- AI 路由权限、并发、两段式计费和后台租约。
- 内容员工完整执行配置、强制联网证据与严格输出契约。
- 模板/契约底稿未完成语义及正式产量统计。
- 老板日报数据范围、零目标语义、移动端与平台角色入口。
- 工具箱完整员工上下文和结果范围。
- 60 名餐饮员工逐岗位严格输出契约、合法样本与非法反例。
- 409 项技能的来源快照、岗位、版本、指纹、离线样本和分层验证证据，并接入工作台、执行快照、提示词与前端。
- 13 项内容连接器运行登记；历史实际 8 个 `catalog_only` 归零，本地辅助运行与需授权外部链路 fail closed。
- 真实生产应用装配与完整中间件链 HTTP 集成回归；数据录入 `system` 模块门禁 fail closed。
- 8 通道零联网运行就绪矩阵；配置、验证和连接状态分层，显式验证证据受进程、TTL 与配置指纹约束。
- 占扣结算/释放数据库权威回读；跨租户完整性校验、停用租户恢复和坏记录/子系统故障隔离。
- 对应自动化测试、评审与交付文档。

## 本轮四个 P1 发布闭环

- 内容仓固定模板只作为参考/草稿，不冒充实时 AI、实时扫描结果或真实经营数据。
- 内容自动化返回 `202 + runId + pollUrl`，支持按 `runId` 精确轮询和同一次运行幂等复用；手动/定时执行统一撤权门禁，费用估算、占扣和生成采用同一实际模型。
- 支付报文拒绝重复关键节点，渠道结果绑定本地商户订单，微信签名响应只接受五分钟新鲜窗口。
- RAG 后台任务执行前预授权、按真实用量结算、与其他 AI 路径共享并发额度，并覆盖超时释放和服务重启恢复。

本轮定义的本地代码层 P0/P1 发布阻断已全部关闭。

## 第二轮真实浏览器验收证据（历史保留）

- 15 个核心路由无错误。
- 7 项禁用旧文案均未出现。
- 内容生产仓 10 位员工全部确认；趋势官七面板和岗位专属操作引导确认。
- 新标签页浏览器控制台 console error 为 0。

第三轮新增的输出契约、技能证据和连接器状态页面验收：**通过**。餐饮 #101 页面展示稳定 URN 严格契约与审批边界；餐饮和内容技能页均展示“目录与执行注入已验证、业务效果待实测”；趋势官连接器为 `requires_live_data / verified_input_assist`，缺实时数据阻断且未出现 `catalog_only`。

## 生产发布前置清单

- [ ] 在服务商后台撤销或轮换本轮已暴露的云雾密钥。
- [ ] 使用新密钥在隔离环境完成云雾文本生成与联网检索成功/失败验收。
- [ ] 使用支付沙箱完成下单、支付通知、主动查单、取消/关单、退款与重复通知验收。
- [ ] 使用飞书测试租户完成消息发送、失败重试、幂等和敏感信息检查。
- [ ] 使用受控测试账号完成外部内容发布、失败回滚和重复提交验收。
- [ ] 确认真实链路测试后无明文密钥、无悬挂 hold、无孤儿后台任务、无重复业务记录。
- [x] 最终自动化测试数已确认：`491/491` 通过，0 失败。
- [x] 第四轮真实浏览器和服务健康最终验收通过；`/api/health` 为 `ok=true`、`db=up`，8 通道矩阵状态准确且浏览器 error 日志为 0。
- [ ] 实现持久化人工对账处理器，并验证重启、重试、人工处置和审计闭环。
- [ ] 实现异步视频后台主动轮询，不依赖媒体列表访问推动终态。
- [ ] 为高成本写请求实现请求级幂等键与重复提交回放语义。
- [ ] 由发布负责人复核环境变量、数据库备份、回滚步骤和责任人。

## 启动与验证

本地无密钥启动沿用项目既有启动命令。发布前至少执行：

```bash
npm run verify
```

并对照以下文件复核：

- `docs/team/09-qa.md`
- `docs/team/10-review.md`
- `docs/team/11-full-regression-matrix.md`
- `docs/team/12-iteration-upgrade-report.md`

## 回滚策略

1. 生产外部链路验收失败时立即关闭相应环境开关或连接器，不降级为“伪成功”模板。
2. 停止 Scheduler 和后台任务领取，等待在途任务进入终态。
3. 核对并释放可安全释放的悬挂占扣；结算状态不明的保留 `pending_reconciliation`，禁止强行改成功。
4. 恢复发布前数据库备份和上一个已验证版本。
5. 轮换可能暴露的凭据，并保留脱敏审计记录。

## 已知剩余债务

- 409 项历史技能的目录与执行注入契约已验证，真实业务效果仍待真实业务样本。
- 真实采集与外部发布连接器仍需账号授权和凭证；缺少条件时保持 fail closed。
- 云雾、支付、飞书和外部发布链路待沙箱/生产凭证验证。
- `pending_reconciliation` 已有安全状态，但缺持久化处理器和人工操作台。
- 异步视频任务仍需独立后台主动轮询；高成本写请求仍需请求级幂等。

## 总指挥拍板

决定：本地无密钥演示 GO；自动化本地回归 GO；生产 CONDITIONAL GO。  
理由：第四轮三项生产可信度缺口已由 `491/491`、静态质量门、健康与真实浏览器关闭；但外部真实链路仍未使用轮换后的授权凭证复验，且持久化对账、视频主动轮询和高成本请求幂等仍是后续生产债务。  
下一步：完成生产前置清单后再做受控放量；当前可继续使用无密钥 3107 本地演示。

## 第五轮本地交付补充（2026-07-31）

- 交付形态：3107 本地无密钥预览，Scheduler 与后台向量任务关闭。
- 最新构建：Vite 3879 个模块；服务端 `522/522`；三角色真实业务流 4/4；70 名数字员工 70/70。
- 角色边界：老板完整岗位档案；运营/员工仅派活、进度和业务结果；员工无公司级计划和管理动作。
- 安全边界：内部岗位档案回显会被拦截并阻止采纳；业务流按租户和组织树投影。
- 保护边界：未修改旧项目，未 commit/push，未写入或复用旧云密钥；两份保护文件哈希不变。
- 发布判定：本地演示 **GO**；生产 **CONDITIONAL GO**，待轮换后的授权凭据验收。

## L5 真实云 API 交付补充（当前有效，2026-07-31）

### 当前发布矩阵

| 交付层级 | 结论 | 证据与边界 |
| --- | --- | --- |
| 真实云数字员工主路径 | **GO** | 70/70；餐饮 60/60、Paihuo 内容 10/10，全部真实 API、契约、主产物、人工审阅、计费和业务流终态完整 |
| 真实云功能主路径 | **GO** | 27/27；25 个真实交付和 2 个权限拒绝反例全部通过 |
| 产物质量门 | **GO** | 70/70 已采纳主产物通过 8 项只读审计；无模板占位冒充、无内部档案标记、无声称已执行外部动作 |
| 服务端自动化 | **GO** | `645/645` 通过，0 失败 |
| 三角色最终浏览器 | **PENDING** | 待主代理补齐老板/管理层/员工最终行走、截图和新鲜控制台日志 |
| 整体生产放量 | **CONDITIONAL GO** | 支付、飞书、外部发布、视频等本轮未授权的不可逆/高成本链路仍保持 fail-closed |

### 本轮实际交付物

- `artifacts/real-employee-matrix-l5-full-v2.json`：70 岗最终真实云矩阵，SHA-256 `e86241c338dff8464e4eb858871762a855b7521729a39ac85c35300f49f371c5`。
- `artifacts/real-employee-output-quality-audit.json` 与 `.md`：70 份主产物质量审计，JSON SHA-256 `662edc056410cfa2aff27a5a0c95440183a66019bc279106432395a56df6fa8f`。
- `artifacts/real-feature-matrix-l5.json`：27 项功能矩阵，SHA-256 `dc6d3b79f30756ffb0cc12ae6c59b0057b08a40a33b53641fa1c3c1ed6f80457`。
- `docs/升级迭代报告-2026-07/08-真实云API全链路验收与迭代报告-2026-07-31.md`：人可读的完整验收与边界报告。

### 账务与安全交接

- 70 岗最终矩阵实扣 11,065 积分、成本 ¥73.5226；25 个功能真实交付实扣 1,151 积分、成本 ¥7.5706；合计 12,216 积分、¥81.0932。
- 隔离验收库共有 174 条 hold 历史记录，全部已进入 `settled`；当前 `held=0`，无遗留预授权占扣。
- 失败、质检不通过或人工驳回路径保留 0 分结算/释放审计记录；不删记录，也不让 `held` 永久挂起。
- 云 API 凭据只由运行进程环境注入。交付物不包含密钥、登录密码、完整提示词或员工内部档案。

### L5 收口清单

- [x] 70/70 真实云数字员工矩阵。
- [x] 70/70 真实产物质量审计。
- [x] 27/27 功能矩阵。
- [x] `645/645` 服务端回归。
- [x] 计费、积分流水和 hold 权威对账；当前悬挂 hold 为 0。
- [x] 报告与证据文件不保存密钥或密码。
- [ ] 主代理完成三角色最终浏览器验收并回填截图/日志。
- [ ] 生产发布前单独完成支付、飞书、外部发布和其他未授权链路的沙箱/受控验收。

## 最终真实收口发布清单（当前权威，2026-07-31）

上方 L5 清单记录进入最终浏览器验收前的历史快照；当前交付以本清单为准。

- [x] 70/70 真实云数字员工；餐饮 60/60、Paihuo 内容 10/10。
- [x] 70/70 已采纳主产物通过 8 项质量审计。
- [x] 27/27 功能场景；25 个真实交付、2 个精确权限边界。
- [x] 最新完整 `npm run verify` 的服务端回归快照 `696/696`；类型、隔离、token、UI 契约、严格 lint、格式和生产构建全部通过。
- [x] content:9 事实门禁已进入回归：无来源百分比、权重、行业基准和阈值不能进入可使用链路；诚实待补项与有来源事实保留。
- [x] 老板、管理层、员工真实账号浏览器复验；控制台错误 0。
- [x] 员工执行页跨用户排行榜泄漏修复并复验。
- [x] 预览库严格对账 6 条内容、6 条审批、2 条员工任务、4 条自动化运行；普通任务提交错位 0。
- [x] 双库当前 `held=0`；3107/3108 健康。
- [x] assets 401 完成定向、全资源与压力复验，当前不可复现。
- [ ] 支付、飞书、外部发布、视频在沙箱/受控账号下完成独立验收。

发布结论：本地演示、真实云文本/vision 主路径、自动化回归与三角色应用链 **GO**；整体生产 **CONDITIONAL GO**。未完成项保持 fail-closed，不能通过演示数据、静态成功文案或手工改状态绕过。

## 最终失败复跑交接（2026-07-31）

- [x] 复盘官历史失败规则以真实云重新运行；运行 #10、内容 #142、审批 #18、hold #22 可追溯。
- [x] 真实用量 5,095 输入 / 591 输出 token；实扣 5 分；当前悬挂 hold 0。
- [x] 老板审批前为待审核，审批后为可使用；资产 1、知识库 1、发布 0。
- [x] #7/#8 驳回与 #9 失败保留，且没有资产/知识库/发布副作用。
- [x] “待执行→生成中→待账务对账→待审阅/质检→可使用”的唯一状态语义完成前后端和数据库回归。
- [x] 最新完整工程门禁 `696/696`，0 失败。

## Paihuo 完整能力复用与自定义审批发布补充（当前权威，2026-07-31）

- [x] Paihuo 0→9 完整团队流水线百分之百复用派活岗位运行包：能力、工作方式、技能库、提示词、工作配置、岗位档案及真实处理器上下文。
- [x] 老板/管理员/平台超管可在每次新建流水线时选择高效、关键节点、内部自动或逐工位自定义审批；默认只在分发官 8 形成发布包后停审一次。
- [x] 管理层没有修改审批点的入口；员工无法访问内容生产仓或岗位内部档案。
- [x] 无论是否关闭内部停审，外部发布仍保持独立人工授权与 fail-closed，不会因流水线自动接力而自动发送。
- [x] 特殊供应商生成、素材落库、预授权结算与异常恢复具备幂等和证据对账；当前 `held=0`。
- [x] 最新服务端完整回归 `1070/1070`，类型、隔离、token、UI 契约、lint、format、build 全部通过；3107/3109 健康。
- [x] 老板、管理层、员工三角色浏览器复验通过；本轮仅验证 UI 与权限，未创建业务任务、未调用云 API、未扣积分。

发布结论：本地 Paihuo 完整能力流水线和老板自定义审批 **GO**；支付、飞书、外部发布、视频等未授权外部链路继续 **CONDITIONAL GO / fail-closed**。

## 2026-08-11｜本地演示交付补充

- [x] 任务 #39 已恢复并形成可读报告；恢复过程不重新调用模型、不新增积分费用。
- [x] 页面常驻 PDF #7、Word #12、Excel #13 三个权威下载入口，三端点均为 HTTP 200。
- [x] 三种文件均完成中文视觉验收，不能只以“文件生成成功”或“文本可抽取”判定通过。
- [x] 最新离线 `npm run verify` 退出 0；本地健康接口返回 `ok=true, db=up`。
- [x] 项目运行于 `http://127.0.0.1:3107`，调度器关闭。

发布结论：任务 #39 与报告/文件演示链 **GO**；外发、付费和不可逆动作继续沿用独立授权边界。

## 2026-08-28｜本机 3107 发布包

### 放行范围

- 提交当前产品升级的源代码、测试、脚本、桌面端配置和正式文档。
- 使用最新 `web/dist` 重启本机 Node 服务，使 `http://127.0.0.1:3107/` 提供本轮构建。
- 不 push、不对外发布、不执行付款、飞书发送、内容外发或其他不可逆业务动作。

### 发布证据

- `npm run verify`：1,791 tests；1,789 pass、0 fail、2 skip；全部 Web/静态质量门与 production build 通过。
- 提交前终检：staged 路径、体积、秘密模式、`git diff --cached --check`。
- 部署后终检：SQLite 备份完整性、`GET /api/health`、首页与构建哈希、3107 监听进程。

### 回滚

1. 保留发布前 Git 提交锚点与 SQLite 一致性快照。
2. 部署冒烟失败时停止新进程，不继续对外暴露；恢复上一个已验证代码版本与数据库快照后再启动。
3. 未完成服务器、域名、HTTPS、生产数据迁移和备份恢复演练前，公网生产状态始终为 **HOLD**。
