# QA 记录

## 2026-08-08 当前有效验收

| 质量门 | 结果 |
| --- | --- |
| 服务端全量自动化 | `1223/1223` 通过，0 失败 |
| 本轮关键业务链 | `230/230` 通过，覆盖审批路由、自动采纳、员工工作台、联网/KB、媒体审查与 30 秒视频 |
| 70 名原派活员工矩阵 | 餐饮 60 + Paihuo 内容 10 通过；低风险自动采纳，高风险按策略人工验收 |
| NanoWork 原生内容员工 | AI 带货员 `idx=10` 完整岗位包通过；内容组合 roster 为 11 人 |
| 内容连接器 | 原 Paihuo 13 项不变，AI 带货员新增 2 项，共15项目录、运行登记和业务入口一致 |
| 工程静态门 | TypeScript、租户隔离、凭证扫描、设计 token、UI 契约、严格 lint、Prettier 与生产构建全部通过；`ContentFactory.tsx` 已由 5353 行拆至 4700 行 |
| 当前工作区运行冒烟 | 3107 健康 200；老板登录成功；内容 roster 11 人；连接器 15/15；AI 带货员完整档案字段齐全 |

业务验收结论：

- 数字员工每次执行都锁定能力、工作方式、完整技能库、提示词、工作配置、岗位档案和运行绑定；老板/管理员可查内部档案，普通员工只看派活与授权结果。
- 派活支持“一个任务目标 + 可选素材”；旧字段仍兼容，岗位默认值由服务端补齐。
- 内容岗位 0–2 联网证据必需；3–10 均允许联网，任务含“最新/实时/官方/趋势/竞品”等信号时触发；所有岗位保留租户级知识库证据。
- 低风险、真实来源、契约合格且账务已结算的内部产物可自动采纳；高风险、外部发布、付费或不可逆动作始终进老板门禁，不自动发布。
- AI 带货员支持图片素材、30 秒脚本/分镜、三段 10 秒（H3 为两段 15 秒）、下载、FFmpeg 合成、预授权、成功结算、失败退回、媒体验收与结果轮询。

> 验收边界：本轮视频端到端测试使用可注入的本地供应商/合成器，验证真实代码路径但不调用外部付费 API，真实 token 费用为 0。

日期：2026-07-31  
修复前基线结论：受控本地真实联调通过；生产发布暂缓。

> 本文从“自动化基线”到“隔离环境清理”为修复前基线证据；第二至第四轮均保留为历史结果。当前有效结论以紧接本说明的“70 人逐岗出活与内部档案权限 QA”小节为准。

## 70 人逐岗出活与内部档案权限 QA（当前有效）

| 质量门 | 结果 |
| --- | --- |
| 70 人逐岗 L4 矩阵 | `70/70 PASS_OFFLINE_PIPELINE`；餐饮 60/60，内容 10/10 |
| 服务端全量自动化 | `509/509` 通过，0 失败 |
| 角色与旁路专项 | `110/110` 通过，0 失败 |
| TypeScript / 隔离 | 通过；61 张隔离表，36 处动态查询保留人工复核提示，0 失败 |
| UI 与静态质量门 | 设计 token、界面契约、严格 lint、Prettier、Vite 构建、`git diff --check` 全部通过 |

### 逐岗出活证据

- 70 名员工均通过正式 `createApp` HTTP、登录鉴权、租户范围与状态、模块门禁和 AI Guard；每人均完成派活、异步终态、岗位契约、非空主产物、数据库落库、人工审批/审阅和积分结算。
- 餐饮 #101-#160 为 60/60；内容 #0-#9 为 10/10。内容产物下载均为 200；分发官没有自动外发；跨租户读取返回 404；开放占扣为 0。
- 证据等级为 `L4_FULL_PRODUCTION_HTTP`，结论为 `PASS_OFFLINE_PIPELINE`。provider 为 `deterministic_mock`、模型为 `deterministic-mock/no-network`；报告里的 providerCalls=70、webEvidenceCalls=10 均为进程内确定性注入，不是云端请求。外网尝试 0、真实 AI 费用 0。
- 证据文件：`docs/升级迭代报告-2026-07/evidence-70-employees/full-production.json`。

### 最终角色矩阵

- `boss/admin/platform_super`：可查看能力、技能库、完整提示词、工作方式、工作配置、岗位档案和内部来源/修订；实际执行与数据库审计使用完整快照。
- `ops_director/manager/staff/sales/partner`：只保留公开岗位摘要、“派活与任务”、本人或授权范围内进度和业务结果；运营负责人原有审阅权不变，但无内部档案查看权。
- 服务端正向白名单覆盖：餐饮/内容工作台、员工与内容目录、分部列表/详情、任务状态、协作任务、内容与媒体列表/详情、生成响应、自动化运行、产出分析与钻取、工具箱、提示词指南和公共技能目录。
- 前端受限角色不渲染能力、工作方式、技能库、提示词、工作配置和岗位档案页签；这只是体验层，安全边界由上述服务端投影承担。

### 双角色真实页面与接口验收

- 老板 `guan`：餐饮员工 #101 实际返回 7 项能力、7 项启用技能；内容员工 #0 实际返回 5 项能力、13 项技能；两类工作台均显示“派活与任务、能力、工作方式、技能库、提示词、工作配置、岗位档案”7 个页签。
- 运营 `yunying`：两类工作台接口均为 `canViewInternalProfile=false`，能力与技能为空，提示词、工作方式、工作配置、岗位档案和来源均为服务端脱敏；页面均只显示“派活与任务”。
- 内容生产仓对运营只显示岗位摘要、运行统计和“打开派活工作台”，不显示能力行或“完整档案”；手动、立即自动、定时任务入口保持可用。
- 运营请求 `/api/sys/marshal-prompts/status` 返回 403；真实访问 `/system?tab=prompts` 时不出现“提示词模板”入口并回落到“系统概览”。
- 3107 健康为 `ok=true`、`db=up`，Scheduler 关闭，AI 为本地模板底稿模式；新鲜浏览器 error/warning 日志为 `[]`。最终保留老板餐饮员工完整工作台供查看。

### 证据边界

- 本轮没有用真实云模型逐岗生成，不能把 L4 管线通过写成 L5 内容质量、延迟、数据新鲜度或供应商稳定性通过。
- 未调用真实 AI、检索供应商、支付、飞书或外部发布服务；未提交或推送 Git。

## 第四轮当前 QA 结论（历史基线）

### 自动化与静态质量门

| 质量门 | 第四轮结果 |
| --- | --- |
| Node 全量自动化 | `491/491` 通过，0 失败 |
| TypeScript | 通过 |
| 多租户隔离检查 | 61 张表；36 处动态查询进入人工复核提示；0 失败 |
| Vite 生产构建 | 通过；3,862 个模块完成构建 |
| Lint | 通过，0 warning |
| Format | 通过 |
| 服务端 JS 语法 | 全部通过 |
| `git diff --check` | 通过 |

### 第四轮专项验收

- 生产中间件链：新生产应用 factory 的真实 HTTP 集成测试覆盖 `auth → tenant scope → tenant gate → module guard → AI guard → Router`。未登录 401、停用账号 401、停用租户 403、缺模块 403、业务校验 400、限流 429 和 `Retry-After` 均符合预期；拒绝路径无业务写入和占扣写入；不同租户数据隔离。
- 数据录入中枢：`/api/data-intake` 现在使用真实 `system` 模块 ID，缺少该模块时返回 403，不再因模块键名不一致静默放行。
- 运行就绪矩阵：AI、Scheduler、联网检索、微信支付、支付宝、飞书、内容外部发布、内容连接器共 8 通道。GET 测试在禁止 `fetch` 时仍通过，证明只读查询不联网；配置存在但没有新鲜显式验证时不显示“已连接”；响应不含密钥。
- 显式验证证据：AI 与飞书测试结果只在当前进程内有效，并受 TTL 与配置指纹约束；配置变化、过期或进程重启后不能继续冒充已验证。
- 占扣权威一致性：结算/释放在事务内按 hold ID 回读数据库权威行，校验租户、流水、用户、金额与类型；伪造跨租户或错字段返回 `409 CREDIT_HOLD_INTEGRITY_MISMATCH`，余额和流水不变；重复终态保持幂等。
- 启动恢复：扫描包含停用租户；单条坏记录或单个恢复子系统失败只记录错误，不阻断其余记录与租户；洛杉矶时区下本地时间创建的陈旧 RAG 占扣也能正确进入恢复。

### 浏览器、健康与发布判定

- 3107 最新服务以无密钥、`ENABLE_SCHEDULER=false` 运行；`GET /api/health` 返回 `ok=true`、`db=up`，老板账号 `guan` 登录成功。
- 管理概览真实显示 8 通道运行就绪矩阵；AI 为“降级模式”，Scheduler 为“当前已关闭”，外部发布为“仅人工流程”，内容连接器为“本地能力可用”。
- 接口管理显示“未配置”和“配置存在不代表通道已连接”；未执行“测试连接”，没有发起真实 AI、检索、支付、飞书或发布调用。
- 系统页的矩阵含表头共 9 行；页面不存在“AI服务已连接”或“应用机器人已连接”误报；新鲜浏览器 error 日志为 `[]`。
- 本地无密钥演示：**GO**。
- 自动化本地回归：**GO**。
- 生产：**CONDITIONAL GO**。仍须完成轮换凭证后的外部真实链路验收，并补齐持久化人工对账处理器、异步视频后台主动轮询和高成本写请求的请求级幂等。

## 第三轮 QA 结论（历史基线，已被第四轮结果取代）

### 自动化与静态质量门

| 质量门 | 第三轮结果 |
| --- | --- |
| Node 全量自动化 | `483/483` 通过，0 失败 |
| TypeScript | 通过 |
| 多租户隔离检查 | 61 张表；36 处动态查询进入人工复核提示；0 失败 |
| Vite 生产构建 | 通过 |
| Lint | 通过，0 warning |
| Format | 通过 |
| 服务端 JS 语法 | 通过 |
| `git diff --check` | 通过 |

### 第三轮专项验收

- 60 名餐饮员工全部拥有逐岗位严格输出契约、合法样本，以及缺字段、空值、错类型、未知字段、空数组等非法反例；契约进入工作台、执行、采纳和审计边界。
- 409 项历史技能中餐饮 344 项、内容 65 项全部进入工作台、执行快照、提示词和前端；当前验证层级严格限定为“目录与执行注入契约已验证”，不证明算法来源真实性或真实业务效果。
- 代码盘点确认历史实际为 8 个 `catalog_only` 而不是旧报告中的 7 个；当前已归零，13 项运行登记完整。本地辅助连接器可运行，真实采集/发布在缺少授权或凭证时 fail closed。

### 浏览器、健康与发布判定

- 第三轮真实浏览器与服务健康最终验收：**通过**。
- `http://127.0.0.1:3107` 最新服务启动，Scheduler 关闭、无真实 AI 通道；`GET /api/health` 返回 `ok=true`、`db=up`。
- 老板账号 `guan` 登录成功；`/`、`/advisor`、`/employees`、`/toolbox`、`/growth`、`/activities`、`/content`、`/execution`、`/analysis`、`/store-data`、`/assets`、`/system`、`/recharge`、`/admin`、`/m` 共 15 个核心路由均无登录回退或致命错误，手机版显示核心工作台。
- 餐饮 #101 完整七面板通过：岗位 Schema 在页面实际包含稳定 URN 契约、严格字段、质量要求和审批边界；技能页显示“历史技能”“目录与执行注入已验证”“业务效果待实测”。
- 内容趋势官完整七面板和岗位专属引导正常；技能页显示同一分层证据；岗位档案连接器策略显示 `status=requires_live_data`、`mode=verified_input_assist`，缺实时数据明确阻断，未出现 `catalog_only`。
- 新鲜浏览器日志为 `[]`，0 错误；视觉截图无布局阻断。
- 本地无密钥演示：**GO**。
- 自动化本地回归：**GO**。
- 生产：**CONDITIONAL GO**。第三轮当时的唯一未决条件是轮换凭证后的云雾、支付、飞书与外部发布真实验收；409 项技能的真实业务效果也只能由后续真实业务样本证明。该“唯一未决条件”表述已被第四轮结果取代。

## 自动化基线

命令：`npm run verify`

结果：

- Node 测试：366/366 通过。
- TypeScript：通过。
- 多租户隔离扫描：通过；保留既有“待复核”提示，无新增失败。
- Vite 生产构建：通过。

## 真实通道探活

| 检查 | 结果 |
| --- | --- |
| 健康检查 | `ok=true`、`db=up` |
| 老板登录 | 200，角色 `boss` |
| 管理概览通道 | `yunwu` |
| 服务商 | 云雾API |
| Base URL | 官方 HTTPS 地址 |
| `/models` | `ok=true`，408 个模型 |
| 完整密钥回显 | 无 |

## 真实 Paihuo 内容员工业务调用

触发：到期的“每日餐饮热点雷达”定时任务。  
员工：趋势官（内容员工 0，热点雷达部）。

| 证据 | 值 |
| --- | --- |
| 自动化运行 ID | 5 |
| 内容 ID | 135 |
| 审批 ID | 11 |
| 积分流水 ID | 6 |
| 模型 | `gpt-5.5` |
| 输入 tokens | 7,087 |
| 输出 tokens | 1,700 |
| 本地成本口径 | 0.3146 元 |
| 企业实扣 | 48 积分 |
| 结算后余额 | 199,952 |
| AI 模式 | `api` |
| 内容状态 | 待审核 |
| 正文长度 | 2,073 字符 |
| 候选选题 | 5 个 |
| 输出契约 | 有效 |
| 悬挂占扣 | 0 |

完整员工快照：

- 档案：`content-0-r0`
- 提示词哈希：64 位
- 必备能力：5 项，全部锁定
- 默认注入技能：13 项
- 工作方式：输入、执行、交付、放行、交棒均存在
- 岗位档案：职责、边界、权限、质量标准、输出契约均存在
- 工作配置：能力必需/启用/锁定均为 true，模型继承企业路由，审批为人工选择
- 结果未出现“当前没有可用AI通道”或“本地模板模式”

## 无费用边界测试

| 场景 | 预期 | 实际 |
| --- | --- | --- |
| 关闭员工 120 必备能力 | 400 | 400，“不能停用、删除或降级” |
| 员工 120 缺标题派活 | 400 | 400，“任务标题必填” |
| 普通员工读取接口配置 | 403 | 403 |
| 读取员工 999 | 404 | 404 |
| 边界测试新增积分流水 | 0 | 0 |

同时确认员工 120 的完整档案可读：

- 员工：许健证
- 岗位：员工健康与个人卫生
- 必备能力：8 项
- 启用技能：7 项
- 完整提示词：存在
- 模型：`deepseek-v4-flash`
- 审批：`owner_review`

## 浏览器验收

通过页面：

- `/content`
  - 显示 Paihuo 内容部门 10 位完整数字员工。
  - 显示手动创作、立即自动、定时任务三种生产方式。
  - 真实内容 135 显示为“待审核”。
- 趋势官完整工作台
  - 能力、工作方式、岗位档案、技能库、提示词、工作配置全部可见。
  - 五项必备能力明确标记不可关闭。
  - 提示词明确企业覆盖只追加、不能替换出厂能力与审批边界。
- `/admin`
  - 概览显示“云雾API已连接”。
  - 今日消耗 48 积分、输入 7,087、输出 1,700。
  - 积分流水显示 `gpt-5.5`、真实API、余额 199,952。
- `/system?tab=approvals`
  - 内容 135 的审批记录可见，状态待审核。

浏览器控制台：0 条 error/warn。

## 数据保护结果

原演示库恢复后：

- 余额：200,000
- 流水：5
- 内容：134
- 审批：10
- 自动化运行：4

隔离测试库：

- 余额：199,952
- 真实流水：1 条
- 启用自动化：0
- 新增真实调用：停止自动化后为 0

## 未纳入本轮

- 图片生成。
- 视频生成。
- 深度思考。
- 长期密钥持久化。
- 生产环境公开部署。

## 全功能回归追加记录

### 自动化与覆盖

| 检查 | 结果 |
| --- | --- |
| Node 测试 | 366/366 通过 |
| 员工/安全专项 | 172/172 通过 |
| TypeScript | 通过 |
| 租户隔离扫描 | 通过；59 张隔离表，36 处动态查询待人工复核 |
| Vite 构建 | 通过；存在 `advancedChunks` 废弃警告 |
| 路由行覆盖率 | 77.89% |
| 路由分支覆盖率 | 58.39% |
| 路由函数覆盖率 | 75.28% |
| 严格 ESLint | 不通过；0 error、199 warning |
| Prettier | 不通过；46 个文件 |

低覆盖模块：经营分析 26.35%、资产 39.22%、增长 50.72%、平台 53.25%。测试大量直接挂载 Router，没有完整覆盖 `auth → tenantGate → moduleGuard → aiGuard` 生产中间件链。

### 隔离实例 API

- 122/122 个 GET 接口返回 2xx 且为合法 JSON。
- 覆盖认证、元数据、老板驾驶舱、增长、内容、工具箱、活动、员工、工作台、顾问、执行、分析、门店、资产、系统、管理、充值、文件、数据导入和自定义 Agent。
- 写操作、权限、失败回滚由 366 项自动化测试覆盖。
- 未真实执行支付、图片、视频、外部发布、消息发送和批量删除等付费或不可逆动作。

### 浏览器

- 老板端 `/`、`/advisor`、`/employees`、`/toolbox`、`/growth`、`/activities`、`/content`、`/execution`、`/analysis`、`/store-data`、`/assets`、`/system`、`/recharge`、`/admin`、`/m` 均可渲染。
- 趋势官六个完整页签全部可打开；派活引导与岗位匹配，未出现“本月食材成本上涨”“这次要解决什么问题”“门店真实情况”等错位文案。
- 内容生产仓可见手动、立即和定时三种模式；自动产物只进入待审核，不自动发布。
- 浏览器控制台日志为 `[]`。

### 内容和输出

| 数据 | 结果 |
| --- | --- |
| 内容总数 | 135 |
| 真实 API 产出 | 1 |
| 通用待核验内容草案 | 120 |
| 岗位交付草案 | 10 |
| 自动任务契约底稿 | 4 |
| 真正可按岗位复核的真实样本 | 1，但未完成联网要求 |

数据扫描未发现“本月食材成本上涨”“这次要解决什么问题”“门店真实情况”“V1.0”“正文示例”“Lorem”“TODO”等旧占位语进入业务结果。但 134 条模板内容仍属于“未完成交付的底稿”，不能计作正常生产结果。

10 套 Paihuo 内容契约的空值反例：趋势、调研、拆解、文案、风格、多媒体、封面、分发、复盘 9 个岗位的必填字段全部传 `null` 仍返回有效；只有网页演示官因为 HTML 必须非空而拒绝。

### 隔离环境清理

- 两条临时自动化规则均已恢复为 `enabled=0`。
- `held` 状态积分占扣为 0。
- 密钥配置行计数为 0。
- 临时平台验收账号计数为 0。
- 3111 隔离服务已停止。
- 3107 正常服务健康检查仍为 `ok=true`、`db=up`。

## 第二轮修复后迭代结果（历史基线）

### 最终质量门

| 检查 | 修复后结果 |
| --- | --- |
| Node 自动化测试 | `456/456` 通过，0 失败 |
| TypeScript | 通过 |
| 多租户隔离检查 | 通过 |
| Vite 生产构建 | 通过 |
| Lint / Format / JS 语法 / Diff | 全部通过；lint 0 warnings |
| 本地无密钥演示 | GO |
| 自动化本地回归 | GO |
| 生产 | CONDITIONAL GO |

### 本轮新增回归证据

- 外部 AI 路由：权限、限流、预授权、结算/释放和后台租约均有失败分支测试。
- 内容员工配置：模型、输出长度和超时进入实际生成参数，不再只写提示词或快照。
- 强制联网：检索发生在预授权后；成功结果保留来源证据，空结果/失败不调用生成模型。
- 10 类内容岗位契约：缺字段、`null`、错类型、空数组、内层结构错误和未知字段均被拒绝。
- 模板/契约底稿：标记 `incomplete`，不进入内容成功、员工产量和汇总口径。
- 内容员工计费：先落库产物再结算；落库失败释放占扣；结算失败保留产物并进入待对账。
- 权限与语义：老板日报数据范围、零目标完成率、移动入口和平台角色入口具备回归覆盖。
- 分发官严格契约与人工采纳链路通过，未执行自动外部发布。
- 真实浏览器：15 个核心路由无错误；7 项禁用旧文案均未出现。
- 内容生产仓：10 位员工全部可见；趋势官七面板与岗位专属操作引导确认。
- 新标签页浏览器控制台：console error 为 0。
- 固定模板真实性：预置模板只显示为参考/草稿，不冒充实时 AI、实时扫描或真实经营数据。
- 内容自动化异步契约：立即执行返回 `202 + runId + pollUrl`，按 `runId` 精确轮询，同一次运行幂等复用；手动/定时均复核租户、员工与 `content` 模块授权，费用估算、占扣和生成采用同一实际模型。
- 支付完整性：重复关键节点被拒绝，渠道结果必须绑定本地商户订单，微信签名响应超过五分钟新鲜窗口即失败。
- RAG 后台生命周期：执行前预授权、按真实用量结算、与其他 AI 路径共享并发额度，并覆盖超时释放和服务重启恢复。

结论：本轮定义的本地代码层 P0/P1 发布阻断已全部关闭。

### 未执行的真实外部验收

本轮修复后没有使用真实凭证执行以下链路，因此不纳入 GO 证据：

- 云雾真实生成与真实搜索供应商调用。
- 支付渠道真实下单、支付、查单、关单或退款。
- 飞书真实消息投递。
- 微信、内容平台或其他外部账号真实发布。

第二轮当时保留 **CONDITIONAL GO** 的唯一原因是上述真实外部链路尚未使用轮换后的授权沙箱/生产凭证验收，且本轮已暴露密钥必须撤销或轮换；该历史口径已被第四轮结果取代。

### 当时保留债务（第三轮已关闭对应本地债务）

- 60 名餐饮员工尚无逐岗位机器输出 Schema。
- 409 项历史技能仍为 `legacy_unverified`。
- 7 个内容连接器仍为 `catalog_only`。
- 外部链路仍待沙箱/生产凭证验证。

## 最新前端增量验收（2026-07-31）

### 自动化与工程门禁

| 检查 | 结果 |
| --- | --- |
| 服务端自动化 | `491/491` 通过，0 失败 |
| TypeScript | 通过 |
| 多租户隔离 | 通过；61 张表，36 处动态查询保留人工复核，0 失败 |
| 设计 token / 界面契约 | 通过；token 引用较基线增加 47，未新增禁止项 |
| 严格 lint / Prettier / diff | 全部通过；0 warning |
| Vite 生产构建 | 通过；3871 个模块转换完成 |
| 服务健康 | `GET /api/health` 返回 `ok=true`、`db=up` |

### 新增回归证据

- 内容列表关键词搜索按租户和当前账号数据范围过滤，老板可看本租户范围，普通内容账号只看本人范围，跨租户结果不可见。
- `%`、`_` 和反斜杠按字面量搜索，不扩大为 SQL 通配符；超过 100 字返回 400。
- 命令面板的旧请求会被取消，远端结果只绑定当前规范化关键词；输入法组合期间 Enter 和方向键不触发命令。
- 最近访问按当前可用导航过滤；Mac 只响应 `⌘K`，非 Mac 只响应 `Ctrl+K`，重复键和混合修饰键不劫持。
- 820px 以下分组菜单恢复横向布局，1320px 以下命令入口保留为图标按钮。
- 图表的含函数/循环引用/特殊值配置随引用和主题版本更新；可序列化配置继续按指纹去重；StrictMode 重建不会留下空图。
- 应用和路由错误状态具备 `role=alert`、断言式 live region 与焦点接管；路由异常随路径和查询参数变化重置。

### 运行冒烟

- 3107 以预览库、Scheduler 关闭、全部外部密钥清空方式启动。
- 老板账号 `guan` 登录返回 200，角色为 `boss`。
- `kw=热点` 内容搜索返回 200，共命中 3 条并返回 3 条。
- 101 字关键词返回 400。
- 应用内浏览器已保留项目页，但其本地地址安全策略阻止本轮自动刷新；因此不把本次增量写成“最新视觉自动化通过”，需人工刷新完成最终视觉确认。

### 仍保留的非阻断项

- 客户、内容和活动搜索结果目前进入对应模块，尚未直达具体记录。
- 活动搜索仍先读取现有列表再在前端匹配，后续应补服务端关键词和分页。
- 本轮未调用真实 AI、搜索供应商、支付、飞书或外部发布服务。

## 数字员工任务闭环与角色权限验收（2026-07-31）

### 自动化与工程门禁

| 检查 | 结果 |
| --- | --- |
| 服务端自动化 | `493/493` 通过，0 失败 |
| TypeScript / 多租户隔离 | 通过；61 张表，36 处动态查询保留人工复核，0 失败 |
| 设计 token / 界面契约 | 通过；未新增禁止项 |
| 严格 lint / Prettier / diff | 全部通过；0 warning |
| Vite 生产构建 | 通过；3871 个模块转换完成 |
| 服务健康 | `GET /api/health` 返回 `ok=true`、`db=up` |

### 角色与数据边界

- 老板 `guan` 读取餐饮员工 #101 时可见 7 项能力、7 项启用技能和完整 7 个工作台页签；内容员工 #0 可见 5 项能力和 13 项技能。
- 运营员工 `yunying` 读取同一餐饮与内容员工时，`canViewInternalProfile=false`，内部档案由服务端删除；只保留公开岗位摘要、派活、授权进度与业务结果。
- 内容员工目录对老板保留 `skill` 与 `capabilities`，对运营员工从响应结构中移除这两个字段。
- 权限不是只靠前端隐藏：餐饮与内容工作台、任务运行快照、内容员工目录和员工输出钻取都在服务端执行同一管理信息掩码。

### 真实页面与任务闭环

- 餐饮员工目录已压缩为“选择数字员工”，任务动态和档案来源默认折叠，员工卡保留岗位决策信息与明确打开按钮。
- 老板端钱商圈工作台可直接看到预览库任务 #15“毛血旺 太原吾悦广场”，状态为待审阅；契约无效结果明确禁止采纳，并可跳到 `/system?tab=approvals` 对应审批记录。
- “待执行”已解释为普通经营任务状态，深链 `/execution#task-board` 实测到达“经营任务 · 待执行”。
- 岗位派活说明首次打开时展开，再次打开默认收起，仍可手动恢复；老板端完整七面板保留。
- 运营员工真实页面只保留“派活与任务”；能力、工作方式、技能库、提示词、工作配置和岗位档案页签均不存在。
- 最终交付页为 `http://127.0.0.1:3107/employees`，浏览器日志为 `[]`。

### 边界

- 任务 #15 是既有模板降级且岗位契约无效的历史结果，本轮没有改写或采纳该结果；应在审批中心驳回，并在真实 AI 通道恢复后重新派活。
- 本轮未调用真实 AI、检索、支付、飞书或外部发布服务，未提交或推送 Git。

## 三角色业务穿刺与 Quiet Command 最终验收（2026-07-31）

| 检查 | 最终结果 |
| --- | --- |
| 服务端自动化 | `522/522` 通过，0 失败 |
| 70 名数字员工离线完整生产管线 | `70/70`；餐饮 `60/60`、内容 `10/10` |
| 三角色与递归业务流 | `4/4`；外部网络 0、计费变化 0、内部档案泄漏 0 |
| 内部档案恶意回显专项 | `98/98` 通过 |
| TypeScript / 多租户隔离 | 通过；61 张表，36 处动态查询保留人工复核，0 失败 |
| 设计 token / UI 契约 | 通过；五项 token 指标均较基线改善 |
| ESLint / Prettier / Vite build | 全部通过；0 warning；3879 个模块转换完成 |
| 保护文件哈希 | 两份均与既有 SHA-256 完全一致 |

浏览器在全新隔离库完成：老板创建并启动根任务、拆给运营；运营首次启动即可看到下级并拆给一线员工；员工无新建/拆解/公司目标/今日作战计划/老板专审，完成提交；运营审核员工并提交汇总；老板终审管理层和根任务。根任务业务流最终显示 6 个权威节点：三级任务和三级结果提交。员工叶子流只显示匿名父节点，不泄露父任务标题、状态、时间、链接、编号或岗位内部档案。

老板端餐饮数字员工工作台保留能力、工作方式、技能库、提示词、工作配置、岗位档案；运营和员工端不展示。模型恶意回显内部档案时，受限角色只见安全提示，产物不可采纳或进入后续业务流。

## L5 真实云 API 最终 QA 补充（当前有效，2026-07-31）

### 证据总表

| 验收面 | 最终结果 | 权威证据 |
| --- | --- | --- |
| 70 名数字员工真实云执行 | `70/70 PASS_REAL_API`；餐饮 60/60，Paihuo 内容 10/10；失败 0，运行中 0 | `artifacts/real-employee-matrix-l5-full-v2.json` |
| 70 份已采纳主产物质量审计 | `70/70 PASS_QUALITY`；8 项质量门逐项 70/70，失败 0 | `artifacts/real-employee-output-quality-audit.json` 与 `.md` |
| 全功能真实云矩阵 | `27/27`；25 个 `PASS_REAL_API` + 2 个 `PASS_PERMISSION_BOUNDARY` | `artifacts/real-feature-matrix-l5.json` |
| 服务端全量自动化 | `645/645` 通过，0 失败 | 最新全量回归输出 |
| 三角色真实浏览器 | **待主代理最终确认** | 需补老板/管理层/员工最终行走、截图与新鲜控制台日志 |

最终三份核心证据的 SHA-256 为：

- 员工矩阵：`e86241c338dff8464e4eb858871762a855b7521729a39ac85c35300f49f371c5`
- 质量审计：`662edc056410cfa2aff27a5a0c95440183a66019bc279106432395a56df6fa8f`
- 功能矩阵：`dc6d3b79f30756ffb0cc12ae6c59b0057b08a40a33b53641fa1c3c1ed6f80457`

### 计费、hold 与终态对账

- 70 岗最终矩阵全部 `billingState=settled`，共 70 个计费 ID；实扣 11,065 积分，成本 ¥73.5226，输入 1,165,483 token，输出 655,814 token。
- 27 个功能场景中，25 个真实交付全部结算完成，共显式回读 27 个 hold ID；实扣 1,151 积分，成本 ¥7.5706，输入 133,151 token，输出 88,684 token。2 个权限反例在路由处返回 403，新增计费、hold 和业务产物均为 0。
- 两个最终矩阵合计 95 个真实云交付，实扣 12,216 积分，成本 ¥81.0932，输入 1,298,634 token，输出 744,498 token。
- 对隔离验收库执行只读权威查询：`credit_holds` 共 174 条历史记录，全部为 `settled`（含失败/驳回后 0 分释放）；当前 `held=0`，占扣积分合计 0。

### 质量与安全边界

- 70 份主产物全部通过正文完整性、岗位契约、人工审核终态、内部档案泄露、模板占位、已知事实、缺材料披露和外部动作声称 8 项检查。
- 质量审计只读数据库、外部 API 调用 0，不在报告中保存正文、提示词、技能库、工作配置或岗位档案。
- 真实云证据仅确认文本/vision 主路径与受控权限反例；没有执行支付、飞书、对外发布或视频生成等不可逆/高成本动作。

## 最终真实收口 QA（当前权威，2026-07-31）

本节追加于历史 QA 之后，取代上文“三角色待确认”和 `645/645` 作为当前口径；历史过程数据不删除、不改写。最新完整 `npm run verify` 的服务端回归快照为 `696/696`，后续若新增状态回归，以最后一次完整 `npm run verify` 输出为准。

### 最终通过矩阵

| 验收面 | 结果 | 最终证据 |
| --- | --- | --- |
| 数字员工真实云执行 | **70/70** | 餐饮 60/60、Paihuo 内容 10/10；真实 API、正 token、有效契约、主产物、人工终态、计费和业务流完整 |
| 已采纳主产物质量 | **70/70** | 8 项检查共 560/560；无模板冒充、内部档案泄漏或虚假外部动作 |
| 全功能矩阵 | **27/27** | 25 个真实云交付 + 2 个精确权限拒绝；拒绝路径无计费、hold 或业务产物 |
| 自动化与工程门禁 | **GO** | 服务端 `696/696`；类型、隔离、token、UI 契约、严格 lint、格式、生产构建全过 |
| 三角色真实浏览器 | **GO** | 老板、管理层、员工使用真实账号逐页行走；最新控制台错误 0 |
| 服务健康 | **GO** | 3107 预览与 3108 L5 隔离实例均健康 |

### content:9 事实门禁专项（工程证据）

- 无来源的百分比、权重、行业基准和阈值会使 content:9 输出契约失败，不能进入“可使用”链路。
- “待补历史基线”“由负责人设定”等明确披露缺口的表述允许通过；任务材料或已验证网页中已经出现的事实允许复用。
- 结构合规但命中事实门禁的首轮输出会携带原任务与错误进入一次真实模型修复重试；模板、fallback 或失败结果不能通过重试冒充成功。
- 上述正反例已纳入 `696/696` 回归；最新真实云浏览器复跑另以运行 #10、内容 #142、审批 #18 和计费 hold #22 留证。

### 三角色页面与业务流复验

- 老板：老板驾驶舱与老板参谋可见；餐饮和 Paihuo 内容员工均保留“派活与任务、能力、工作方式、技能库、提示词、工作配置、岗位档案”七个完整页签；内容生产仓具备手动、立即自动和定时任务入口。
- 管理层：只读取团队经营范围；可以处理组织范围内的内容审批，老板专审/高风险动作保持禁用；数字员工岗位内部档案不返回，只保留派活、进度和业务结果。
- 员工：只见本人工作台和本人任务；员工目录被服务端/路由边界阻断；执行页不再显示他人姓名、名次或跨用户排行榜。该泄漏由真实点击发现，修复后以同一账号复验通过。
- 三角色任务状态、操作入口和审批权限与数据库流向一致；浏览器最终新鲜日志中 `error=0`。

### 历史异常与数据库对账

- 预览库只迁移严格识别的历史演示异常：6 条内容、6 条审批、2 条员工任务、4 条自动化运行。迁移保留记录、系统原因和操作日志，不删除历史，也不碰真实来源、已完成审阅或已有下游证据的记录。
- 普通任务与提交的状态错位为 0；“任务完成但提交待审核”“任务待审核但没有待审提交”等不可处理组合均已消除。
- 预览库与 L5 隔离库当前 `held` 均为 0；L5 隔离库 174 条历史 hold 全部 `settled`。
- assets 401 经 6/6 定向复验、全新服务 650/650 请求、100 轮共 600 个资源压力样本，以及 20 轮鉴权/限流/就绪组合复验均未复现。当前代码路径未发现可重复的 401；原结果按陈旧或瞬态测试输出保留，不伪造修复提交。

### 最终边界

本地真实云文本/vision 主路径、自动化回归和三角色应用链 **GO**。支付、飞书、外部发布、视频生成仍未获得本轮实际执行所需的凭据/沙箱/不可逆授权，继续 fail-closed；因此整体生产放量保持 **CONDITIONAL GO**。

## 未完成任务真实复跑与状态不变量（最终追加，2026-07-31）

### 复盘官真实云证据

- 运行 #10 / 内容 #142：`api + deepseek-v4-flash`，输入 5,095、输出 591 token，`finishReason=stop`，契约一次有效。
- hold #22：预授权 43 分，结算实扣 5 分，成本 ¥0.0284，状态 `settled`，全库当前 `held=0`。
- 审批 #18：生成后为待审核；老板阅读正文后通过，内容转为可使用；内容资产 1、知识库 1、发布记录 0。
- 正文只陈述数据缺口、采集计划和验证计划，没有无来源的平台算法、硬阈值、行业基准或虚假经营成效。

### 失败副作用反例

- #7 / 内容 #140 与 #8 / 内容 #141 均经人工驳回，资产、知识库、发布均为 0。
- #9 在 JSON 截断和事实门禁均失败后安全终止；content_id 为空、实扣 0、资产/知识库/发布均为 0。
- 历史记录保留；不存在通过删除失败、手工改状态或写模拟响应制造通过的情况。

### 状态不变量

1. 未生成：不能审阅。
2. 已生成但账务未终态：只显示待账务对账，不能预览、下载、导入或发布。
3. 真实 API + 正 token + 有效契约 + 持久化 + 无泄漏 + 已结算：才有资格进入质检/人工审阅。
4. 自动质检放行与人工采纳使用不同文案；二者都不代表已经外部发布。
5. 驳回/失败不产生使用中资产或知识库；人工采纳后才生成可追溯下游记录。

最新完整验证为服务端 `696/696`，并通过类型、隔离、token、UI 契约、严格 lint、格式与生产构建门禁。

## 2026-08-11｜任务 #39 最终报告与三格式文件验收

- 浏览器：`/employees?employee=102&task=39` 显示“已自动采用（可用于业务）”、执行摘要/关键结论/事实证据/风险/行动计划，页面无原始 `deliverables` JSON；下载链接为 PDF #7、Word #12、Excel #13。
- 下载端点：三项均返回 HTTP 200；MIME 分别为 `application/pdf`、DOCX、XLSX。XLSX 二次生成复用同一 #13，`artifactRenderVersion=xlsx-layout/3`。
- PDF #7：8 页 A4，中文字体嵌入、可复制、无乱码/空白/裁切；SHA256 `ea5eb7f84e7cf7406b5b8130815968a24be5a9738255295825039a3cfa24dfa1`。
- DOCX #12：15,209,012 bytes，真实渲染 29 页，中文、表格、页眉页脚和页码可见；SHA256 `7edff27e8add54611e99cba99bffa0070ca92e35d5e4417718e369ec5dd60fc1`。
- XLSX #13：18,589 bytes，Quick Look 无自定义 Fontconfig/字体别名预览中文标题、表头、正文和风险来源均可见；SHA256 `49da932ac159d3ea3c495c298cfae0b9d22c6377d609a908a4a5d798cdb68828`。
- 定向文件回归通过；最终 `npm run verify` 在全部云/搜索/飞书密钥显式清空、允许本地回环测试端口的环境中退出 0。

## Paihuo 完整流水线与老板自定义审批收口（当前权威，2026-07-31）

- 服务端完整回归：`1070/1070`，0 失败；类型检查、多租户隔离、token 门禁、UI 契约、严格 lint、格式检查和生产构建全部通过。
- Paihuo 10 个内容岗位不再使用另一套缩水结构：流水线运行时统一读取派活岗位的能力、工作方式、技能库、提示词、工作配置和岗位档案，并在每一站保存可核验的运行快照；老板端只展示脱敏证据和指纹，不回显提示词正文、密钥或内部配置正文。
- 老板审批策略支持四种任务级配置：高效（只审分发官 8，默认）、关键节点（0/3/5/6/8）、内部自动（不设工位停审）和逐工位自定义（0—9 任意组合）。关闭工位停审仅代表内部接力，不会自动对外发布。
- 权限浏览器复验：老板可配置审批点；管理层可查看和执行业务，但审批策略由企业负责人锁定且不可修改；员工无法进入内容生产仓，也不能读取岗位内部档案。
- 特殊供应商调用具备稳定 attempt/ref 标识与幂等重放；对账同时核验租户、任务、占扣、素材摘要、模型和供应商，证据不完整、重复、未知或仍在执行的任务保持阻断。
- 本轮 UI 复验未提交任务、未调用云 API、未产生积分费用。复验结束时 3107 前端与 3109 后端健康，当前悬挂预授权 `held=0`。
- 浏览器控制台存在 Ant Design 的既有开发期弃用/上下文警告，不影响本轮审批功能；不将其误报为“控制台零告警”。

## 2026-08-20 真实运行复核

- 项目地址：`http://127.0.0.1:3107/`；健康检查 `ok=true, db=up`。
- 真实任务 #62：`已完成` / `已自动采用（可用于业务）`，真实 API 正 token 结算 18 积分，正文 6,995 字；v4 contract、4 输入、7 方法、5 交付和 hard delivery 通过。
- 证据链：7 项能力与 7 项技能锁定注入；研究计划 7 skills、6 lanes；agentic/controlled/location 通道均有快照。高德/点评/美团只作为公开检索 lane，`apiClaims=[]`，不宣称官方 API 直连。
- 产物 #51/#52/#53：分别为 PDF/DOCX/XLSX，`draft=false`，实体文件哈希与数据库一致；PDF 6 页，DOCX/XLSX 均通过 ZIP 完整性检查并完成 QuickLook 视觉复核。
- 旧任务 #49/#50 已由 #62 取代；旧文件接口返回 HTTP 409 `DELIVERY_SUPERSEDED`，并指向 replacement task #62。
- 离线聚焦回归：40/40 通过（contract、report-first、supersession、source artifact）；前端 typecheck、strict lint、Vite build、`git diff --check` 通过。
- 72 岗确定性前置矩阵：72/72 generation-ready；这不是 72 岗都真实消耗云模型的证明。功能清单 395 项中 372 项尚无运行证据，23 项保持外部副作用安全阻断。

## 2026-08-20｜当前版本真实功能与浏览器复验（最新权威）

本节覆盖本轮实际执行，取代本文更早的 70/70、27/27 历史快照；历史证据保留但不再作为当前结果。

### 浏览器只读巡检

- 餐饮员工 101–161：61/61 页面打开成功，身份、技能/能力区域、派活输入均存在，当前 HTTP 4xx/加载错误 0。
- 内容员工 0–9：10/10 内容工作台入口打开成功，内容员工任务与一句话执行区存在，当前 HTTP 4xx/加载错误 0。
- 工具箱 12/12 卡片均已打开并检查输入控件/运行入口，无当前 HTTP 4xx/加载错误。
- 证据：[employee-ui-smoke-2026-08-20.json](/Users/wanglei/Documents/纳米work企业版本/artifacts/browser-evidence-final/employee-ui-smoke-2026-08-20.json)。

### 36 条真实功能矩阵

- 当前证据：[real-feature-matrix-2026-08-20.json](/Users/wanglei/Documents/纳米work企业版本/artifacts/real-feature-matrix-2026-08-20.json)。
- 总计 16/36 通过；真实 API 11/31 通过；权限边界 5/5 通过；L1 21/36；L2 19/36。
- 输入/输出 token：94,110 / 64,545；成本 ¥5.9943；1,038 积分。
- 通过项含老板参谋联网深度会诊、自定义智能体（管理层/员工）、活动独立草稿、私域话术、异议处理、内容日更包、内容自动化、图片识别、老板同步对话、知识库图片识别；权限反例全部按预期 403 且零计费。
- 失败集中在工具箱未达到 `canUse=true`、供应商 502/超时、业务事实/语义锚点不足、审批增量或模板污染。最终安全审计 active=0、held=0、无飞书外发残留。

### 内容部门真实矩阵

`run-real-content-automation-matrix.mjs` 覆盖 0–9 号员工的 immediate + scheduled，源库只读复制到临时隔离数据库；每条验证真实 API、岗位快照、联网证据、人工审阅边界、定时认领、防重复计费、失败退款及发布/知识库副作用。

- 结果：`0/20` 通过（immediate `0/10`，scheduled `0/10`）；11/20 取得真实 API 正 token，8/20 形成内容 ID/有效契约，但最终业务门禁全部失败。
- Token：输入 `120,873`，输出 `81,499`。
- 主要失败：联网证据不完整、图片 provider 无稳定位图/专项账务、待审边界与资产/知识库副作用不一致、模板/降级结果、契约字段/事实门禁失败。
- 最终安全审计：PASS；`activeRunCount=0`、`heldCount=0`、`publishLogCount=0`、`duplicateScheduledCycleCount=0`。
- 权威证据：[real-content-automation-matrix-2026-08-20.json](/Users/wanglei/Documents/纳米work企业版本/artifacts/real-content-automation-matrix-2026-08-20.json)。

### 2026-08-21｜全 HTTP 功能接口级补测

- 只读报告：[nanowork-all-get-api-functional-report-2026-08-21.md](/Users/wanglei/Desktop/nanowork-all-get-api-functional-report-2026-08-21.md)：190/190 GET 已请求，145 个 2xx、45 个预期参数/权限边界，0 个 5xx、0 超时、0 敏感信息泄漏。
- 写接口报告：[nanowork-all-write-boundary-report-2026-08-21.md](/Users/wanglei/Desktop/nanowork-all-write-boundary-report-2026-08-21.md)：168/168 POST/PUT/DELETE 已记录，143 个预期边界、23 个外部发送/付款/配置/删除等安全阻断、2 个隔离会话正常响应，0 个 5xx/超时。
- 写接口只在临时隔离数据库执行；真实业务功能是否“交付通过”仍由真实 API、输出契约、账务、审批和副作用矩阵决定。
- 页面路由报告：[nanowork-all-web-route-report-2026-08-21.md](/Users/wanglei/Desktop/nanowork-all-web-route-report-2026-08-21.md)：24/24 页面/主菜单 URL 返回可加载应用壳；员工深层交互继续以浏览器 71/71 证据为准。
- 最新真实用户复跑：任务 #51 输入/输出/质量报告见桌面 [nanowork-employee-real-user-rerun-51-2026-08-21.md](/Users/wanglei/Desktop/nanowork-employee-real-user-rerun-51-2026-08-21.md)。供应商连续 504 时系统无模板交付、无扣费、全额退款，失败证据已保留。
- 最终服务端回归：显式空云密钥环境下 1,713 passed / 0 failed / 1 skipped；skip 仅为本机缺少 bundled documents/render_docx.py，非业务失败。

### 72 个数字员工真实云执行与 0→9 总流水线

- 权威证据：[real-employee-matrix-2026-08-20.json](/Users/wanglei/Documents/纳米work企业版本/artifacts/real-employee-matrix-2026-08-20.json)。61 个餐饮岗位 + 11 个内容岗位共 72 个单岗已全部发起真实执行或记录明确阻断；业务通过 `0/72`，其中 `71 FAIL_REAL_API`、`1 BLOCKED_VIDEO`。
- 累计模型输入/输出 `631,150 / 368,659` token，供应商估算成本约 ¥5.7013，客户账本实扣 ¥5.4473 / 846 积分；隔离库结束 `held=0`。
- 新建流水线 `#25` 的工位 `0–4` 真实完成并通过审阅（5/10），第 `5` 工位因媒体授权边界进入 `awaiting_media_authorization`，后续工位安全阻断；旧中断流水线 `#24` 不计入结果。
- 该轮严格区分“真实调用/真实 token/前置档案通过”和“业务交付通过”，未将失败、运行中或授权阻断项伪装成成功。

## 2026-08-21｜全功能报告污染修复与语义化复跑（当前权威）

### 根因核验

- 旧 WRITE 报告在同一会话执行 `/api/auth/logout` 后继续探测，140 个受保护接口因此统一返回“会话已退出或被吊销”；这是测试器污染，不是 140 个产品功能同时故障。
- 旧静态路由扫描存在串 mount，包含 45 个伪接口并漏掉至少 117 个真实 mounted-router 接口；旧 GET 动态参数统一填 `overview`，产生“未知钻取类型/未知来源类型/请求内容格式错误”。
- 旧 GET 响应在 JSON 解析前截断，并把 200 响应中携带的历史失败记录当成本次请求失败。
- 旧 employee v2 矩阵中 57 条已完成、57 条可使用、58 条契约有效，但 59 条 report-first 产物被统一误判为非 JSON，59 条权威哈希和输入记录也被测试器错误判负；旧 `0/72` 整源作废。

### 修正版实跑

- 实际路由清单：471 个入口，其中 434 个唯一 HTTP API（GET 228，WRITE 206），重复 0。
- GET 语义探针：228 项；正向功能 173，负向边界 52，外部副作用未发送 3，测试器无效 0，当前产品失败 0。
- WRITE 隔离探针：206 项；正向功能 17，负向边界 146，高风险动作未发送 43，测试器无效 0，当前产品失败 0。
- 浏览器语义验收：真实 React 登录页完成挂载，关键标题/入口存在，控制台 error=0、warning=0；其余 23 个需登录页面明确保留“尚未验证”，不再把同一 SPA HTML 壳计为页面通过。
- 旧响应中的历史问题只从失败状态、错误字段、失败通知和退款/待对账证据提取；岗位说明、提示词、技能规则、文件名和正常结算不再列为故障。

### 修复与回归

- `POST /api/content/templates` 非字符串/空值输入不再触发 SQLite 500，改为入库前明确 400。
- task-center 非法 kind/id 返回固定安全领域码；合法 kind 但记录不存在返回 `404 TASK_NOT_ACCESSIBLE` 与“任务不存在或无权查看”。
- 员工矩阵 runner 支持 report-first 的权威 Markdown 哈希链，并传入完整 requirement。
- 报告/探针/员工矩阵/content template/task-center 相关回归最新 89/89 通过；补充语义报告定向 24/24 通过；服务端全量 `1744/1744` 通过、0 失败、1 项按设计跳过；Web typecheck、strict lint、format check 与 3,934 modules 生产构建全部通过。
- 修正版总报告 ID：`ff1f9ea61cd5c1ff66116c54f28caa033c36434fb021d41237995a393ab5f4f3`。
- 权威文件：`artifacts/nanowork-all-functions-corrected-2026-08-21.md` 与同名 JSON；5 份旧桌面报告均已保留 `.legacy-invalidated` 备份并由逐文件 SHA-256 一致的修正版替换。

## 2026-08-21｜报告语义二次终验（当前权威）

### 修复对象

- 旧写探针在 `/api/auth/logout` 后复用同一 token，导致 140 条正确的会话吊销 401 被误标“已真实执行”。新探针每个受保护写接口独立会话。
- 动态 GET 旧探针统一填 `overview`，造成“未知钻取/来源/格式错误”。新探针对每条动态路由使用显式合法 happy-path fixture，并把非法参数另作负向边界。
- 非 `happy_path` 的 2xx 不再自动计正向；无明确边界或成功契约的 2xx 归 `UNVERIFIED`。
- 正常 2xx `message` 显示“消息”，只有 HTTP 错误、`error` 或失败业务态才显示“错误”。
- 员工矩阵与输出质量审计均支持 report-first：`parsedOutput=null` 时使用 contract、hardDelivery、正 token 和权威 Markdown 四段哈希链，不再强制伪造 JSON。

### 真实本地复跑（无云、无外部动作）

- GET：228 项；173 `HAPPY_PATH_PASS`、52 `NEGATIVE_BOUNDARY_PASS`、3 `SAFETY_NOT_EXECUTED`，其余为 0。
- WRITE：在临时 isolated 数据库、调度器关闭、外部服务密钥清空条件下执行 206 项；2 `HAPPY_PATH_PASS`、150 `NEGATIVE_BOUNDARY_PASS`、50 `SAFETY_NOT_EXECUTED`、4 `UNVERIFIED`，产品/探针故障 0。
- 两份探针和总报告扫描不到“会话已退出或被吊销”“请重新登录”“未知钻取类型”“未知来源类型”“请求内容格式错误”旧污染文本，也未发现真实凭据、Bearer、Cookie 或 raw body。
- 总报告 ID：`bcfb6bafede91fe7591af12dfecd77ee5ccc8e2589d4b39e77255d16b47b3951`；Markdown 为精简用户版，完整语义 JSON 保留逐项证据。

### 业务与质量证据

- 业务功能历史矩阵：16/36（真实 API 11/31、权限边界 5/5），20 条失败带日期/runId进入“历史失败”，明确要求当前代码重跑。
- 原 v5 员工输出质量审计声明的来源矩阵 SHA 为 `b6475b40…`，实体文件实算为 `d967032d…`，整源作废；不得用其 4/4 结论。
- 最新可复现员工输出质量 v3：1/1 能力质量通过、0/1 业务生产、1 项运营阻断；引用矩阵声明与实算 SHA 一致。两份 v1 各 70 条也整源作废：一份是旧浅口径，另一份含 60 条 report-first/JSON 系统性假阴性。

### 回归门禁

- 报告、探针、员工矩阵、输出质量、content template、task-center 定向：103/103。
- 服务端全量：1,758 tests；1,757 pass、0 fail、1 skip（缺 bundled documents 渲染环境时按设计跳过视觉层，OOXML 契约仍执行）。
- Web：typecheck、strict lint、format check、Vite build（3,934 modules）通过。
- 其他：`check:isolation`、`check:tokens`、`check:ui-contract`、`git diff --check` 全部通过。
- Desktop 交付已逐对校验：总报告 Markdown `776c20f1…`、完整 JSON `3755d83f…`、GET 报告 `245f651a…`、WRITE 报告 `5188d1da…`，均与工作区 artifact SHA-256 一致；对应 `.legacy-invalidated` 旧版备份仍保留。

## 2026-08-28｜提交前完整回归

- 命令：仓库根目录执行 `npm run verify`，退出码 0。
- 服务端：1,791 项；1,789 pass、0 fail、2 skip。跳过项分别受外部 DOCX 可视渲染环境和工具箱真实表格验收样本约束，契约与其余实现仍进入回归，未计为通过。
- 定向复验：联网研究与安全并发门组合 28/28；并行场景以处理器就绪事件同步，不再依赖固定睡眠。
- Web：typecheck、strict lint（0 warning）、format check、隔离门、token 门、UI 契约门和 Vite production build 全部通过；构建处理 3,957 modules。
- 代码卫生：`git diff --check` 通过；Growth 页面已拆出可复用组件/工具，系统治理与内容卡片的静态内联样式已迁入 CSS，token 基线与当前代码一致。
- 判定：源代码提交 **GO**；本机 3107 部署待提交后执行；公网生产因无部署目标保持 **HOLD**。
