# 研发状态

更新时间：2026-07-31  
通道：标准通道  
当前阶段：第四轮生产可信度加固 · 阶段 12 复盘完成  
在岗角色：总指挥、架构师、工程师、测试负责人、审查负责人  
模式：自动  
当前结论：第四轮三个切片已经完成：真实生产守卫链与数据导入 `system` 门禁进入 HTTP 集成回归；8 通道零联网运行就绪矩阵落地；占扣结算/释放与启动恢复改为数据库权威回读并具备跨租户校验和故障隔离。最终 `491/491` 自动化、全部静态质量门、3107 健康检查与真实浏览器验收通过。本地无密钥演示和自动化本地回归 **GO**；生产继续 **CONDITIONAL GO**，条件同时包含真实外部链路凭证验收，以及持久化对账处理器、异步视频主动轮询和高成本请求级幂等三项后续生产债务。

> 基线说明：本文后半部分保留 2026-07-30 真实云雾联调与全功能回归原始记录，统一视为“修复前基线”；页首状态和“第四轮当前有效结果”为当前有效状态。

## 本次升级阶段账本

| 阶段 | 状态 | 产物 | 备注 |
| --- | --- | --- | --- |
| 00 需求接收 | 已过关口 | `docs/team/00-brief.md` | 用户要求直接完善大量不通过项 |
| 01 压力测试 | 已跳过 | — | 存量产品缺陷修复，不是新市场想法 |
| 02 用户研究 | 已跳过 | — | 本轮目标由真实回归证据确定 |
| 03 PRD | 已满足（存量产物） | `docs/升级迭代报告-2026-07/05-全功能回归与迭代升级报告-2026-07-30.md` | 含验收标准和发布关口 |
| 04 需求拷问 | 已过关口 | `docs/team/04-decisions.md` | 采用风险优先、无付费调用策略 |
| 05 设计方向 | 已满足（存量产物） | 现有企业后台设计系统 | 本轮只修权限入口和状态，不做视觉重设计 |
| 06 架构盘点 | 已过关口 | `docs/team/06-architecture.md` | 统一外部调用与员工执行边界 |
| 07 实现计划 | 已过关口 | `docs/team/07-plan.md` | 第二轮 7 个切片 + 第三轮 3 个能力成熟度切片 + 第四轮 3 个生产可信度切片 |
| 08 里程碑实现 | 已过关口 | 源代码与测试 | 13 个升级切片均已实现 |
| 09 TDD 与 QA | 已过关口 | `docs/team/09-qa.md` | `491/491`、静态质量门、3107 健康与第四轮真实浏览器均通过 |
| 10 评审与安全 | 已过关口 | `docs/team/10-review.md` | 本地无密钥与自动化可放行；外部凭证和三项后续生产债务保留条件 |
| 11 发布 | 条件过关口 | `docs/team/11-release.md` | 形成条件发布包；本地验收完成；未执行生产发布 |
| 12 复盘 | 已过关口 | `docs/team/12-retro.md` | 已记录结果、债务和下一迭代入口 |

## 第二轮升级切片进度（历史完成）

- [x] 切片 1：服务启动默认不跑自动化，真实密钥不落 SQLite，上游错误统一脱敏。
- [x] 切片 2：高风险外部 AI 入口接入权限、限流、两段式计费与后台任务生命周期保护。
- [x] 切片 3：内容员工手动、立即、定时执行真实采用工作配置，并兑现 `webRequired`。
- [x] 切片 4：10 套内容岗位严格契约拒绝空值、错类型、空数组和未知字段；模板底稿不计成功产出。
- [x] 切片 5：销售不能读取老板日报；缺少目标不计算百分比。
- [x] 切片 6：移动端按角色过滤入口，平台超级管理员路由与页面一致。
- [x] 切片 7：工具箱加载完整岗位上下文并限制同租户用户结果范围。
- [x] 收口：专项与全量自动化、类型检查、构建及发布评审；最终自动化测试 `456/456` 通过。

## 本次升级已定决策索引

- D-010：本轮进入完整通道自动模式，安全闸内不调用真实付费服务。
- D-011：不靠页面隐藏解决权限问题，后端权限是唯一安全边界。
- D-012：模板/契约底稿必须显式标记“未完成”，不得计入成功产出。
- D-013：员工配置必须影响真实执行参数，不能只进入提示词或快照。
- D-014：外部检索必须在预授权后执行，失败不得冒充岗位完成。
- D-015：后台 AI 并发租约保持到真实任务终态。
- D-016：业务产物先可靠落库再结算，失败进入释放或待对账。
- D-017：修复后采用三级发布结论，不把本地通过等同于真实外部链路通过。
- D-018：未完成的岗位 Schema、历史技能和连接器保持显式债务。
- D-019：内容仓固定模板只作为参考/草稿，不得冒充实时 AI 或实时业务数据。
- D-020：内容自动化采用 `202 + runId` 异步契约，并统一幂等、撤权门禁和实际模型口径。
- D-021：支付链路拒绝重复关键节点、绑定本地订单并校验微信响应五分钟新鲜度。
- D-022：RAG 后台任务统一预授权、真实结算、共享并发、超时释放和重启恢复。
- D-023：技能验证分层，只把目录与执行注入契约写成已验证。
- D-024：连接器状态必须对应真实运行能力和启用条件。
- D-025：60 名餐饮员工产出采用逐岗位严格机器契约。
- D-026：第三轮自动化、静态质量门、浏览器与服务健康均通过；该历史结论已被第四轮 D-030 更新。
- D-027：生产守卫链必须成为自动化证据，未知模块不得静默放行。
- D-028：配置存在不等于已验证连接；GET 就绪查询不得联网。
- D-029：占扣终态只信任数据库权威记录。
- D-030：第四轮 `491/491`、静态质量门、健康与真实浏览器通过；生产条件包含外部凭证和三项后续生产债务。

## 第二轮升级下一步（历史）

最终测试数已回填；随后按 `docs/team/11-release.md` 执行发布前人工清单。在取得轮换密钥及支付、飞书、外部发布等沙箱/生产凭证前，不进行真实生产放量。

## 第三轮能力成熟度切片

- [x] 切片 8：60 名餐饮员工逐岗位机器输出契约——每岗已有严格 Schema、合法样本与缺字段/空值/错类型/未知字段/空数组反例。
- [x] 切片 9：409 项历史技能证据化——餐饮 344 项、内容 65 项均绑定来源快照、岗位、版本、指纹和离线验收样本，并进入工作台、执行快照、提示词与前端。
- [x] 切片 10：8 个内容连接器状态收口——历史实际 8 个 `catalog_only` 已归零，13 项运行登记完整，本地辅助连接器可运行，需外部授权的链路 fail closed。
- [x] 收口：专项测试、`483/483` 全量回归、类型/隔离/构建/lint/format/语法/diff 质量门及交付文档已完成。
- [x] 第三轮真实浏览器与服务健康验收：3107 最新服务健康；老板账号登录成功；15 个核心路由、餐饮 #101 与趋势官七面板及新增分层状态均通过；新鲜浏览器日志 0 错误。

## 第三轮下一步

仅在取得轮换后的授权凭证时，按云雾、支付、飞书、外部发布顺序执行真实外部验收；不得用离线契约或本地浏览器证据替代真实业务效果或渠道结果。

## 第四轮生产可信度切片（已完成）

- [x] 切片 11：抽离可测试的生产应用装配，新增完整 `auth → tenant scope → tenant gate → module guard → AI guard → Router` 集成验收；修复数据导入 `system` 模块门禁静默失效。
- [x] 切片 12：新增零联网运行就绪矩阵，把 AI、Scheduler、搜索、支付、飞书、外部发布与内容连接器明确拆成“实现、配置、启用、验证、有效状态”；没有新鲜显式验证证据时不得显示“已连接”。
- [x] 切片 13A：积分占扣结算/释放按数据库权威行回读并校验租户、流水和金额；启动恢复覆盖停用租户，单条坏记录不得阻断其余恢复。
- [x] 收口：专项测试、`491/491` 全量回归、全部静态质量门、3107 最新服务与真实浏览器验收、第四轮评审和报告。

### 第四轮边界

- 不使用已暴露的云雾密钥，不发起任何真实 AI、检索、支付、飞书或发布调用。
- 不把“已配置”写成“已验证”，不把进程内验证写成跨实例或长期生产证据。
- 本轮不承诺一次完成持久化人工对账队列、异步视频后台主动轮询和全部高成本请求幂等操作单；审计证据进入后续 P1 债务。
- 不改旧项目、不提交或推送 Git、不触碰受保护交付文件。

## 禁止改动

- 旧项目及用户未放入本项目范围的任何目录。
- `docs/升级迭代报告-2026-07/04-路线图与验收标准.md`。
- `docs/纳米Work升级迭代报告-2026-07-26.zip`。
- 不调用真实图片、视频、支付、外部发布或其他付费/不可逆服务。

## 第四轮当前有效结果

- 最终自动化结果：`491/491` 通过，0 失败。
- 静态质量门：typecheck、build、format、服务端 JS 语法与 `git diff --check` 全部通过；lint 0 warning；多租户隔离检查覆盖 61 张表、提示人工复核 36 处动态查询、0 失败。
- 生产装配集成测试覆盖未登录、停用账号、停用租户、缺模块、租户隔离、业务校验与 AI 限流；拒绝路径无业务或占扣写入。`/api/data-intake` 的 `system` 门禁不再静默跳过。
- 运行就绪矩阵覆盖 AI、Scheduler、联网检索、微信支付、支付宝、飞书、内容外部发布和内容连接器共 8 通道；GET 全程零联网，配置与验证分离，密钥不回显。
- 积分占扣结算/释放在事务内回读权威行并校验租户、流水、用户、金额和类型；恢复扫描包含停用租户，单条坏记录和单个子系统失败不会阻断其他恢复。
- 第四轮真实浏览器与服务健康验收：**通过**。3107 以无密钥、Scheduler 关闭方式运行；健康为 `ok=true`、`db=up`。管理员与系统页均显示 8 通道矩阵，AI 为“降级模式”、Scheduler 为“当前已关闭”、外部发布为“仅人工流程”；未出现“AI服务已连接”或“应用机器人已连接”误报；浏览器 error 日志为 `[]`。
- 本地无密钥演示：**GO**。
- 自动化本地回归：**GO**。
- 生产：**CONDITIONAL GO**。
- 生产剩余条件：轮换凭证后的云雾、检索、支付、飞书与外部发布真实验收；持久化人工对账处理器；异步视频任务在无人打开媒体列表时的后台主动轮询；高成本写请求的请求级幂等。

## 第三轮当前有效结果（历史基线，已被第四轮结果取代）

- 最终自动化结果：`483/483` 通过，0 失败。
- 静态质量门：typecheck、build、format、JS 语法和 `git diff --check` 全部通过；lint 0 warning；多租户隔离检查覆盖 61 张表、提示人工复核 36 处动态查询、0 失败。
- 第三轮真实浏览器与服务健康验收：**通过**。`http://127.0.0.1:3107` 最新服务启动，Scheduler 关闭且无真实 AI 通道；`GET /api/health` 返回 `ok=true`、`db=up`；老板账号 `guan` 登录成功；15 个核心路由无登录回退或致命错误，手机版显示核心工作台；餐饮 #101 与内容趋势官完整七面板、岗位专属引导、技能分层证据及连接器阻断状态均符合预期；新鲜浏览器日志为 `[]`，视觉无布局阻断。
- 本地无密钥演示：**GO**。
- 自动化本地回归：**GO**。
- 生产：**CONDITIONAL GO**。
- 本轮定义的本地代码层 P0/P1 发布阻断已全部关闭。
- 本轮四个 P1 闭环：
  - 内容生产仓固定模板明确为参考/草稿，不冒充实时 AI 生成或实时经营数据。
  - 内容自动化返回 `202 + runId + pollUrl`，支持按 `runId` 精确轮询与同一次运行幂等复用；手动/定时执行统一撤权门禁，费用估算、占扣和生成采用同一实际模型。
  - 支付链路拒绝重复关键节点，渠道结果必须绑定本地商户订单，微信签名响应只接受五分钟新鲜窗口。
  - RAG 后台任务在执行前预授权、按真实用量结算，与其他 AI 任务共享并发额度，并覆盖超时释放和服务重启恢复。
- 第三轮当时保留 **CONDITIONAL GO** 的唯一原因：尚未用轮换后的授权沙箱/生产凭证完成云雾、支付、飞书和外部发布链路验收，且本轮已暴露凭据必须撤销或轮换。该“唯一原因”表述已被第四轮审计结论取代。
- 已关闭本轮主要阻断：启动调度默认消费、凭据/错误回显风险、AI 路由保护缺口、内容员工配置降级、强制联网缺证据、内容契约空值绕过、模板底稿统计污染、角色权限与零目标语义问题。
- 第三轮新关闭：
  - 60 名餐饮员工均有逐岗位严格输出契约、合法样本与非法反例，派活、采纳和审计共享同一契约。
  - 409 项历史技能全部进入工作台、执行快照、提示词与前端；当前只证明“目录与执行注入契约已验证”，业务效果仍需真实样本。
  - 历史实际 8 个 `catalog_only` 已归零；13 项连接器运行登记完整，本地辅助能力可执行，真实采集/发布在缺少授权或凭证时明确拒绝。
- 剩余债务：云雾、支付、飞书和外部发布真实链路仍待轮换后的沙箱/生产凭证验证；409 项技能的真实业务效果仍待真实样本。

## 修复前基线（完整保留）

以下“本轮目标”“已完成”“当前运行方式”“发布关口”和全功能回归结论，均记录代码修复开始前的真实联调与诊断状态，不代表修复后的发布判定。

## 本轮目标

在不改动旧项目、不把密钥写入仓库或数据库的前提下，验证纳米Work行业版现有云雾接入是否能完成：

1. 通道鉴权与模型清单探活。
2. Paihuo 内容数字员工真实生成。
3. 完整岗位档案与执行快照锁定。
4. 两段式积分结算、内容落库与审批归档。
5. 管理后台和前台页面可见。
6. 权限、防降级和凭据边界。

## 已完成

- `npm run verify` 全量通过：366 项测试、TypeScript 类型检查、多租户隔离检查、前端生产构建。
- 云雾 `/models` 真实探活成功，返回 408 个模型。
- 管理接口真实识别 `channel=yunwu`，密钥仅通过本次服务进程环境注入。
- Paihuo 趋势官定时任务完成 1 次真实生成：
  - 自动化运行：`content_automation_runs.id=5`
  - 内容：`contents.id=135`
  - 审批：`approvals.id=11`
  - 积分流水：`credit_logs.id=6`
  - 模型：`gpt-5.5`
  - 用量：输入 7,087 tokens，输出 1,700 tokens
  - 本地计费：48 积分，结算后余额 199,952
  - 结果：`ai_mode=api`、输出契约有效、5 个候选选题、待审核、无悬挂占扣
- 真实运行快照包含趋势官的 5 项锁定能力、13 项默认注入技能、完整工作方式、岗位档案、完整提示词哈希和工作配置。
- 浏览器核验通过：内容生产仓、趋势官完整工作台、审批中心、接口管理、积分流水均能看到对应证据；页面控制台无错误。
- 原演示库已恢复到测试前状态：200,000 积分、5 条流水、134 条内容、10 条审批、4 条自动化运行。
- 真实结果保存在隔离测试库；测试库的自动化规则已全部关闭。

## 当前运行方式

- 原演示库：保持测试前状态。
- 隔离测试库：`server/data/nanowork-yunwu-realtest-2026-07-30.db`
- 隔离测试库只用于复核本轮真实结果，自动任务为关闭状态。
- 真实密钥不落库、不落 `.env`、不提交 Git；安全交接时服务应以无密钥模式运行。
- 本轮交接页面：`http://127.0.0.1:3107/content`，当前以无密钥模式运行，可查看已保存的真实结果但不会继续产生云雾费用。

## 发布关口

受控本地联调：通过。  
长期保留真实密钥：不通过。  
生产发布：不通过，须先关闭 `docs/team/10-review.md` 中的阻断项并轮换本轮凭据。

## 下一步

全功能回归追加结论：

- 366/366 自动化测试通过，TypeScript、租户隔离检查和生产构建通过。
- 隔离实例 122/122 个只读 API 返回 2xx 且为合法 JSON。
- 浏览器完成 14 个主页面、老板/销售/平台超级管理员三类角色及趋势官六个完整档案页签验收，控制台 0 条错误。
- 60 名餐饮员工、10 名 Paihuo 内容员工、45 项锁定能力和 409 张技能卡结构可加载；全部 409 项历史技能仍为 `legacy_unverified`。
- 真实趋势官产出虽然完成调用、落库、审批与计费，但明确未执行联网搜索，不能算完成了 `webRequired=true` 的岗位任务。
- 135 条内容中只有 1 条真实 API 产出；120 条为通用“待企业核验的内容草案”，10 条为岗位交付草案，4 条为自动任务契约底稿。流程可运行不等于产出可交付。
- 内容输出契约可被空值绕过：10 个岗位中 9 个岗位的必填字段全部传 `null` 仍会通过。
- 严格前端门禁不通过：199 条可访问性/交互警告、46 个文件未通过格式检查。
- 销售角色可读取全租户老板日报；执行目标缺失时页面出现 `36,910,800%`；移动端存在无权限死入口；平台超级管理员路由与页面角色判断冲突。
- 隔离回归服务已停止，临时自动化已全部关闭，无悬挂积分占扣、无密钥落库；正常 3107 服务保持健康。

下一迭代顺序：

1. 统一全部外部 AI 调用的限流、占扣、结算、后台并发保护和启动调度开关。
2. 统一手动/立即/定时/工具箱四条员工执行链，真实应用模型、长度、超时、联网与审批配置。
3. 为 10 名内容员工和 60 名餐饮员工建立严格、非空、可机器验收的岗位输出契约。
4. 修复角色数据范围、零目标百分比、移动端入口和平台超级管理员路由冲突。
5. 清理占位底稿的产品语义，逐项核验历史技能与连接器，关闭前端严格门禁问题。
6. 使用轮换后的测试密钥做受控复验，全部 P0 关闭后再决定生产放行。
