# 云雾真实联调计划

日期：2026-07-30

## 切片一：基线与保护

范围：

- 检查 Git 状态、数据库配置覆盖和端口状态。
- 备份演示库并做完整性检查。
- 运行仓库全量验证。

验收：

- 不触碰两份既有未跟踪交付物。
- 数据库备份 `PRAGMA integrity_check=ok`。
- `npm run verify` 通过。

## 切片二：真实通道探活

范围：

- 通过进程环境临时注入云雾密钥。
- 登录老板账号。
- 检查管理概览、接口配置和 `/models`。

验收：

- `channel=yunwu`。
- `/api/admin/api-config/test` 返回 `ok=true`。
- 模型数量大于 0。
- 响应不回显完整密钥。

## 切片三：Paihuo 内容员工真实业务闭环

范围：

- 验证趋势官的真实自动化生成。
- 核对员工档案快照、结构化输出、内容、审批和计费。

验收：

- `ai_mode=api`，不能是模板。
- 内容非空且满足岗位 JSON 输出契约。
- 五项岗位能力、技能库、工作方式、提示词哈希和工作配置进入快照。
- 内容为待审核，审批记录存在。
- 占扣已结算，无悬挂 hold。
- 余额变化等于实扣积分。

## 切片四：无费用边界

范围：

- 尝试关闭岗位必备能力。
- 提交缺少标题的派活请求。
- 普通员工访问管理接口。
- 访问不存在的员工编号。

验收：

- 分别返回 400、400、403、404。
- 测试前后积分流水数量不变。
- 不触发新的上游调用。

## 切片五：浏览器验收

范围：

- 内容生产仓。
- 趋势官完整工作台。
- 管理后台概览、接口管理、积分管理。
- 系统审批中心。

验收：

- 页面能看到真实内容、真实 API 流水和待审核审批。
- 趋势官能力、工作方式、技能库、提示词、工作配置、岗位档案都能打开。
- 页面控制台无错误。

## 切片六：安全交接

范围：

- 保存隔离测试库并关闭自动化。
- 恢复原演示库。
- 扫描凭据落盘风险。
- 停止携带真实密钥的服务进程。

验收：

- 原演示库恢复测试前计数与余额。
- 测试库无启用自动化、无明文密钥配置。
- 真实密钥不进入 Git、`.env`、数据库和报告。

## 追加计划：全功能回归与迭代升级报告

状态：已完成。

### 切片七：自动化与接口矩阵

- 运行 366 项仓库测试、类型检查、租户隔离检查和生产构建。
- 统计 341 个唯一服务端路由及覆盖率。
- 在无密钥隔离实例逐项读取 122 个业务接口。
- 运行员工、安全、权限、计费、调度、支付、文件和 RAG 的 172 项专项测试。

### 切片八：浏览器与角色验收

- 逐页检查 14 个老板端主页面。
- 检查销售移动端和平台超级管理员入口。
- 打开趋势官能力、工作方式、技能库、提示词、工作配置和岗位档案六个完整页签。
- 检查内容仓手动、立即和定时三种生产方式。

### 切片九：输出质量与业务语义

- 对 135 条内容、工具运行、内容员工运行、周报和聊天结果做空值、占位语和长度扫描。
- 复核真实趋势官输出是否满足联网、来源、证据和岗位契约。
- 对 10 套内容输出契约做全空值反例。
- 复核经营目标百分比、销售数据范围、移动端入口和平台角色行为。

### 切片十：报告与清理

- 输出全功能回归矩阵、P0/P1/P2 缺陷和四阶段迭代路线图。
- 关闭隔离库全部自动化，确认无密钥、无悬挂占扣、无临时测试账号。
- 停止 3111 隔离服务并确认 3107 正常服务保持健康。

## 第二轮升级实现计划

日期：2026-07-30  
行走骨架：无密钥启动服务时调度器不运行、错误不泄密、测试可证明。

### 切片 1｜发布安全骨架

任务：管理员能够安全配置供应商地址，但真实密钥只从环境读取；服务默认不启动自动化，任何上游错误在返回和落库前脱敏。  
范围：环境配置、服务启动、管理 API、云雾错误处理及测试；不连接真实云雾。  
验收标准：无开关时 Scheduler 不创建运行；API Key 写入请求被拒绝或忽略且不落库；Canary 密钥不出现在响应、日志和数据库。  
验证方式：新增安全测试 + 相关现有测试 + 无密钥隔离启动检查。

### 切片 2｜外部调用成本与后台生命周期

任务：所有高风险外部 AI 入口都在调用前完成权限、余额占扣和并发占位，并在真实后台任务结束后结算或释放。  
范围：AI 限流/计费公共层、工具箱、内容自动化、员工派发、活动/增长/OCR/RAG 的高风险入口；不改支付。  
验收标准：余额不足不调用上游；失败释放占扣；同一请求只扣一次；后台完成前并发位不释放；工具箱不再免费调用真实 AI。  
验证方式：失败注入、并发和两阶段计费专项测试。

### 切片 3｜完整内容员工执行

任务：趋势官等内容员工无论手动、立即还是定时，都真实采用岗位模型、输出长度、超时和联网配置。  
范围：内容自动化、内容员工工作台和共享执行上下文；不做外部发布。  
验收标准：测试修改配置后，模拟上游收到的实际参数一致；`webRequired=true` 必须产生来源证据，否则标记未完成。  
验证方式：路由集成测试、搜索失败/成功反例和快照断言。

### 切片 4｜严格岗位契约与底稿语义

任务：用户看到的“完成产出”必须满足岗位严格 Schema；模板降级和契约底稿清楚标为未完成。  
范围：10 套内容契约、内容落库状态、汇总和效果排行；餐饮岗位先建立可扩展严格契约骨架。  
验收标准：空值、错类型、空数组、缺字段全部拒绝；模板底稿不进入成功数量和效果榜；真实合法样本通过。  
验证方式：每岗位合法样本 + 至少四类非法反例、内容汇总集成测试。

### 切片 5｜老板日报与经营目标

任务：销售只能看到本人/团队允许的数据，缺少经营目标时页面给出“待设置”。  
范围：Dashboard 日报、Execution 目标 API 及前端展示。  
验收标准：销售读取老板日报为 403；老板正常；目标为 0 或不存在时 `rate=null` 且不显示百分比。  
验证方式：角色集成测试、零目标单测和浏览器检查。

### 切片 6｜角色入口一致

任务：桌面与移动端只显示角色真正可访问的入口，平台主管路由和组件判断一致。  
范围：移动端工作台、App 路由、Admin/Recharge 角色判断；不扩大角色权限。  
验收标准：销售不见内容/工具箱/分析死入口；403 简报不会成为首页错误；`platform_super` 路由与页面同一结果。  
验证方式：组件行为测试、三角色浏览器路径。

### 切片 7｜工具箱完整岗位上下文

任务：工具箱运行继承员工档案、能力、技能、质量门和审批边界，并限制运行结果查看范围。  
范围：工具箱路由/引擎和运行列表；不新增工具。  
验收标准：真实执行快照包含完整岗位上下文；普通用户只能看到自己创建的运行；老板/管理员可审计全租户。  
验证方式：工具箱集成测试和跨用户访问反例。

### 排序依据

先修可能产生费用或泄密的架构风险，再修员工完整能力和输出语义，随后修角色/业务问题，最后补工具箱上下文并全量回归。

### 测试策略

- 每个缺陷先增加能真实失败的公共行为测试。
- 每个切片跑专项测试，里程碑后跑 `npm run verify`。
- UI 改动在隔离实例检查桌面和 375px 移动端。
- 全程清空外部密钥，禁止真实付费调用。

### Git 检查点计划

建议但不执行：

- `fix: harden ai runtime and scheduler safety`
- `fix: enforce complete employee execution contracts`
- `fix: align role permissions and business metrics`

### 回滚路径

所有改动保持为未提交工作树文件；若单切片验证失败，只回退该切片的明确文件，不使用 reset、clean 或覆盖用户文件。

## 第三轮能力成熟度实现计划

日期：2026-07-30  
通道：标准通道（现有项目的多模块契约与状态收口）  
行走骨架：任意餐饮员工的岗位档案返回可机器验证的输出契约，合法样本通过、非法样本在业务边界被拒绝。

### [x] 切片 8｜60 名餐饮员工逐岗位输出契约

任务：老板派给任意餐饮员工的结果都能用该岗位自己的严格机器契约验收。  
范围：餐饮岗位目录、工作台档案、员工执行/采纳边界和专项测试；不改变员工职责，不自动批准外部动作。  
验收标准：101-160 每岗均有唯一 Schema、主产物和合法样本；缺字段、空值、错类型、未知字段、空数组至少五类反例被拒绝；企业覆盖不能削弱契约。  
验证方式：餐饮目录/工作台/产出路由专项测试，循环覆盖全部 60 岗，再跑全量测试。

完成证据：60 岗全部具有稳定严格契约、合法样本和非法反例；工作台、派活、采纳、审批与审计共用该契约。

### [x] 切片 9｜409 项历史技能证据化

任务：用户能知道每张历史技能卡来自哪里、适用哪个岗位、当前验证到哪一层，并且系统只把通过离线契约的技能注入执行。  
范围：技能目录、加载校验、工作台展示数据和专项测试；不联网核验第三方算法说法，不承诺真实业务效果。  
验收标准：409 项均有稳定 ID、岗位、版本、来源快照、内容指纹、离线验收样本和明确验证层级；篡改、错岗、空样本或指纹不符时启动失败；界面不再显示含糊的 `legacy_unverified`。  
验证方式：全目录完整性测试、内容篡改/错岗/空样本反例、70 个员工工作台装载测试。

完成证据：餐饮 344 项、内容 65 项全部进入工作台、执行快照、提示词和前端；验证层级固定为“目录与执行注入契约已验证”，真实业务效果明确保留为待真实样本验证。

### [x] 切片 10｜8 个内容连接器运行状态收口

任务：用户在内容员工档案和执行时看到的连接器状态与新项目真实可运行能力一致。  
范围：代码实际盘点出的 8 个 `catalog_only` 连接器、统一运行登记、工作台状态和专项测试；不接真实平台账号、不发布、不产生外部费用。  
验收标准：目录中不再存在 `catalog_only`；每个连接器都有运行模式、启用条件和失败边界；本地可执行连接器能产出契约化结果，需要凭证的连接器明确拒绝并返回可操作说明。  
验证方式：连接器目录和运行器单测、全部 10 名内容员工档案断言、无凭证调用反例。

完成证据：历史实际 8 个 `catalog_only` 已归零，13 项运行登记完整；本地辅助连接器可运行，真实采集/发布缺授权或凭证时 fail closed。

### [x] 第三轮评审与回归（自动化与静态质量门）

任务：证明三个切片没有削弱既有权限、计费、调度、内容和支付边界。  
范围：专项测试、全量测试、类型/构建/格式/隔离/语法检查、服务健康与真实页面；不做 Git 提交或生产发布。  
验收标准：新增测试全部通过；既有 456 项无回归；前端无错误边界和控制台错误；报告只陈述实际证据。  
验证方式：`npm test`、`npm run typecheck`、`npm run check:isolation`、`npm run build`、严格 lint/format、浏览器验收和 `/api/health`。

完成证据：`483/483` 自动化通过，0 失败；typecheck、build、format、JS 语法、diff 检查通过；lint 0 warning；隔离检查覆盖 61 张表、提示人工复核 36 处动态查询、0 失败。

- [x] 专项测试、全量回归与静态质量门收口。
- [x] QA、评审、发布、矩阵、迭代报告和复盘文档收口。
- [x] 第三轮真实浏览器与 `/api/health` 最终验收：3107 最新服务健康，老板登录与 15 个核心路由通过，餐饮 #101、趋势官七面板及新增契约/技能/连接器状态通过，新鲜浏览器日志 0 错误，视觉无布局阻断。

## 第四轮生产可信度实现计划

日期：2026-07-31
通道：标准通道
行走骨架：同一请求必须先经过真实生产守卫链；同一外部通道必须用“配置事实 + 新鲜验证证据”共同决定展示状态；同一占扣必须只按数据库权威记录结算或释放。

### [x] 切片 11｜生产装配链与模块守卫

任务：把 Express 生产装配从启动副作用中分离，并用真实 HTTP 请求证明认证、租户、模块和 AI 限流的执行顺序。  
范围：应用 factory、启动入口、数据导入模块门禁与生产链集成测试；不改业务 Router。  
验收标准：未登录 401、停用账号 401、停用租户 403、缺模块 403、授权请求抵达 Router、低限额 429；`/api/data-intake` 缺 `system` 必须拒绝；拒绝路径无业务或占扣写入；不同租户数据隔离。  
验证方式：隔离临时库、真实本地监听、零外网集成测试。

完成证据：生产应用 factory 已与启动副作用分离；真实 HTTP 集成测试覆盖 401、403、400、429、`Retry-After`、租户隔离和拒绝路径零写入；数据导入缺 `system` 模块返回 403。

### [x] 切片 12｜零联网运行就绪矩阵

任务：统一报告 AI、Scheduler、搜索、支付、飞书、外部发布和内容连接器的当前运行事实，纠正“配置存在即已连接”。  
范围：纯本地 readiness 聚合、管理/系统 API 与页面状态文案；不自动探测任何外部服务。  
验收标准：GET 期间即使 `fetch` 被禁止也正常返回；配置存在但没有新鲜显式测试时为 `configured_unverified`；只有配置匹配且新鲜测试通过才为 `connected`；Scheduler 关闭、微信缺验签证书、外部发布无适配器等情况明确阻断；响应不含密钥。  
验证方式：纯函数和路由测试、前端类型检查、真实页面检查。

完成证据：8 通道矩阵已接入管理概览、接口管理和系统页；GET 测试在禁止 `fetch` 时通过；显式测试证据按进程、TTL 和配置指纹约束，响应不回显密钥；真实页面无连接状态误报。

### [x] 切片 13A｜恢复与占扣权威一致性

任务：结算/释放不信任调用方携带的占扣字段，恢复扫描不遗漏停用租户也不被单条坏记录拖垮。  
范围：积分引擎、启动恢复与专项测试；本切片不新增外部供应商轮询和人工对账 UI。  
验收标准：事务内按 hold ID 回读并校验租户、流水、金额；伪造跨租户对象失败且账本不变；重复终态幂等；停用租户可恢复；单条错误不阻断其他记录；余额与流水恒等。  
验证方式：双租户、伪造字段、重复操作、停用租户和故障注入测试。

完成证据：结算/释放事务内回读权威 hold；跨租户、错流水、错用户、错金额和错类型返回完整性冲突且账本不变；重复终态幂等；停用租户、单条坏记录和子系统故障隔离均通过。

### 第四轮收口（已完成）

- [x] 全部服务端测试 `491/491` 通过，0 失败。
- [x] 前端类型、构建、严格 lint/format、隔离、服务端 JS 语法和 diff 质量门全部通过。
- [x] 3107 重启到最新代码；健康、管理员概览、接口管理和系统运行状态通过真实浏览器验收，浏览器 error 日志 0。
- [x] QA、评审、发布与复盘完成；持久化对账、视频主动轮询和高成本请求幂等继续作为后续生产债务。

## 最新前端增量整理计划

日期：2026-07-31
通道：标准通道
范围：只整理新项目刚升级的未提交增量，不修改旧项目，不提交或推送 Git。

### [x] 切片 14｜全局搜索与业务检索

任务：把新增命令面板从“能打开”收口到权限正确、查询正确、并发状态正确。
验收标准：导航与最近访问只出现当前角色入口；客户、内容和活动按现有模块权限启用；内容搜索使用真实接口；旧请求不能覆盖新查询；辅助搜索失败不打断主流程；输入法和键盘操作可靠。
完成证据：内容搜索端点、关键词 SQL 转义、租户/人员范围、请求取消、查询版本、IME、ARIA 和 100 字边界均已实现并进入回归。

### [x] 切片 15｜分组导航与响应式回归

任务：保留桌面分组语义，同时不破坏 820px 以下的横向导航和 1320px 以下的命令入口。
验收标准：桌面显示四组导航；窄屏隐藏组标题并展开为横向列表；中窄屏保留可操作的搜索图标；系统快捷键不劫持错误平台或组合键。
完成证据：响应式组容器、命令按钮尺寸与 Mac/非 Mac 快捷键边界已修复。

### [x] 切片 16｜图表、主题与异常恢复

任务：保证异步数据、含函数配置、StrictMode 重建和主题切换都能驱动图表更新，并为应用/路由异常提供可访问恢复状态。
验收标准：图表不因不可序列化配置停更；重复配置不做无效更新；主题在图表创建前生效；异常提示可被辅助技术感知并获得焦点；查询参数变化可重置路由异常。
完成证据：配置双路径更新、主题版本、挂载时序、图表标签、错误边界焦点和 `pathname + search` 重置键均已落地。

### [x] 切片 17｜工程与运行验收

任务：对合并后的最新工作树执行完整门禁、生产构建、无密钥启动和核心接口冒烟。
验收标准：自动化零失败；类型、隔离、设计、lint、format、build 和 diff 全通过；3107 健康；老板可登录；内容搜索返回结果；超长关键词返回 400。
完成证据：`491/491` 通过，3871 个前端模块完成生产构建；健康、登录、内容搜索和输入边界 HTTP 验收通过。应用内浏览器因本地地址安全策略未能自动刷新，视觉确认保留为人工刷新项。

## 数字员工任务闭环与工作台减法计划

日期：2026-07-31
通道：标准通道
范围：只改新项目的数字员工目录、工作台、任务入口和对应运行查询，不修改旧项目，不删除或降级完整岗位能力，不执行真实外部 AI，不提交或推送 Git。

### [x] 切片 18｜持久任务进度与审阅入口

任务：让餐饮数字员工关闭工作台后仍能找到本人权限范围内的任务，并把“待审阅”解释为已生成、等待人工处理。
验收标准：最近任务按租户、岗位和账号数据范围过滤；老板可见本租户范围，普通账号只见本人范围，跨租户不可见；待审阅计数准确；任务详情能识别岗位契约未通过的模板降级结果并禁止误导用户采纳；审阅统一进入审批中心。
验证方式：服务端专项测试、权限反例、真实预览库只读核对和浏览器任务详情验收。
完成证据：餐饮工作台已持久展示最近 8 条权限内任务；预览库 #15 可从钱商圈工作台直接找到，明确标识“待审阅不是卡住”，契约无效结果禁止采纳并可直达审批中心。

### [x] 切片 19｜操作优先的完整员工工作台

任务：把“派活、看进度、去审阅”放在首屏，把内容员工任务历史前移，把重复说明和非必填材料折叠。
验收标准：老板、管理员和平台超管的七个完整面板仍直接存在；其他角色只显示“派活与任务”，接口和运行快照不泄露能力、技能、提示词、工作方式、工作配置、岗位档案及内部来源，但派活执行仍锁定完整档案；岗位派活说明按租户、账号、员工和档案版本首次展示一次，后续默认隐藏且可恢复；选填材料可展开；派活后刷新任务历史；“待执行”明确链接普通经营任务看板。
验证方式：角色入口契约、TypeScript、键盘与 ARIA 检查、桌面和窄屏真实页面验收。
完成证据：老板端七个完整面板保留；运营员工端仅保留“派活与任务”，服务端同时清空所有内部档案；岗位说明首次展开、再次打开默认收起。该最终口径由切片 26 与 D-035 取代本切片早期只隐藏能力/技能的窄口径。

### [x] 切片 20｜员工目录与任务看板减法

任务：减少员工目录营销装饰和重复数据，使选择员工、打开工作台和寻找普通任务入口更直接。
验收标准：目录头部紧凑；动态和档案来源默认折叠；员工卡只保留岗位决策所需信息与明确操作；侧栏统一命名“任务看板”；`/execution#task-board` 可定位并聚焦经营任务区。
验证方式：TypeScript、lint、设计 token、桌面/窄屏浏览器与深链验收。
完成证据：目录已压缩为岗位选择界面，卡片只保留姓名、岗位、状态、职责和打开按钮；侧栏及深链统一为任务看板，浏览器实测可到达“经营任务 · 待执行”。

### [x] 切片 21｜完整回归与本地交付

任务：验证本轮减法没有破坏岗位完整能力、权限、审批、内容生产、既有前端增量和本地无密钥安全边界。
验收标准：服务端全量测试、前端类型、隔离、设计、lint、format、build 和 diff 全部通过；3107 使用最新构建启动且健康；老板登录后能在两次操作内找到待审阅任务和普通待执行任务；浏览器无新增错误。
验证方式：`npm run verify`、本地 API 冒烟、真实浏览器和受保护文件哈希复核。
完成证据：`npm run verify` 全量通过，服务端 `493/493`；3107 最新无密钥构建健康；老板与运营员工完成接口和真实页面角色验收；浏览器日志为 `[]`；受保护文件哈希未变化。

## 70 名数字员工逐岗出活测试计划

日期：2026-07-31
通道：标准通道
范围：60 名餐饮数字员工（#101-#160）与 10 名 Paihuo 内容员工（#0-#9）；测试只写隔离临时库，不污染 3107 预览库，不调用真实外部 AI，不提交或推送 Git。

### [x] 切片 22｜逐岗执行行走骨架与证据矩阵

任务：建立一条可重复运行的隔离验收命令，让每名数字员工都真正经过派活、后台生成、结构化校验和业务落库，而不是只检查目录或静态样例。
范围：测试脚本、隔离数据库、确定性测试生成器和逐员工结果矩阵；不修改员工业务定义，不访问公网。
验收标准：矩阵恰好包含 70 名不同员工；每人有员工编号、岗位、任务 ID、终态、AI 模式、契约结果、产物数、输出摘要、审批/审阅状态和计费终态；任一员工缺失或失败时命令非零退出。
验证方式：运行专用 70 人验收命令，核对员工唯一数、隔离库清理和失败反例。
完成证据：新增 `npm run test:employees` 与 70 行 L4 矩阵；恰好覆盖餐饮 #101-#160、内容 #0-#9，缺岗、重复、契约失败、空产物、悬挂占扣均会让命令非零退出。证据落盘到 `docs/升级迭代报告-2026-07/evidence-70-employees/full-production.json`。

### [x] 切片 23｜60 名餐饮员工逐一出活

任务：逐一派活给 #101-#160，并验证每个岗位生成自己的结构化交付。
范围：餐饮员工工作台、分部任务、岗位输出契约、内容落库、审批单和积分占扣；不自动采纳或执行外部动作。
验收标准：60/60 均从生成中进入待审阅；输出非空且岗位契约有效；主产物属于当前员工；任务、内容、审批、快照与积分记录一一对应；无串岗、孤儿审批、悬挂占扣或假成功。
验证方式：隔离 HTTP 派活 + 后台轮询 + 数据库权威行断言，并逐项输出矩阵。
完成证据：60/60 通过正式 `createApp` HTTP、中间件、岗位快照、严格输出契约、内容与审批落库、人工通过和积分结算闭环；每岗只有一个主产物，无悬挂占扣。

### [x] 切片 24｜10 名内容员工逐一出活

任务：逐一派活给 #0-#9，并验证趋势、调研、拆解、文案、风格、多媒体、封面、演绎、分发和复盘岗位各自产出。
范围：内容员工工作台、岗位 JSON 契约、派生产物、运行快照、审阅和积分；不自动发布。
验收标准：10/10 均进入待审阅；契约有效且至少一个主产物；HTML 岗位生成安全可下载 HTML，分发岗位只生成待人工采纳的发布包；无错岗、空壳产物、悬挂占扣或自动外发。
验证方式：隔离 HTTP 派活 + 后台任务排空 + 运行/产物/计费断言，并逐项输出矩阵。
完成证据：10/10 通过岗位严格 JSON 契约、主产物下载、运行快照、人工采纳、素材落库和积分结算；演绎师 HTML 可安全下载，分发官只形成可发布内容且发布日志为 0。

### [x] 切片 25｜失败修复、全量回归与证据分级

任务：修复全部逐岗失败并生成可复核报告，明确区分确定性运行契约测试与真实云端内容质量测试。
范围：失败岗位代码与测试、全量工程门禁、QA/状态/决策文档；真实云端 70 次调用单独走费用安全闸。
验收标准：本地隔离矩阵 70/70 通过，模板降级和非法输出不能被计作出活；`npm run verify` 通过；报告逐员工列证据，并明确哪些业务效果仍需真实云端样本。
验证方式：专用逐岗命令、`npm run verify`、受保护文件哈希和报告逐项复核。
完成证据：`npm run test:employees` 为 70/70 `PASS_OFFLINE_PIPELINE`；`npm run verify` 为服务端 `509/509`，TypeScript、隔离、设计、界面契约、lint、Prettier 与构建全部通过。该证据明确标记为 `L4_FULL_PRODUCTION_HTTP + deterministic_mock`，外网尝试 0；不替代 L5 真实云模型质量、延迟和稳定性验收。

### [x] 切片 26｜老板侧岗位内部档案权限收口

任务：把能力、技能库、提示词、工作方式、工作配置、岗位档案及内部来源从普通员工视图彻底移除，同时保留派活、本人进度和结果。
范围：餐饮/内容员工工作台、员工目录、任务与运行快照、产出钻取、工具运行、内容提示词指南、前端页签与角色文案；不削弱服务端真实执行注入。
验收标准：老板、管理员、平台超管可见完整内部档案；运营负责人和所有普通角色的正式 API 响应不包含上述内部数据，且不能通过其他详情端点旁路读取；前端只显示“派活与任务”；审阅和本人结果权限保持原样。
验证方式：老板/运营负责人/普通员工角色矩阵、敏感词响应扫描、前端类型与构建、真实浏览器双角色验收。
完成证据：仅 `boss/admin/platform_super` 获得完整内部档案；`ops_director/manager/staff/sales/partner` 在员工目录、两类工作台、分部详情、任务状态、协作任务、内容/媒体列表与详情、生成响应、自动化记录、产出穿透、工具箱、提示词指南、公共技能目录和提示词状态接口均由服务端正向白名单或角色门禁保护；受限角色不再获得执行快照、哈希、连接器策略、契约源字段或字段级错误，无 AI 降级底稿也不复述内部档案。角色专项 `110/110`、全量 `509/509` 通过；真实页面中老板保留 7 页签，运营两类工作台只保留“派活与任务”，系统页无提示词入口。

## 三角色真实验证、业务穿刺与 Quiet Command 升级计划

日期：2026-07-31
通道：完整通道
范围：新项目内老板、运营管理层和一线员工的任务闭环、业务穿刺和全局 UI；不修改旧项目，不使用旧暴露密钥，不执行图片/视频/支付/飞书/外部发布，不提交或推送 Git。

### [x] 切片 27｜需求、角色矩阵与设计锁定

任务：把三角色操作、真实 API、安全停止条件、业务不变量和 Quiet Command 方向写成可验收契约。
范围：`docs/team/00-brief.md`、PRD、设计、架构、决策、计划和状态账本；不改运行代码。
验收标准：每个角色有可点击页面、允许动作、拒绝动作、数据范围和最终证据；真实云样本与真实应用 API 明确分级；官方设计来源和不做项可追溯。
验证方式：文档交叉复核、路由/角色矩阵和现有页面基线截图。

### [x] 切片 28｜唯一数字员工审核命令

任务：统一餐饮数字员工旧审核入口与审批中心，消除任务、内容、审批、知识和资产分叉。
范围：审核领域服务、两个兼容路由、权限、事务、幂等和专项测试；不改变岗位执行质量。
验收标准：员工自审 403，高风险非老板 403；通过/驳回同步所有关联状态；重复请求不重复建知识或资产；并发只有一个决定成功。
验证方式：TDD、双入口等价测试、角色反例、重复/并发和数据库不变量断言。
完成证据：餐饮旧审核入口与审批中心复用同一事务命令；任务、内容、审批、知识与资产同步，权限、幂等和高风险老板终审进入回归。

### [x] 切片 29｜人工任务状态机与审核审计

任务：关闭任意 PATCH 状态旁路，补齐提交、审核、退回与重开审计。
范围：任务路由、转换服务、向后兼容字段和专项测试；不批量改写历史任务。
验收标准：只有合法转换成功；待审核必有待审提交；审核记录含人、时间和理由；完成重开需要独立动作和理由。
验证方式：角色/状态矩阵、并发更新、历史兼容和跨租户反例。
完成证据：负责人本人接单/提交、职责分离审核、父子完成门禁、驳回重提、带理由重开和审核人/时间/意见审计全部通过。

### [x] 切片 30｜老板参谋到管理层接单

任务：把会诊建议转成有负责人、有下一步、可关闭的经营任务，移除假“执行中”数字员工任务。
范围：参谋路由、负责人解析、分部接单契约、前端反馈和专项测试。
验收标准：文本执行人能匹配同租户账号；缺省由运营负责人接单；转派分部生成待执行管理任务；响应返回任务、执行中心和穿刺入口；重复转换不重复。
验证方式：老板/运营角色测试、负责人匹配、非法分部、幂等和数据库断言。
完成证据：会诊转任务按本企业负责人解析，缺省进入运营管理层；转派分部形成待执行管理任务，不再制造无 worker 的假执行中任务。

### [x] 切片 31｜业务穿刺 API 与数字任务入口

任务：提供来源到任务、运行、审批、内容/素材/资产的角色安全读模型，并让用户从任务处看到当前节点和下一动作。
范围：只读穿刺路由、角色 DTO、前端时间线/入口和测试；不伪造完整内容流水线。
验收标准：餐饮、内容、人工/参谋三类链路可查；节点和连线均有权威 ID；跨租户 404；运营/员工响应无内部档案；终态与下一动作正确。
验证方式：fixture 工厂、角色/租户矩阵、断链空态、浏览器时间线检查。
完成证据：四类来源均接入业务流入口；人工任务支持 12 层受限递归，根链包含全部可见后代和提交，员工父链匿名；专项 4/4 通过。

### [x] 切片 32｜Quiet Command 全局壳层

任务：把四边常驻框架重构为两栏主工作区和按需助手，统一主题层级与交互动效。
范围：主题、MainLayout、命令面板、响应式和可访问性；不重写业务页面。
验收标准：1440px 主工作区明显扩大；助手默认关闭且可键盘打开；无常驻底栏；导航可收起；焦点、移动端与 reduced-motion 正常。
验证方式：类型/lint/build、桌面/窄屏截图、键盘导航和计算样式检查。
完成证据：两栏壳层、可收起导航、默认关闭助手、命令面板、响应式与 reduced-motion 已落地，设计 token 五项门禁全部改善。

### [x] 切片 33｜员工、内容生产仓与执行页任务化

任务：把核心页面从等权卡片墙改成当前任务、Artifact 和业务时间线优先。
范围：数字员工目录/工作台、内容生产仓、任务与审批核心区；保留完整岗位能力与角色权限。
验收标准：老板两次操作内能派活/看进度/去审核；运营看到接单和下一派活；员工看到本人任务和提交；内容自动与定时任务入口可见；空态和加载态不是静态占位。
验证方式：三角色浏览器行走、页面可访问性、视觉对比和业务 API 断言。
完成证据：Paihuo 内容部门进入内容生产仓；员工目录、工作台和任务指挥台改为列表/当前任务/业务流优先；老板完整档案、运营与员工受限视图均实测通过。

### [x] 切片 34｜三角色真实应用 API 验收

任务：用正式应用装配模拟老板、运营和销售/员工逐项操作，不联网、不调用供应商。
范围：隔离数据库、登录、模块权限、任务、审核、穿刺、计费不新增和可重复验收脚本。
验收标准：允许路径成功、拒绝路径 403/404、跨租户隔离、状态和关联记录一致；脚本默认外网调用 0、供应商调用 0。
验证方式：专用命令、输出矩阵、数据库快照差异和请求日志。
完成证据：全新隔离库首次启动后，老板 → 运营 → 一线员工完成派发、接单、拆解、提交、逐层审核和递归穿刺；员工无公司目标/计划或内部岗位档案；外网和计费变化均为 0。

### [x] 切片 35｜受控真实云模型样本

任务：在轮换测试密钥、隔离库和供应商硬额度下完成最多三个代表性文本生成样本。
范围：老板参谋、餐饮员工、内容员工；销售只跑拒绝路径；不含图像、视频和任何外部动作。
验收标准：三个样本均为 `aiMode=api` 且使用目标模型；岗位契约、落库、审批、占扣结算和输出摘要有效；任何停止条件触发立即终止。
验证方式：`/models` 探活、单次无重试 POST、轮询、数据库权威行和供应商调用计数。
完成证据：后续获得新的受控运行凭据后，验收范围已从 3 个代表样本扩大到 70 个数字员工真实云任务和 27 个功能场景；凭据只在进程环境中使用，没有落入仓库、报告或数据库。

### [x] 切片 36｜全量回归、视觉复验与报告

任务：修复三条主线的所有失败，完成工程门禁、浏览器回归、保护文件校验、迭代报告和 3107 最新版本交付。
范围：专项与全量测试、类型/隔离/设计/lint/format/build、截图和文档；不发布生产。
验收标准：本地 P0/P1 关闭；测试零失败；三角色流向证据和 UI 前后对比可复核；保护文件哈希不变；3107 健康。
验证方式：`npm run verify`、三角色脚本、浏览器、健康检查、哈希和报告复核。
完成证据：`522/522`、70/70、业务流 4/4、类型/隔离/token/UI/lint/format/build 全绿；三角色浏览器与保护哈希通过，最终报告已更新，3107 使用最新构建交付。

## L5 全量真实云 API 逐岗验收与状态机重构

日期：2026-07-31
通道：标准通道（自动模式）
范围：新项目共 70 名数字员工（餐饮 #101-#160 共 60 名，Paihuo 内容 #0-#9 共 10 名）及内部文本 AI 主路径；不修改旧项目，不实际执行外部发布、支付、飞书或其他不可逆动作。

### [x] 切片 37｜全量功能与状态语义盘点

任务：让老板看到一张没有遗漏的真实验收清单，并找出“可使用/禁止使用/待审阅/失败”等互相打架的状态。
范围：AI 用户入口、员工目录、任务/产出/审批/内容/素材/资产状态、后端转换和前端文案；不改业务数据。
验收标准：每个入口有角色、端点、模型/凭证、可逆性、目标终态和验证方式；状态图能唯一回答是否生成、是否合规、是否审过、是否可用、下一步是什么。
验证方式：代码与数据库 schema 搜索、正式 API 响应抽样、浏览器状态文案核对。
完成证据：`artifacts/real-feature-matrix-l5.json` 盘点并验收 27/27 个功能场景，其中 25 个真实云 API 交付和 2 个权限边界均符合预期；自动质检通过与人工采纳已分层表达。

### [x] 切片 38｜真实云逐岗矩阵运行器

任务：可以安全地分批执行 70 个（60+10）真实云员工任务并断点续跑，任何降级或半成品都会让矩阵失败。
范围：复用现有逐岗脚本和正式 HTTP API，增加真实供应商模式、并发控制、结果证据、重试队列和费用汇总；不注入 mock。
验收标准：每岗保存业务 ID、模型、`aiMode=api`、token、费用、时延、契约、主产物、审阅/账务终态和失败原因；中断后只续跑未完成项；密钥不落盘。
验证方式：先以 2 个餐饮岗 + 2 个内容岗做行走骨架，再扩大到全量；人工核对数据库权威行与报告一致。
完成证据：`artifacts/real-employee-matrix-l5-full-v2.json` 保留 70 岗的业务 ID、哈希、契约、人工审阅、计费和业务流证据；`template/failed/mock/fixture/fallback` 均不能计通过，凭据策略为 `environment_only`。

### [x] 切片 39｜60 名餐饮员工真实逐岗执行

任务：逐一派活给 #101-#160，确认每个岗位能用真实模型生成自身完整交付。
范围：正式登录、岗位派活、异步执行、契约、内容、审批、人工审阅、知识/资产和计费；不执行对外动作。
验收标准：60/60 均为真实 API、契约有效、主产物非空、完整岗位快照锁定、人工终态与账务终态一致；失败项必须带根因并修复后重跑。
验证方式：真实云矩阵、任务/内容/审批/积分数据库交叉核对、业务流抽查。
完成证据：餐饮 #101-#160 共 60/60 `PASS_REAL_API`，全部契约有效、有主产物、人工采纳、账务 `settled`、业务流终态完整。

### [x] 切片 40｜10 名 Paihuo 内容员工真实逐岗执行

任务：逐一执行趋势、调研、拆解、文案、文风、多媒体、封面、演绎、分发和复盘，并把合格产物纳入内容生产仓。
范围：正式派活、必要联网证据、严格 JSON/HTML 契约、事实门槛、人工审阅、素材/内容和计费；不自动发布。
验收标准：10/10 `aiMode=api`、契约有效、主产物可下载/可审阅、事实缺失不虚构、采纳后内容/素材状态一致；发布日志保持 0。
验证方式：真实云矩阵、产物下载、内容仓与业务流抽查、失败返工重跑。
完成证据：Paihuo #0-#9 共 10/10 `PASS_REAL_API`，全部为 `aiMode=api`、契约有效、主产物非空并经人工采纳，外部发布始终为 `false`。

### [x] 切片 41｜唯一产出状态机与失败修复

任务：让所有页面和接口对“是否可使用、为何禁止、谁来处理、下一步是什么”给出同一个答案。
范围：任务、生成、契约、风控、审阅、内容、素材和资产状态映射；历史审计记录不删除。
验收标准：生成状态、质量状态、审阅状态和使用状态分层；`可使用` 只在真实 API + 契约有效 + 必要审批通过后出现；禁止使用必须返回机器原因码和人话下一步；前后端无冲突文案。
验证方式：先写状态转换反例，再实现；API/数据库/UI 三方契约测试和历史异常记录回放。
完成证据：自动质检通过显示为“质检通过，待发布决策”，仅真实人工审批通过显示“人工已采纳”；驳回与全额释放在同一事务终结，未授权角色不再获得审核操作。服务端最新全量回归 `645/645` 通过。

### [x] 切片 42｜失败重跑、角色穿透与发布收口

任务：只重跑失败项直到明确终态，并给出逐岗真实证据和仍受外部条件阻断的功能。
范围：失败队列、三角色浏览器、业务穿透、全量工程门禁、QA/评审/报告；不提交或推送 Git。
验收标准：70（60+10）逐岗矩阵无未解释失败；外部不可逆入口全部诚实 fail-closed；`npm run verify`、浏览器桌面/窄屏和服务健康通过；报告可追溯到每个业务 ID。
验证方式：L5 矩阵、失败重跑清单、`npm run verify`、浏览器截图/日志、数据库对账和报告复核。
最终完成证据：70/70 真实员工矩阵、70/70 质量审计、27/27 功能矩阵通过；最新完整 `npm run verify` 的服务端全量回归快照为 `696/696`，类型、隔离、token、UI 契约、严格 lint、格式和生产构建门禁全绿。老板、管理层、员工三角色均以真实账号完成浏览器行走，最新控制台错误为 0；员工排行榜跨用户泄漏已修复并复验。预览库定向对账 6 条内容、6 条审批、2 条员工任务和 4 条自动化运行，普通任务与提交错位为 0；预览库与 L5 隔离库当前 `held` 均为 0。3107/3108 服务健康，支付、飞书、外部发布和视频仍按边界 fail-closed。

### [x] 切片 43｜content:9 事实门禁工程收口

任务：防止“每周内容效果复盘”在没有业务来源时生成看似专业但不可证实的百分比、权重、行业基准或阈值。
范围：content:9 输出契约、事实来源判定、失败后的真实模型重试上下文和回归测试；本切片不把最新真实浏览器复跑结果写成已通过。
验收标准：无来源的百分比、权重、行业基准和阈值必须使契约失败；“待补历史基线”“由负责人设定”等诚实待补表述允许通过；任务材料或已验证网页中明确存在的事实可以复用。
完成证据：上述正反例均进入服务端回归，最新完整 `npm run verify` 为 `696/696`；TypeScript、租户隔离、设计 token、UI 契约、严格 lint、Prettier 和生产构建全部通过。真实云浏览器复跑另以运行 #10、内容 #142、审批 #18 和计费 hold #22 留证。

### [x] 切片 44｜未完成任务逐因关闭与唯一状态语义

任务：逐项解释历史未完成任务，修复复盘官真实重试、计费资格和跨页面状态冲突，再以同一自动规则真实复跑。
范围：自动内容、内容员工任务中心、媒体任务、工具箱、业务流、驾驶舱、审批、内容资产和知识库；历史审计记录只读保留。
验收标准：供应商返回不等于可用；未结算统一待对账且无审阅/下载/入库；只有有效契约、真实用量和结算证据可进审批；驳回无资产/知识库，批准后才沉淀，且发布记录仍为 0。
完成证据：运行 #10 使用真实云模型一次成功，5,095/591 token、实扣 5 分、hold #22 已结算；内容 #142 经审批 #18 从待审核变为可使用，资产 1、知识库 1、发布 0。此前 #7/#8 驳回和 #9 失败均保留且无下游副作用。完整工程门禁 `696/696`。

## 2026-08-08｜切片 45–53 开发计划

### [ ] 切片 45｜普通员工产出免审 walking skeleton

- 任务：让一个低风险数字员工从最简派活到可用结果全链路不创建审批单。
- 输入/输出：`goal` → canonical 执行 → 合法主产物 → `已完成/可使用`。
- 边界：高风险、付费、外部动作仍为老板审批；历史待审不迁移。
- 验收：HTTP、数据库、任务中心、内容/资产读回一致；重复完成不重复建资产；反例进入正确审批。

### [ ] 切片 46｜超级管理者审批规则

- 任务：审批策略 v2、兼容迁移、系统配置 UI 和权限。
- 输入/输出：四种策略与指定审批人 → 可解释规则摘要与任务锁定快照。
- 边界：仅 `platform_super` 可写，老板/管理员只读；三条安全底线不可关闭。
- 验收：角色 200/403、v1 兼容、v2 语义、防篡改快照和四策略矩阵。

### [ ] 切片 47｜最简派活与 resolvedBrief

- 任务：餐饮、Paihuo 内容员工统一只要求一句目标，兼容旧字段。
- 输入/输出：目标/附件 → 字段级来源的 `resolvedBrief` 与工具计划。
- 边界：不猜业务事实；关键缺失只返回一个聚焦问题。
- 验收：商圈任务自动取门店位置并计划地图/联网；内容趋势任务自动联网；纯内部任务不做无意义外呼。

### [ ] 切片 48｜70 人完整能力默认启用

- 任务：运行包统一注入所有能力、老板确认技能、提示词、配置、岗位档案、API、工具、联网和知识库。
- 输入/输出：canonical profile → execution snapshot 与加载证据。
- 边界：角色可见性维持老板侧七面板，受限角色无旁路泄漏。
- 验收：70/70 字段、技能、绑定加载计数通过；前台无“历史技能待核验/待启用”误导文案。

### [ ] 切片 49｜内容仓入口收敛

- 任务：主导航隐藏 AI 图片/AI 视频/AIPPT，改为数字员工中心；旧深链兼容跳转。
- 输入/输出：旧 tab/query → 多媒体师、演绎师或 AI 带货员派活界面。
- 边界：不删除历史媒体任务、处理器、素材和下载能力。
- 验收：桌面/375px 入口、历史任务回看、路由兼容和权限通过。

### [ ] 切片 50｜AI 带货员完整岗位

- 任务：新增第 11 名内容员工的 canonical profile、老板七面板、最简派活、图片分类、脚本/分镜契约和任务持久化。
- 输入/输出：目标 + 图片 → 事实清单、口播、字幕、三镜头计划。
- 边界：不足事实不补造价格、功效、顾客评价和经营结果。
- 验收：单员工 API/UI、角色权限、输入校验、契约正反例和无凭证可恢复阻断。

### [ ] 切片 51｜MiniMax Hailuo 异步视频连接器

- 任务：实现 MiniMax H3 v2 与 Hailuo 2.3 兼容白名单、创建/轮询/取文件、错误脱敏、幂等和重启恢复。
- 输入/输出：镜头 + 首帧 → 固化视频片段与 provider 证据。
- 边界：密钥不落库不回显；默认 transport 测试不联网；未授权不调用。
- 验收：H3 未核价时零外呼阻断；模拟 transport 下覆盖成功、排队、失败、超时、非法 URL/MIME、重复回调和断点恢复。

### [ ] 切片 52｜30 秒分段合成与费用闭环

- 任务：H3 2×15 秒 / Hailuo 3×10 秒片段编排、本地合成、逐段重试、预授权/结算/释放与最终产物。
- 输入/输出：三段固化视频 → 单一 30 秒视频、预览/下载、用量费用。
- 边界：片段未齐不伪装完成；失败不重复扣成功片段；对外发布另行授权。
- 验收：模拟 provider 完整 E2E、合成时长、幂等、部分失败恢复、悬挂占扣为 0。

### [ ] 切片 53｜全量验收与交付

- 任务：自动化、三角色页面、任务流向、业务穿刺、安全和文档收口。
- 输入/输出：新鲜隔离库 → 可复查证据与迭代报告。
- 边界：不使用已暴露密钥，不发起未授权视频费用，不提交/推送 Git。
- 验收：`npm run verify` 全绿；角色/审批/70 人/AI 带货媒体专项全绿；浏览器控制台无错误。

## 2026-08-08｜全员人工操作、统一任务中心与桌面证据包（当前执行计划）

### [ ] 切片 54｜72 名员工真实输入输出矩阵

- 任务：逐个以老板身份给 60 名餐饮员工、10 名 Paihuo 内容员工和 1 名 AI 带货员派发岗位匹配任务，并等待权威终态。
- 范围：专用隔离库、loopback 服务、真实云文本通道、联网证据、岗位业务产物和账务；不污染 3107 预览库，不自动发布、支付或发送消息。
- 验收标准：每岗有真实请求、模型/联网证据、主产物、契约结论、终态、耗时、token、积分和人民币口径；失败/阻断也保留完整原因。
- 验证方式：Luna 运行真实员工矩阵并以 API、数据库、hold、credit log、产物哈希交叉核对，开放占扣最终为 0。

### [ ] 切片 55｜统一任务中心和紧凑任务看板

- 任务：老板从唯一“任务中心”看到刚派发的所有员工、内容、媒体、自动化和工具任务，知道执行到哪一步并能打开结果。
- 范围：统一只读聚合 API、任务中心页面、导航入口、角色数据范围、紧凑看板；不改变各业务表的权威状态机。
- 验收标准：任务卡显示员工、任务、来源、当前步骤、进度、耗时、费用、异常和结果入口；管理角色看企业范围，员工只看本人/授权范围；空/加载/错误状态齐全。
- 验证方式：Sol 实现，Luna 用老板与员工账号逐项派活后检查任务出现、状态推进和结果深链；桌面与 375px 截图复查。

### [ ] 切片 56｜内容员工完整档案与默认免审

- 任务：修复分发官等内容员工看不到技能、工作方式、提示词、工作配置、联网/知识库和岗位档案的问题，并让普通内部产出默认自动采用。
- 范围：0–9 Paihuo 与 idx10 工作台投影、角色白名单、审批路由和图标；普通员工继续只看派活与授权结果。
- 验收标准：老板/admin/platform_super 获得完整七面板和运行绑定；普通内部任务无审批记录并进入可使用；付费、外发、不可逆动作仍显示执行授权门。
- 验证方式：Sol 修复，Luna 执行角色 API/UI、审批行、任务终态与图标渲染专项。

### [ ] 切片 57｜内容生产仓全功能人工行走

- 任务：逐个打开内容仓 11 名员工、自动任务、任务/结果、素材、模板、AI 带货员和历史媒体能力，执行可执行主路径。
- 范围：浏览器真实点击、表单输入、上传测试素材、状态轮询、结果查看；缺凭证或高成本外部能力按真实阻断记录。
- 验收标准：所有入口有明确成功、失败或阻断终态；无死按钮、通用错误指引、缺失档案或伪进度；控制台无新错误。
- 验证方式：Luna 浏览器逐项操作并保存截图、输入、输出和缺陷表；Sol 修复后再由 Luna 复验。

### [ ] 切片 58｜桌面三文件证据包与总报告

- 任务：将每名员工证据和全功能矩阵整理到桌面独立文件夹，提供老板可直接浏览的索引。
- 范围：72×3 Markdown、总览、费用汇总、功能矩阵、失败与阻断清单、截图索引；不包含密钥、登录密码、完整内部提示词或跨租户数据。
- 验收标准：每名员工恰好有三份主文件，输入/输出/费用可互相追溯；汇总数量、token、积分、成本和终态与权威 JSON/数据库一致。
- 验证方式：Luna 导出后运行目录结构、哈希、敏感信息扫描和汇总算术检查，主线程抽样复核至少餐饮、内容、AI 视频三类。

## 2026-08-10｜派活式对话与文件交付切片

### [ ] 切片 64｜数字员工对话工作台

- 任务：把餐饮与内容单岗的公开界面收敛为消息流和底部一句话输入框。
- 输入/输出：问题与附件 → 用户消息、真实执行消息、助手报告消息、文件附件。
- 边界：员工岗位包、Skills、提示词、API 和配置继续在后台权威装载；仅管理角色可从次级入口查看，不在公开主界面平铺。
- 验收：任务历史可重建对话，执行中显示真实阶段，完成后同页阅读报告；刷新不丢任务。

### [ ] 切片 65｜演示租户报告优先策略

- 任务：在 `data_mode=demo` 下将非安全类质检降为 advisory，保留真实模型报告；`live` 继续严格。
- 边界：非真实 API、零 token、空输出、内部档案泄漏、伪造/越权来源和外部不可逆动作仍 fail closed。
- 验收：同一软错误在 demo 保存报告并带 warnings，在 live 失败；硬错误两种模式都失败并退款。

### [ ] 切片 66｜权威来源文件交付

- 任务：按 `sourceType/sourceId` 从服务端权威正文懒生成并持久化 PDF、DOCX、XLSX、PPTX，提供稳定下载。
- 边界：租户/owner 权限、内容哈希幂等、MIME、SHA-256 和安全文件名；浏览器不能伪造源正文。
- 验收：餐饮任务和内容 run 均可生成、刷新回查和下载；相同正文/格式不重复生成。

### [ ] 切片 67｜内容团队对话化与内部自动流转

- 任务：0→9 内容团队保留真实工位状态机，把创建/进度/报告/产物改为对话呈现；公开入口默认 `internal_auto`。
- 边界：外发、付费和不可逆动作仍需授权；失败、账务待对账和真实 provider 状态不能伪装成功。
- 验收：任务从 0 到 9 不因普通审阅停机，最终报告和原生工位产物可读可下载。

### [ ] 切片 68｜本地端到端与工程门

- 任务：重启 3107，浏览器实测对话、执行可视化、报告排版和文件下载；执行服务端/前端/隔离/格式/构建门禁。
- 验收：至少餐饮单岗、内容单岗、内容团队各一条本地可见链；无控制台错误、无悬挂 hold、无真实外发。
