# 纳米Work企业版本最终验收记录

验收时间：2026-07-25（Asia/Shanghai）

## 当前结论

- 新项目最新代码已在 `http://127.0.0.1:3107` 启动，健康检查返回 `ok=true`、`db=up`。
- 后端、前端类型、严格 ESLint、租户隔离扫描和生产构建均通过。
- 运行态 API、70 位员工完整档案、岗位专属派活、Paihuo 内容生产部、每日/每周自动任务和立即自动运行均已验证。
- 旧初始化内容已全部降回待审核并清除虚构发布效果；内容效果榜不再把参考草稿当作真实经营成效。
- 操作者刷新 3107 页面后，应用内浏览器已完成桌面端 1440×900、移动端 390×844 的真实页面验收；关键页面和员工工作台无阻断问题，浏览器控制台无错误。

## 项目边界

- 本次所有写入都位于 `/Users/wanglei/Documents/纳米work企业版本` 及其独立预览数据库。
- 旧项目仅作为只读参考，本次没有向旧项目源码、数据库或运行目录写入。
- 新项目使用独立端口 3107 和独立数据库 `server/data/nanowork-preview.db`。
- 启动前 3007 没有监听进程；本次没有启动、停止或修改任何 3007 服务。

## 70 位数字员工

程序化全量检查结果：

- 员工总数：70（餐饮 60，Paihuo 内容员工 10）。
- 70 位员工都具备身份、能力、工作方式、技能库、提示词、工作配置、岗位档案和单独派活合同。
- 70 条任务标题指引全部唯一，70 条任务要求指引全部唯一。
- “本月食材成本上涨”“门店真实情况”“这次要解决什么问题”等通用错位占位语命中数为 0。
- 出厂岗位 Skill：70 个；历史技能卡：409 张（餐饮 344，内容 65）。
- 运行态抽查：
  - #101 餐饮市场机会研究：指引为商圈、品类机会和 90 天验证；
  - #136 需求驱动排班：指引为分时需求、员工技能与劳动规则；
  - #0 趋势官：指引为热点、账号人设和候选选题；
  - #7 演绎师：指引为移动端 HTML 演绎长页。

## 内容生产仓

- 运行态返回 10 位正式内容员工：趋势官、情报员、拆解师、撰稿人、文风师、多媒体师、封面师、演绎师、分发官、复盘官。
- 质量审查服务不计作第 11 位员工。
- 每位员工执行岗位专属输出契约；契约失败不能采纳。
- 素材引用、不可变快照、人工审阅、媒体验收、知识库来源关系和两阶段计费均已进入后端闭环。
- 自动化时区为 `Asia/Shanghai`，边界为“只生成系统内容，不自动发布”。
- 预览库已建立：
  - 每日餐饮热点雷达：趋势官，热点扫描，每日 09:00；
  - 每周内容效果复盘：复盘官，复盘报告，每周一 09:30。
- “每日餐饮热点雷达”已立即运行一次：
  - 运行状态：成功；
  - 输出契约：通过；
  - 产物状态：待审核；
  - 计费状态：已结算；
  - 外部发布：未执行。

## 驾驶舱与界面文案

- 页面名称统一为“老板驾驶舱”。
- 合成演示横幅、旧“门店驾驶舱”、发布编号和技术版本/修订标签已从核心界面移除。
- 新增回归测试，持续禁止核心界面重新出现上述文案。
- 底层审计哈希、内部快照和数据库修订字段仍保留，不在页面显示技术版本号。
- 老板驾驶舱卡片、员工榜、分部入口和自定义模块已补齐键盘操作与语义状态。
- 内容生产仓的创作频道已改为语义按钮；移动端自动化规则改为卡片布局，不再被固定操作列遮挡。

## 初始化参考数据真实性

- 当前预览库中原有 130 条初始化内容均为“待审核”，发布渠道、浏览量、线索量全部为 0。
- 初始化内容统一标为“待企业核验”，AI 模式统一为本地模板，不再冒充外部模型生成。
- 内容效果 TOP 运行态返回 0 条，不再把初始化参考草稿计入传播或线索成效。
- 12 条初始化知识建议与 3 条会诊建议已改为正向待核验表述；旧“演示资料”“演示会诊”“正文示例”命中数均为 0。
- 修改前预览数据库已保存可恢复快照，并在修改后再次通过 SQLite 完整性检查。

## 自动化验证

最终执行 `npm run verify`，并补跑严格 ESLint：

- Node 后端测试：341 / 341 通过；
- TypeScript：通过；
- 严格 ESLint：0 error，0 warning；
- 多租户隔离扫描：无高危失败，36 处动态作用域查询列为人工复核项；
- Vite 生产构建：通过；
- SQLite `PRAGMA integrity_check`：`ok`。

构建仅有 Vite 对 `advancedChunks` 的弃用提示，不影响当前构建或运行。

## 运行态

- 3107 监听进程：新项目 Node 服务。
- 健康接口：通过。
- 老板、企业管理员、平台超级管理员三种账号均已实际登录验证。
- `npm run accept:runtime` 已实际登录当前 3107，逐个核对 60 位餐饮员工与 10 位内容员工：70 个完整岗位工作台、70 条唯一标题指引、70 条唯一要求指引全部通过。
- 同一运行态验收同时确认 Paihuo 内容员工 10 人、每日自动任务 1 条、每周自动任务 1 条、上海时区和禁止自动外发边界。
- 企业共享积分池：200000 分。
- 当前 AI 模式：本地模板引擎；未配置外部模型密钥时不会冒充联网或外部生成。

## 浏览器与视觉验收

应用内浏览器在操作者刷新后完成了以下真实页面检查：

- 老板驾驶舱：页面、侧栏均使用“老板驾驶舱”；未出现旧“门店驾驶舱”、合成演示声明、V1.0 或其他技术版本标签。
- #101 赵先机：逐页打开并确认“单独派活、能力、工作方式、技能库、提示词、工作配置、岗位档案”七个完整面板；派活材料、标题示例和交付物均匹配市场机会研究。
- #136 喻排班：七个面板完整；派活指引要求排班周期、分时需求、技能覆盖和劳动规则，不再出现食材成本等错位占位语。
- #0 趋势官：逐页确认七个完整面板；派活指引围绕当前热点、账号人设、平台信号和五个候选选题。
- #7 演绎师：七个面板完整；主交付为移动端 HTML 演绎长页，PPT 明确为按需附加交付。
- 内容生产仓：确认 Paihuo 内容部门 10 位员工、手动创作、立即自动、定时任务三个入口；每日热点与每周复盘规则均启用，自动产物必须人工审核且不会自动发布。
- 桌面端 1440×900：老板驾驶舱、内容员工区、自动化规则表和完整员工抽屉均可读可操作。
- 移动端 390×844：员工工作台保留七个可横向访问页签；自动化规则使用卡片布局，两条规则的状态、计划和操作均完整显示。
- 浏览器控制台错误：0。

验收截图保存在当前 Codex 任务的可视化工件目录，包含老板驾驶舱、内容员工部门、桌面员工工作台、桌面自动规则、移动自动规则和移动员工工作台。
