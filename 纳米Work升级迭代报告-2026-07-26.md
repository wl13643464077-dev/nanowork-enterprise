# 纳米Work行业版 · 升级迭代报告

> 出具日期：2026-07-26
> 工作内容：① 全网同类产品调研（2026-07-26 实时检索）② 对外部审计建议文档的逐条评估 ③ 第一轮升级迭代实施（已完成并合入本仓库）④ 后续路线图
> 验收基线：后端测试 **341/341 全绿**，TypeScript 检查通过，多租户隔离扫描无高危，生产构建成功

---

## 一、执行摘要

本轮迭代围绕两个最痛的问题动手：**「UI 太烂、登录页不动态不高端」** 和 **「假功能损伤客户信任」**。已完成并验证：

| # | 已落地 | 效果 |
|---|---|---|
| 1 | **登录页整页重做**（CSS mesh 动态渐变 + 0-800ms 分层进场编排 + 玻璃拟态卡片） | 删除 2018 年套路的 canvas 粒子连线（O(n²) 每秒 8.6 万次距离计算归零）；明暗双主题原生支持；`prefers-reduced-motion` 降级 |
| 2 | **AuthShell 统一认证三页**（Login/Register/Pending） | 三页背景、品牌、卡片规格、按钮渐变、输入框高度完全一致，登录→注册不再像「跳到另一个产品」 |
| 3 | **设计 token 层建立**（theme.css） | 间距 8 档（8px 网格）/ 圆角 3 档 / 字号 7 档 / 阴影 3 档 / 动效时长与缓动 / 中性灰阶 5 级 / 语义色唯一化 / z-index 层级 / 断点 5 档；全局 `:focus-visible` 焦点环 |
| 4 | **登录页假数据全部删除** | 「今日营业额 ¥18,642 / 午市翻台率 2.34」等编造数字清零；「服务 6 个行业」收敛为「深耕餐饮 · 更多行业陆续开放」；替换为真实产品闭环展示（经营数据→员工诊断→派活执行→结果回流） |
| 5 | **「AI音频」纯装饰 Tab 移除**（前端 4 处 + 后端统计字段） | 用户不会再「点生成音频得到一段文字」 |
| 6 | **经营工具箱诚实化标注**（8 个工具） | 「约 X 积分」假计费标注全部改为「免费 · 模板整理」，页头与运行提示明示「当前不调用外部 AI 模型」 |
| 7 | **客单价默认值修正**（8000 → 120，4 处） | 消除上一代招商产品残留；经营健康度不再被系统性算错 |
| 8 | **知识库 RAG 宣传改条件渲染** | 未配置 AI 密钥时明示「关键词/热度检索（降级模式）」；删除与工程事实相反的「知识库越大越准」 |
| 9 | **资产估值不再凭空生成** | 未填估值按 0 记入「信息不完整资产」清单，不再按分类编造默认值混入「资产总价值」 |
| 10 | **`analysis.js` 的 `SELECT 1` 占位清理**、移动端「60 名员工」与桌面端「70 岗」表述统一、README 工具箱宣称对齐实际 | 文档与实现一致性修复 |
| 11 | **xlsx 供应链风险解除**（CDN tgz → npm 镜像包 `@e965/xlsx@0.20.3` 别名） | 本轮构建中 `cdn.sheetjs.com` 被网络策略拦截导致 `npm install` 失败，审计文档预警的「构建单点故障」当场应验；现已回到 npm registry 管辖（audit/Dependabot 可覆盖），前端代码零改动 |

**未在本轮做的高优先级项**（原因与计划见第五部分）：支付通道、餐饮数据模型 8 类表、抖音/企业微信集成、RAG 重构、DuckDuckGo 替换、90 处静默吞错清理。

---

## 二、对外部建议文档的评估

外部审计文档（《纳米Work行业版·升级迭代报告》，全量静态读码 + 竞品调研）整体质量高，量化证据扎实。逐块评估结论：

### 2.1 采纳并已执行
- P0 止损清单（登录页假数据、AI音频、客单价、`SELECT 1`、资产估值、RAG 宣传）——**全部采纳，本轮完成**
- UI 诊断（无设计系统是根因、不换 antd 而建 token 层、登录页方案 A「CSS mesh + 进场编排」优于 WebGL shader）——**全部采纳**；token 层与登录页已按其技术要点落地（毛玻璃 0.66-0.72 透明度、字距 -0.5px、字重 700、缓动统一、reduced-motion 降级）
- 工具箱「选项 B：诚实标注模板模式」——**采纳**（选项 A「接真 AI」列入下轮，可复用现成的 `INPUT_SCHEMAS` 校验层与两阶段计费）

### 2.2 采纳但按实际情况修正
- **「餐饮员工派活主入口前端 0 调用」——该结论对当前仓库不成立。** 经代码核实：`Employees.tsx:210` 以 `domain="restaurant"` 打开员工工作台，工作台「单独派活」Tab 提交到 `POST /employee-workbench/restaurant/:idx/dispatch`（`employee-workbench.js:72`），该路由直接调用 `dispatchMarshalTask`（两阶段计费/审批/退款完整链路）。审计对象是旧的本地副本，GitHub 仓库已接通。**剩余真实缺口**：餐饮任务派发后无状态轮询/进度露出（content 域有，restaurant 域没有），已列入下轮。
- 客单价修正：文档建议「按业态分档 + 首次登录引导确认」（1 天工作量）；本轮先落地默认值 120 并注明分档参考（快餐 20-40 / 茶饮 15-30 / 正餐 80-150 / 火锅 100-200），引导流程列入下轮。

### 2.3 采纳但需要补充最新市场情报（见第三部分）
- 文档的竞品结论方向正确，但截至本轮检索又有关键变化：**美团智能掌柜已从「问答式」升级为「主动管店」（每日主动巡检+归因+推送），北京试点，预计 2026-08 全国铺开**。这把「AI 帮老板看数据」的竞争门槛又抬高了一级，进一步支持文档「不要在看报表维度正面竞争」的判断。

### 2.4 评估后建议谨慎对待
- 「51 个死接口」具体清单基于旧副本，需对当前仓库重新扫描后再逐个决策（接通/删除/flag），避免误删已接通的接口（派活入口即为一例）。
- 「Agentic Resource Planning（ARP）」在本轮检索中**尚未被分析师机构确立为独立品类名**，相关叙事散落在 Agentic ERP/Agentic Enterprise 中。路线 C（治理层）方向成立，但对外话术建议使用更稳的「AI 数字员工治理 / AI Workforce Governance」表述。

---

## 三、全网竞品调研（2026-07-26 实时检索）

### 3.1 直接竞品（餐饮经营 AI）

| 竞品 | 关键事实（检索验证） | 对纳米Work的含义 |
|---|---|---|
| **美团智能掌柜** | 单店版累计约 70.8 万商家、解决 660 万经营问题，累计服务超 100 万商家（美团官方，2026-03）；品牌顾问（连锁版）超 50 家品牌试用，30+ 品牌生成 16 万份报表、省 65% 人工；**2026-06/07 升级「主动管店」：每日主动巡检+营业额归因+商圈对比推送，预计 2026-08 全国铺开**；对合作商家免费 | 「免费问答式 AI + 看报表」已被平台封死；且交互基准已升级为「主动推送」——纳米Work已有的 scheduler（原子认领+重启恢复）应迁移到「每日经营诊断卡片」场景，这是现有资产最高价值复用 |
| **客如云（阿里本地生活）** | 五大智能体 2025-10 发布（老板助手/营销助手/超级店员/超级IT/智能小On）；2026「AI+生态」战略，宣布五大智能体**免费开放**；14 年积累 181 万商家数据、1200+ 服务商 | 「AI 助手」本身不再构成付费理由；无数据管道的 AI 建议先天可信度不足 |
| **百度一见 / Hogee** | 一见：视觉智能体，1000+ 视觉 Skills，某头部咖啡 3 万门店全量接入、识别准确率 80%→95%；Hogee：AI 营销团队，经营分析→策略→物料→上屏闭环 | 验证了「定时自动执行 + 异常主动汇报」是被市场买单的产品形态 |
| **瑞幸 AI Lucky** | 2026-07 WAIC 首秀，C 端消费智能体（自建，非售卖 SaaS） | 品牌自建路线，非直接竞争 |

### 3.2 通用平台（定价锚点）
- **钉钉悟空**：邀测免费，第三方预估商业化 ¥99-199/人/月
- **飞书 Aily**：挂靠套餐，AI 企业版 Plus 约 29.7 万/年、旗舰约 99 万/年（中大企业项目制）
- **含义**：单店端付费能力对标「兼职人力成本的零头」（几百元/店/月量级）；连锁总部端走项目制/年费价格带。纳米Work积分制需能对照这个锚点自圆其说。

### 3.3 国际 AI Workforce 治理品类
- **ServiceNow**（Knowledge 2026）：AI Control Tower 扩展为治理**任何来源**的 agent（30 个新集成），发布 Otto 统一入口与 Action Fabric 开放执行层，主题词「Autonomous Workforce」
- **Atomicwork**（$40.3M 累计融资）、**Sema4.ai**（Snowflake 投资，Transparent Reasoning 可视化推理）
- **含义**：企业买单的是**可审计、可追溯、可授权**的 agent。纳米Work已有的两阶段计费、prompt 哈希快照、审批边界、产出穿透溯源，恰是这个品类的核心资产——这是「70 个 JSON 人格」之外真正值钱的东西。

### 3.4 2026 UI/UX 基准（本轮登录页重做的依据）
- B 端基准：Linear 式极简 + 渐进披露；design token（20-30 个原语）+ 8px 网格；品牌色 ≤10% 界面面积
- 登录页：split-screen + 细腻 mesh gradient / 磨砂玻璃卡浮于慢速漂移色团（纯 CSS，无需 canvas/WebGL）——**本轮已按此实现**
- 无障碍硬约束：WCAG 2.2 AA（正文对比 4.5:1、UI 3:1、2px 焦点环）；欧盟 EAA 2025-06 已生效。餐饮老板常在后厨强光/夜间弱光看手机，高对比本身就是留存功能

*主要来源：meituan.com、news.cn（新华网）、21jingji.com、chinadaily.com.cn、keruyun.com、cloud.baidu.com、finance.people.com.cn、newsroom.servicenow.com、sema4.ai、feishu.cn、saasui.design、eleken.co 等；无法验证项（客如云/百度定价、悟空正式定价等）已在调研底稿中标注。*

---

## 四、战略结论（维持并强化外部文档的判断）

1. **路线 A（收缩定位：餐饮内容与营销 AI 工作台）为主线**——content（3,240 行 + 输出契约）、营销活动闭环（26 接口）、会员 CRM（20 接口）是评分 7-9 的真能力；不做收银替代，做「平台之上的 AI 内容与营销中台」。
2. **路线 C（AI 数字员工治理）作为差异化卖点同期包装**——计费原子性、审批不可绕过、产出可溯源、三层隔离是国际品类正在定义的核心能力，对连锁总部是销售语言。
3. **路线 B（正面补齐门店经营）仅在拿到连锁订单后启动**——美团「主动管店」8 月全国铺开后，这个维度的正面竞争窗口进一步收窄。
4. **交互基准升级**：行业已从「用户提问」进入「AI 每日巡检+主动推送」。下轮应优先把现有 scheduler 迁移为「每日一条经营诊断卡片」（成本低、感知强、可差异化）。

---

## 五、本轮变更明细（代码级）

### 5.1 前端
- `web/src/theme.css`：新增设计 token 层（间距/圆角/字号/阴影/动效/灰阶/语义色/层级/断点）+ 认证页主题变量（明暗两套）+ 全局 `:focus-visible`；修掉硬编码 antd Alert 补丁
- `web/src/components/AuthShell.tsx` + `AuthShell.css`（新增）：认证三页共享外壳——mesh 动态背景（4 色团 transform 漂移，GPU 合成）、品牌六边形 SVG 描边 draw-in、进场编排（blur+位移+错峰）、玻璃卡片、统一表单规格、统一断点（1024/640）、reduced-motion 降级
- `web/src/pages/Login.tsx`：整页重写。删除 canvas 粒子（~70 行）、双轨道、网格层、9KB CSS 模板字符串、`LIVE_SIGNALS` 假数据、6 行业轮播；表单逻辑保持不变
- `web/src/pages/Register.tsx` / `Pending.tsx`：迁移到 AuthShell；`<a onClick>` 假链接改为 `<button>`
- `web/src/pages/ContentFactory.tsx`：移除「AI音频」全部 4 处引用
- `web/src/pages/Toolbox.tsx`：8 个工具卡改「免费 · 模板整理」；页头与提交行明示不调用外部 AI；「结果已回流经营数据」改为「已保存到工具运行记录」
- `web/src/pages/System.tsx`：RAG 宣传改条件渲染（按 `ai.available`），删除「知识库越大越准」
- `web/src/pages/Mobile.tsx`：员工数量表述统一
- `web/package.json` / `package-lock.json`：`xlsx` 改为 `npm:@e965/xlsx@0.20.3`（SheetJS 官方构建的 npm 镜像，API 完全一致）

### 5.2 后端
- `server/src/baseline.js`、`routes/system.js`、`routes/execution.js`、`engines/plans.js`：`avg_deal_amount` 默认 8000 → 120（附业态分档注释）
- `server/src/routes/analysis.js`：删除 `SELECT 1` 占位
- `server/src/routes/assets.js`：删除 `defaultValue()` 编造估值；未填估值记 0 进「信息不完整资产」
- `server/src/routes/content.js`：移除永远为 0 的 `audio` 统计字段
- `server/src/routes/toolbox.js`：运行完成文案诚实化
- `README.md`：工具箱宣称与实现对齐

### 5.3 验收结果
- 后端测试：**341/341 通过**
- TypeScript 检查：通过
- 多租户隔离扫描：无高危（36 处待复核为既有基线）
- 生产构建：成功；登录页已无 canvas，动效全部走 transform/opacity
- 截图验证：明/暗双主题 × 桌面/移动端布局正确，闭环卡与页脚无遮挡

---

## 六、下一轮迭代路线图

### 第二期 · 真数据与真闭环（建议 4-6 周）
| 优先级 | 事项 | 说明 |
|---|---|---|
| P0 | 每日经营诊断卡片（主动推送） | 复用 scheduler + 飞书通知；对齐 2026 交互基准，差异化感知最强 |
| P0 | 迁移基建 + 第一批 4 张表（stores/dishes/order_items/costs） | 先冻结 `migrateV2()`，新表全部进隔离扫描清单；解锁真实客单/菜品排行/毛利 |
| P0 | 微信支付 + 支付宝（收费 SaaS 生死线） | 复用 `content.js` 幂等发布范式做回调幂等 |
| P0 | 餐饮派活任务状态轮询/进度露出 | 补齐本轮核实出的真实缺口（content 域模式已有，迁移即可） |
| P1 | 90 处 `.catch(()=>{})` 清理（先修 5 个整页静默） + `useQuery` 推广 | 关掉 `allowEmptyCatch` 防回归 |
| P1 | RAG 重构（预筛 + LIMIT + 移出主线程；评估 sqlite-vec） | 解除 ~500 文档天花板 |
| P1 | DuckDuckGo 替换为国内可用商业搜索 API（博查/Tavily 等，多源 fallback） | 目前国内部署联网搜索 100% 失败 |
| P1 | 工具箱接真 AI（选项 A）+ 两阶段计费 | 校验层与计费基建现成 |
| P1 | 长任务反馈（`api.stream` 推广 + 后台任务 hook + 取消按钮） | 消除最长 135 秒无进度等待 |
| P2 | 死接口重扫描逐个决策；业务数据 Excel 导出；枚举统一下发 | |

### 第三期 · 集成与规模化（建议 6-8 周）
抖音生活服务 OpenAPI（先启动服务商资质流程）→ 企业微信集成（私域必经入口，当前零接入）→ 10 个高价值餐饮岗位输出契约 → 多门店/连锁总部视图 → 超大文件拆分（`content.js` 3240 行 / `ContentFactory.tsx` 2701 行）。

### 防回归机制（建议立即生效）
1. CI 检查：禁止新增硬编码 hex 颜色（`rg '#[0-9a-f]{6}' web/src --glob '!theme.css'` 增量为 0）
2. 装 `eslint-plugin-jsx-a11y`；关 `allowEmptyCatch`；`chunkSizeWarningLimit` 1600 → 800
3. 每期结束重跑本报告量化指标（硬编码色值数、内联 style 数、`.catch(()=>{})` 数、死接口数）写入 CHANGELOG

---

# 附录 · 第二轮迭代（同日完成并推送）

第一轮推送后继续执行的第二轮升级，全部通过 `npm run verify`（后端 341/341、typecheck、隔离扫描、生产构建）。

## A1. 第二轮补充调研（体验细节对标，2026-07-26 检索）

- **美团「主动管店」每日推送结构 = 涨跌总结 → 转化环节归因 → 商圈竞对对比 + 建议**；商家 30 秒看到评价聚类结论（来源：meituan.com、央广网、21jingji.com）
- **Shopify Home 洞察卡：每日最多 3 条、24 小时过期、可 dismiss 并附反馈理由**——刻意稀缺防噪音（help.shopify.com、changelog.shopify.com）
- **Toast 每天固定 9 点邮件推送昨日经营摘要**——推到商家习惯渠道，不等登录（support.toasttab.com）
- **骨架屏实证**：同等时长下用户感知内容加载「快至多 50%」；共识为「系统动作用 spinner、内容加载用内容感知骨架」（uxdesign.cc、logrocket.com）
- **45 秒以上 AI 任务只放 spinner ≈ 放弃触发器**；主流为「流式+分阶段+可取消+可转后台」四件套（particula.tech、franciscomoretti.com）
- **餐饮驾驶舱 KPI 收敛为 6-8 个**：营业额、单量、客单价、食材成本%、人力成本%、prime cost%（≤60% 目标）（xenosoftsolutions.com、databox.com）
- **antd 5 暗色破功点清单**：静态方法组件拿不到主题、硬编码色、图表库不跟随 token（ant.design、github.com/ant-design）

## A2. 第二轮已落地变更

| # | 变更 | 依据 |
|---|---|---|
| 1 | **驾驶舱新增「今日经营诊断」主动卡片**：最多 3 条（问题 + 归因维度 + 建议 + 一键问参谋），无数据/无权限时整卡隐藏不硬生成 | 对齐美团主动管店与 Shopify 每日 3 条模式 |
| 2 | **切页全屏白屏根治**：MainLayout 内容区加 Suspense + 内容感知骨架（`PageSkeleton`：KPI 卡组 + 图表区骨架），切换页面保留侧栏与头部 | 骨架屏感知提速实证 |
| 3 | **整页静默吞错修复（第一批 3 页）**：Platform（5 路数据全挂时显示错误态）、Recharge、Dashboard 加 `ErrorState` 组件（平实文案 + 局部重试按钮） | 错误态三要素标准 |
| 4 | **经营工具箱接真 AI**：`generateToolboxRun` 调用 `engines/ai.js` 三级降级链（云雾→Claude→模板），AI 生成时 provenance 如实标注 `api` 模式与模型名，模板回退保留；提示词硬边界禁止编造数据/暗示联网。仍免费不消耗积分（计费接入列下轮） | 8 个一级功能从"纯模板"变真 AI |
| 5 | **餐饮派活进度露出**：派活后显示四阶段 Steps（已派发→AI生成中→待审阅→已完成），生成中每 5 秒轮询 `/marshals/tasks/:id/status`（带可见性门控），失败明示积分已退回 | 补齐第一轮核实出的缺口 |
| 6 | **轮询治理**：通知 60s 轮询加可见性门控且不再随切页重建；飞书 OAuth 轮询从固定 1.8s 无上限改为指数退避（1.8→3→5→8s，5 分钟封顶）；Dashboard/System 30s 轮询后台标签页零请求，回前台立即刷新 | 挂机 8 小时 ≈ 2880 次无效请求归零 |
| 7 | **深色主题对比度修复**：MainLayout 帮助抽屉 `#3d4a5f`（深字压深底近不可读）与通知时间 `#c2c9d6`（压白底 1.8:1）改为主题 token | WCAG AA |
| 8 | **员工列表性能**：搜索 200ms 防抖（原逐字符触发 70 卡重算）；卡片加 `content-visibility: auto` 跳过屏外渲染 | |

## A3. 下一轮优先项（更新后）

1. 每日诊断升级为「涨跌→归因→建议」三段式规则版（环比降幅最大环节定位），并接飞书/站内每日定时推送（复用 scheduler）——对标 Toast 9 点日报
2. 诊断卡 dismiss + 反馈收集（不准确/不重要/已处理）——对标 Shopify
3. 支付通道（微信 Native + 支付宝当面付）；工具箱两阶段计费接入
4. 迁移基建 + stores/dishes/order_items/costs 四表；KPI 首屏按 6-8 个收敛并补毛利/成本占比
5. 剩余 ~80 处 `.catch(()=>{})` 分批清理 + `useQuery` 推广 + 关 `allowEmptyCatch`
6. RAG 重构、DuckDuckGo 替换、`api.stream` 推广 + 取消按钮、长任务转后台+通知

---

# 附录 · 第三轮迭代（同日完成并推送）

围绕「主动式 AI」这一 2026 交互基准的核心落地轮。全部通过 `npm run verify`（341/341、typecheck、隔离、构建），日报引擎另做了空库与有数据两组冒烟测试。

## B1. 已落地变更

| # | 变更 | 说明 |
|---|---|---|
| 1 | **每日经营日报引擎**（`engines/daily-digest.js`，新增） | 规则版三段式：【昨日】营收与前 7 日均值涨跌 →【归因】漏斗卡点（低于基准的环节）/ 超期跟进积压 / 缺数据 →【建议】按卡点映射到具体产品动作（邀约话术、到店确认、成交方案、复购触达）。双口径取数（orders 优先、daily_ops 兜底）；**完全无数据时不生成**（宁缺毋滥，Shopify 门槛原则） |
| 2 | **每日 08:00 定时推送**（scheduler 新增 job） | 复用原子认领 `runOnce` 幂等机制（`daily-digest:日期`），逐租户推送给老板与运营负责人的站内通知；企业已绑定飞书群机器人时同步推送飞书（异步、失败不影响主流程） |
| 3 | **驾驶舱日报头条**（`GET /dashboard/daily-digest`） | 「今日经营诊断」卡顶部新增昨日日报头条（涨跌 Tag + 归因 + 建议 + 数据口径标注），与 3 条诊断项合并为一张主动卡 |
| 4 | **诊断卡可关闭 + 反馈**（Shopify 模式） | 「今日不再显示」附理由（不准确/不重要/已处理），当日隐藏次日自动恢复；理由本地留存，后续可上报做排序信号 |
| 5 | **RAG 容量护栏**（`ai.js`） | 候选集按热度预筛 200 篇 + 分块查询 LIMIT 2000：消除全表拉取 embedding（原 1000 文档单次 ~90MB、阻塞全服 3-5 秒）与超 SQLite 变量上限直接报错两个致命点；超限时如实打点告警。完整向量索引（sqlite-vec）仍列下轮 |
| 6 | **联网搜索多源化**（`websearch.js` 重写） | 按已配置 Key 依次尝试 博查 → Tavily → Serper，全部未配置/失败时兜底 DuckDuckGo；统一结果结构与失败降级 note，保留 `wrapUntrusted` 防注入包裹；`.env.example` 补三个 Key 位。解决「目标用户在国内、唯一检索源在国内不可达」的 100% 失败问题 |

## B2. 验证

- `npm run verify` 全绿：后端 341/341、TypeScript、隔离扫描、生产构建
- 日报冒烟：空库 → 正确跳过不硬生成；含 8 天营收数据 + 漏斗数据 → 正确输出「营收 ¥3,200 较前 7 日均值下降 27% → 卡在已沟通环节 → 邀约话术建议」并写入老板站内通知

## B3. 剩余路线（按优先级）

1. **支付通道**（微信 Native + 支付宝当面付）——需要商户号资质，代码侧可先行做回调幂等框架
2. **迁移基建 + stores/dishes/order_items/costs 四表**——先冻结 `migrateV2()`，新表入隔离扫描清单
3. 工具箱两阶段计费；`api.stream` 推广到全部文本生成入口 + 取消按钮；长任务转后台+完成通知
4. 剩余 `.catch(()=>{})` 分批清理（System 27 处、ContentFactory 17 处）+ 关 `allowEmptyCatch`
5. RAG 向量索引重构（sqlite-vec）；企业微信集成；业务数据 Excel 导出；超大文件拆分

---

# 附录 · 第四轮迭代（自主并行开发，5 个 worktree 子代理）

按《docs/自主开发计划-2026-07-26.md》分派 5 个 git worktree 子代理并行开发，逐个验收合并回收。合并后全量验证：**测试 363/363 全绿（新增 22 个用例）、TypeScript 通过、ESLint 0 error、隔离扫描无高危、生产构建成功、服务引导冒烟通过**。

## C1. 五个子代理交付

| 代理 | 交付 | 新增测试 |
|---|---|---|
| **A · 餐饮数据模型** | stores/dishes/order_items/costs 四表（tenant_id 内建 + 隔离监控登记）；`/api/store-data` CRUD + KPI（真实客单价/菜品TOP/毛利率/盈亏平衡，**无数据一律返回 null 不估算**）；dashboard `||` 双源短路修复（显式口径 + revenueSource 字段）；「门店数据」管理页三 Tab；驾驶舱新增真实客单/毛利 KPI 卡（null 整卡不渲染） | 9 |
| **B · 导出与枚举** | `/api/meta/enums` 统一枚举下发；Growth 客户/Activities 活动/Execution 任务三处「导出 Excel」；Mobile 枚举单源化（删 STAGE_COLOR 副本）+ TabBar role=tab 键盘可达；Growth 身份枚举改接口下发（带回退） | — |
| **C · 系统与资产** | 飞书按 V3 语义接通手动绑定接收人 + applink 入口（并发现审计报告「扫码绑群」与后端 V3 实现脱节：群绑定接口为废弃桩）；System 整页静默改 allSettled + 错误态重试；Analysis/Assets 数据来源地图露出（指标→事实表→样本穿透）；资产单条创建/编辑 + 未估值标签 + 台账导出 | 6 |
| **D · 长任务与壳层** | api 客户端支持外部 AbortSignal（取消不弹全局错误）；内容工厂媒体/文案生成「取消生成」按钮 + 耗时预期提示（后端核实：取消即退占扣，「不计费」文案属实）；17 处静默 catch 逐处处置（8 处转显式报错、9 处注明保留理由）；MainLayout 5 个图标热区按钮化 + emoji→icons + pressable 键盘可达；jsx-a11y 插件（warn 级，0 error / 198 存量警告可见化） | — |
| **E · 支付框架** | `engines/payment.js` 微信 Native APIv3 + 支付宝当面付（纯 node:crypto 零新依赖）；下单→扫码（antd QRCode + 3s 可见性门控轮询）→回调（免登录、验签即鉴权：时间戳防重放/平台证书验签/AES-256-GCM 解密/RSA2 验签/逐分金额核对）→幂等入账（BEGIN IMMEDIATE + 单向状态机，**重复回调零重复入账**，与平台手工确认同一记账口径）；未配置通道时旧流程逐字不变；.env.example 全量补齐 | 7 |

## C2. 主会话整合与收尾

- 冲突解决 3 处（System.tsx 加载块、Recharge.tsx 加载与支付并存、MainLayout 头部/帮助抽屉），保留双方语义：错误态 + 可见性门控 + 图标化
- 统一接线：侧边导航新增「门店数据」（analysis 模块权限）
- 老板参谋假四步进度改为真实事件驱动三阶段（提交→首个流式分片→完成）
- 全部合并推送 GitHub；worktree 与分支即时回收

## C3. 遗留与建议（正式上线前）

1. 微信平台证书为静态配置（约 5 年轮转），换证需人工更新；未配置证书时回调安全默认拒绝，可由平台超管手工确认兜底
2. 支付未实现主动对账查询（alipay.trade.query / 微信订单查询），掉单依赖网关重试 + 人工兜底；二维码 30 分钟过期未做自动关单
3. order_items 暂无更新/删除接口；营收口径为订单头合计（KPI note 已注明口径）
4. 198 条 a11y 存量警告待按文件收敛；飞书废弃桩接口（chats/bind/autobind）可在后续版本删除
5. 待决策项保持不变：抖音/企微集成（需资质）、工具箱两阶段计费（需定价决策）、RAG sqlite-vec、PostgreSQL 评估

---

*报告完 · 2026-07-26（含同日第二、三、四轮迭代附录）*
