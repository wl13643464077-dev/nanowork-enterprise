// 旧项目提示词里的阶段标签是历史迁移说明，不属于数字员工的岗位能力。
// 新项目保留旧源码 SHA 作为溯源证据，但运行文案使用当前真实能力边界。
export function currentPaihuoSourceTemplate(value) {
  return String(value)
    .replace(
      "V1 适配层用联网检索近似替代平台爬虫;商业授权闭环后切换为真实采集",
      "当前运行通过联网检索补充公开资料;接入合法授权采集连接器后再启用平台采集",
    )
    .replace("V1 只用其文案生成职责", "当前只使用其文案生成职责")
    .replace(
      "V1 未接 muapi.ai 付费生图,用精美 SVG 信息图/插画本地渲染,零成本;接入后可切换 AI 生图",
      "当前按任务配置调用已接通的图片能力;供应商不可用时必须明确失败,不得用占位产物冒充",
    )
    .replace(
      'V1 未托管真实平台账号,产出"各平台完整发布包",老板一键复制/打包上传;',
      '未获得平台账号授权时,仅产出"各平台完整发布包",由有权限的人确认后操作;',
    )
    .replace(
      'V1 未接真实平台数据回流,产出"发布后复盘计划+预测性复盘";接入采集后切换为 T+1/3/7 真实数据',
      '未接通真实平台数据回流时,只产出"发布后复盘计划+预测性复盘";接通后使用 T+1/3/7 真实数据',
    );
}
