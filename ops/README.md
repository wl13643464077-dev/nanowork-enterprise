# 运维目录

纳米Work行业版当前只交付本地可运行的新项目，尚未绑定生产域名、服务器账号、系统服务名或备份目录。

为避免误操作，本项目不继承来源项目的 Nginx、systemd、备份和发布脚本。正式部署前应针对目标环境重新制定：

- 独立服务账号与最小权限目录；
- HTTPS、反向代理与可信代理范围；
- `NANOWORK_DB` 的加密备份、校验与恢复演练；
- 密钥注入和轮换；
- 健康检查、日志脱敏、告警和回滚；
- 发布前的 `npm run verify` 验收门禁。

在上述参数经负责人确认前，不提供一键生产部署入口。
